# Silver Pulse · 自我纠错/自我迭代（Loop）落地 + 可落地新技术 + UX 走查 综合方案

> 调研：艾年研究员（走查 + 方案）｜日期：2026-07-16
> 配套参考：`调研_Loop工程落地方案.md`（2026-07-09 旧版，已吸收其结论并向前推进）
> 定位：本文件给小爽看全局，并可直接"派单"给其他 AI / 技能。

---

## 0. 一句话结论（先看这个）

你这个项目最值钱的动作**不是再加更复杂的循环，而是三件已经能做、但被卡住的事**：

1. **把当前 `output/` 部署上线** —— 你本地已经做完 V20 分类（8 个一级）、1505 家企业、收藏/反馈/同步云端全套 UI，但线上还停在 07-07 的旧版（992 家、13 个老分类、无收藏）。**你做完了 90%，只是没让人看到。**
2. **把"自我校验"做厚** —— 死链检测、字段断言、评分一致性抽样、条数骤降硬门槛。零模型、纯脚本，立刻能挡住 90% 的翻车（尤其你第一原则是"数据可信度"）。
3. **激活 L3 反馈闭环** —— 收藏 UI 已经写好，只差恢复自动化去读 `feedback.jsonl`。这是把"小爽每天用网站"变成"系统自我进化燃料"的唯一入口。

"Loop（自我纠错/自我迭代）"对你这个**无后端静态站**，最务实的形态是：**定时触发的"写查分离"小循环 + 保守自适应 + 人工护栏**，不是常驻多智能体。这点我和旧版调研结论一致，下文是具体怎么落。

---

## 1. 走查发现的真实状态（基于事实，不是猜测）

### 1.1 最大的矛盾：代码 / 线上 / 真相源 三重割裂

| 维度 | 线上站点（你给的链接） | 仓库 `output/`（当前构建 07-16） | 真相源 `data/` |
|---|---|---|---|
| 构建时间 | 2026-07-07 21:47 | **2026-07-16 16:15** | 持续更新 |
| 企业数 | 992 | **1505** | 1505（V20 后） |
| 一级分类 | 13 个老分类 | **8 个（V20）** | 8 个 |
| 收藏/反馈 UI | 无 | **有（含同步云端）** | — |

**根因**：07-10 起 5 个自动化（含部署）被暂停，`output/` 是本地最新但从未 `deploy_ghpages.py` 推上 `gh-pages` 分支。

> 影响：用户（和你）现在看到的体验，比项目实际能力落后了 9 天和 513 家企业。所有"线上没有 X"的判断，要先区分"真没做"还是"做了没部署"。

### 1.2 线上站点（07-07 版）的 UX 问题 —— 这些在 07-16 `output/` 多数已修，但仍逐条核对

- **收藏/反馈入口**：线上无；`output/` 已有。→ 属部署问题，非缺失。
- **两个搜索框并存**：首页 filter-bar 内联搜索 + 全宽 `news-search`，功能重叠易混淆。→ **`output/` 仍未合并**，需前端修。
- **雷达 TOP 企业点击不定位**：`href="enterprise.html"` 只跳企业库顶部，不落到具体企业。→ **`output/` 仍如此**，需深链。
- **主题切换在深色系统上切不回浅色**：CSS 用 `@media (prefers-color-scheme: dark){ :root:not(.light){…} }`，但 `toggleTheme()` 只切 `.dark`、从不加 `.light` → 深色 OS 上"浅色"永远不可达。**已确认 `output/` 同一段代码仍在，是真 bug。**
- **资讯外链全是 `news.google.com/rss/articles/...` 跳转链接**：Google News 重定向 URL 极易失效 → 死链，直接击穿"数据可信度"第一原则。
- **推荐理由/研究价值只给数字不给"为什么"**：雷达理由如"上市企业""B2B/B2C 硬件产品"过弱；评分引擎仍是旧 6 维（4 步新架构未实现）。
- **企业库 1505 张卡片全量内联**：移动端可能卡顿（需分页/虚拟滚动）。
- **"今日"措辞与停更数据矛盾**：自动化暂停后数据停在 ~07-09/10，仍写"今日"。

### 1.3 当前 `output/`（07-16）已具备、但线上看不到的能力

收藏、已读、不再显示、备注、收藏标签、同步云端（写 `data/feedback.jsonl`）、V20 八分类、1505 企业、竞争对手/近期热点关联。**这些就是 L3 闭环的"燃料收集器"，只差恢复自动化去消费。**

---

## 2. Loop 自我纠错/自我迭代：原理 + 本项目落地设计

### 2.1 人话版 Loop（不必追术语）

Loop = 智能体**重复"观察→行动→校验→（自适应）→重复"，带明确的停止条件和护栏**。它和"一次性提示"的区别，就在于多了**自我校验（verify）**和**边界控制（stop）**。反面教材是 2023 年的 AutoGPT：因为没有校验、没有停止条件，自己转晕了。

按触发方式分四种（我用中文名避免和你的分类标签 L1/L2 混淆）：
- **触发型**：用户/脚本一句话启动，做完即停（如手动跑一次评分审计）。
- **目标型**：有可验证的出口标准，达标或到轮次上限就停（如"部署门禁：有 CRITICAL 就不发"）。
- **定时型**：固定间隔跑（如每天/每周的healthy快照、评分审计）。
- **主动型**：无人实时在场也按计划转（远景：全自动闭环，需业绩背书才开）。

### 2.2 五个可落地的具体循环（每个都给 trigger/校验/停止/产物/人审）

**循环 A — 评分引擎自纠错（Reflexion 风格：让评分器"自我反思"）**
- 触发：每次自动化跑完评分后（定时型）。
- 做法：取 N 条已评分企业/资讯，用 LLM（DeepSeek V3.2 免费档）按 `about.html` 里写的"4 步评分宪法"重新打分 → 与原分对比 → 生成"自我批评"：哪些维度被旧 6 维系统性遗漏（如反常识、国内可比性）。
- 校验：差异分布统计；某维度系统性偏离即记录。
- 停止：**不自动改分**（防"幻觉自我强化"）。只产出"评分偏差报告 + 建议权重修正"。
- 产物：`data/score_self_audit.md` + `data/score_drift.json`。
- 人审：权重修正进 config 需你拍板。

**循环 B — 标签漂移自愈（Constitutional 风格：用 `about.html` 当"宪法"）**
- 触发：每次标签生成后 + 改 `all_enterprises.json` 后（定时型）。
- 做法：跑 L2→L1 唯一映射校验（你记忆里已建的 `_l2_l1.json`）。发现"同一 L2 挂多个 L1""裸维度词 SaaS/AI"即报错。
- 校验：`validate_tags.py` 9 项全绿。
- 停止：自动修"确定性错误"（映射冲突）；"是否合并某 L2"类判断只出报告。
- 产物：`data/tag_drift_report.md`。
- 人审：合并/删除类需你拍板（按你的铁律：动你手动标注须【动你标注】显式标注）。

**循环 C — 信源健康监控（已有，做厚，P0 最值钱）**
- 死链检测（外链抽样 HEAD，超阈值阻断）、JSON Schema 字段断言、评分一致性抽样（5 条重算误差>0.01 报警）、前后条数骤降硬门槛（今日<昨日×0.6 即 FAIL）。
- 全零模型、纯脚本，并入 `loop_audit.py` / 新建 `self_verify.py`。**立刻能挡 90% 翻车。**
- 护栏：校验脚本异常必须 `exit(1)`，绝不静默通过。

**循环 D — 推荐理由/评分质量 LLM-as-Judge 评测**
- 用 LLM 当裁判，对一批"推荐理由"打分：是否空洞、是否重复、是否点出"创业者价值"。
- 低成本（批处理，DeepSeek 免费档）。直接解决 U6（理由弱）的体验问题。
- 产物：`data/rec_quality_report.md`，驱动"推荐理由变体库"优化。

**循环 E — L3 反馈闭环（收藏 → `feedback.jsonl` → 权重微调）**
- UI 已建（`output/` 的收藏/同步云端）。
- 需：恢复自动化读取 `feedback.jsonl` → `feedback_loop.py` 微调精选权重（±0.03）。
- 护栏：无反馈不自动晋升；以收藏命中率为 ground truth。

**循环 F — 自我记录（loop_memorize）**
- 每天把循环结论蒸馏进 `.workbuddy/memory/`，并标"待固化规则"供你审阅。
- 护栏：自动写 memory 只**追加**，绝不回写 `RULES.md`/改 `config.py`（防 AI 自己改自己规则）。

### 2.3 两条不能破的护栏（对照 AutoGPT 教训）

1. **可量化校验**：校验越可量化，AI 越容易自验（参考文档原话）。死链数、字段缺失数、分数差——都用数字。
2. **停止 + 人工边界**：全进化层零模型（纯统计）、定时触发（非实时常驻）、任何权重/规则"晋升类"动作只出报告你拍板。绝不允许"无限自调用烧积分"或"幻觉自我强化"。

---

## 3. 可落地的火热新技术（按可落地性排序）

> 约束：静态站 + Python 生成器 + 免费 LLM（HY3→DeepSeek V3.2 07-22 后）+ **无后端**。带 ★ 的是我特别推荐优先做的。

| 排名 | 技术 | 落地路径（无后端怎么做） | 工作量 | 对本案的具体好处 |
|---|---|---|---|---|
| ★1 | **轻量隐私分析**（Plausible / GoatCounter / 客户端事件→JSON） | 加一段脚本埋点，GoatCounter 免 Cookie、自托管友好；或用 GitHub Issues/JSON 收事件 | S | 立刻拿到"哪些企业/资讯被看/被收藏"的真实信号，既驱动 UX 迭代，又喂给循环 E 的 L3 |
| ★2 | **LLM-as-Judge 评测循环**（即循环 D） | 构建期批处理，DeepSeek 免费档当裁判 | S/M | 直接抬升推荐理由/评分质量，免费可承受 |
| ★3 | **客户端 TTS 通勤音频**（浏览器 SpeechSynthesis / 构建期 Edge TTS 预生成） | 给"今日选题雷达"加"🔊 听一遍"，移动端用原生语音 | S | 小爽通勤场景直接可用，情感价值高 |
| 4 | **知识图谱 / Graph RAG（赛道地图）** | Python 预计算企业关系（竞争对手/赛道/投融资链）→ 静态 SVG 或交互图 | M | 极契合"以海外为镜"，一眼看清某赛道海外玩家分布 |
| 5 | **异常 / 数据漂移检测**（循环 C 增强） | 评分分布偏移、信源连续失败、标签爆炸告警，纯脚本 | S | 把"翻车"挡在发布前 |
| 6 | **基于收藏的个性化推荐** | 复用循环 E 的收藏数据，规则化（你过去常看"养老机器人"就多推）而非协同过滤 | M | 让"选题雷达"越用越懂你 |
| 7 | **Agentic RAG 问答**（"哪些海外养老机器人值得写？"） | **别上实时 RAG**；改为"构建期预生成高频问答集"或 Cloudflare Worker 无服务器检索 | M/L | 想要"问 corpus"体验时最稳的方案，避开后端 |
| 8 | **多智能体辩论评选题** | 多个 LLM 评审取一致（循环 D 的增强） | M | 提升选题判断稳健性 |
| 观察 | **prompt caching / small-model routing / long-context** | 把 `about.html` 规则做缓存、难判断才调强模型、一次性喂全规则 | S | 降本增效，让免费档更耐用 |

**明确不碰（过度工程）**：向量数据库常驻、RL 自训练、动态派生几百 agent、实时常驻 loop。与参考文档一致——从最简单的解法开始。

---

## 4. UX / UI 走查问题清单（按优先级）

| 优先级 | 编号 | 问题 | 现状 | 建议 |
|---|---|---|---|---|
| **P0** | U1 | 线上站点停在 07-07，与 07-16 `output/` 严重脱节（差 513 家企业、13 vs 8 分类、无收藏） | 线上旧 / 仓库新 | **部署当前 `output/`**（恢复部署自动化或手动 `deploy_ghpages.py`） |
| **P0** | U2 | 主题切换在深色系统上无法切回浅色 | `output/` 仍在 | 改 CSS/JS：加载时按偏好显式加 `light`/`dark` 类，去掉 media 反模式（见 §6 代码片段） |
| **P0** | U3 | 资讯外链全是 `news.google.com/rss` 跳转链接，极易失效→死链 | 线上+output 均存在 | 抓取时存**原始来源 URL** + 循环 C 死链检测 |
| **P1** | U4 | 雷达 TOP 企业点击只跳企业库顶部，不定位企业 | output 仍在 | 深链 `enterprise.html#ent-<id>` 或 `?q=<名称>` |
| **P1** | U5 | 首页两个搜索框并存，功能重叠 | output 仍在 | 合并为一个，或明确"一个筛雷达、一个筛资讯"的分工 |
| **P1** | U6 | 推荐理由/研究价值只给数字不给"为什么" | 旧引擎 | 卡片可展开维度明细；理由文本用循环 D 提质 |
| **P1** | U7 | 企业库 1505 张卡片全量内联，移动端可能卡 | output 新增大 | 分页 / 虚拟滚动 / 懒加载 |
| **P1** | U8 | "今日"措辞与停更数据矛盾 | 两端均存在 | 改为"数据快照：YYYY-MM-DD" |
| **P2** | U9 | 资讯卡片未关联相关企业（企业→资讯有，反向无） | 仅单向 | 资讯页加"相关企业"模块，强化"以海外为镜" |
| **P2** | U10 | 空状态/无结果引导偏弱 | — | 给筛选无结果时的下一步引导 |
| **P2** | U11 | 无障碍（emoji 无 aria、无跳转链接） | — | 内部工具可缓，顺手加 aria-label |
| **P2** | U12 | 企业库"行业媒体"L2 仍是大杂烩 | 另一 AI 范围 | 白名单准入（标记待办，不在此方案动手） |

---

## 5. 给其他 AI / 技能的任务派单（可直接执行）

> 标注：范围边界遵循你的铁律——标签大改归"标签 AI"，评分权重复活归"评分引擎 AI"，部署权在你（研究员不碰）。

- **T1 部署当前 output** ｜负责：你/部署流程 ｜优先级 P0 ｜范围：跑 `deploy_ghpages.py` 把 `output/` 推 `gh-pages` ｜验收：线上显示 1505 家、8 分类、有收藏 UI。
- **T2 修主题切换 bug** ｜负责：前端 AI ｜P0 ｜范围：见 §6 代码，去掉 media 反模式 ｜验收：深色 OS 上能切回浅色。
- **T3 死链检测 + 存原始来源 URL** ｜负责：数据/研究员 AI ｜P0 ｜范围：采集时存 `source_url`；`self_verify.py` 加外链 HEAD 抽样（超 3 死链阻断）｜验收：循环 C 跑通、报告含死链数。
- **T4 雷达深链** ｜负责：前端 AI ｜P1 ｜范围：雷达企业链接带 `#ent-<id>` ｜验收：点击定位到该企业卡片。
- **T5 搜索框合并** ｜负责：前端 AI ｜P1 ｜范围：首页合并为单一搜索或明确分工 ｜验收：无功能重叠。
- **T6 评分自纠错 Loop（循环 A）** ｜负责：评分引擎 AI ｜P1 ｜范围：实现 Reflexion 风格重打分 + 偏差报告，不自动改分 ｜验收：`score_self_audit.md` 产出、权重修正需人审。
- **T7 标签漂移自愈（循环 B）** ｜负责：标签 AI ｜P1 ｜范围：跑 `_l2_l1.json` 映射校验 + `validate_tags.py` 9 项 ｜验收：漂移报告产出、合并类只出报告。
- **T8 LLM-as-Judge 推荐理由评测（循环 D）** ｜负责：内容 AI ｜P1 ｜范围：批处理评理由质量 + 变体库 ｜验收：`rec_quality_report.md` + 理由空洞率下降。
- **T9 L3 反馈闭环重新激活** ｜负责：你（取消自动化暂停）｜P1 ｜范围：恢复读取 `feedback.jsonl` → `feedback_loop.py` 微调权重 ｜验收：有收藏数据后权重生效。
- **T10 知识图谱/赛道地图** ｜负责：前端+数据 AI ｜P2 ｜范围：Python 预计算关系 → 静态交互图 ｜验收：某赛道海外玩家可视化。
- **T11 轻量分析接入** ｜负责：前端 AI ｜P1 ｜范围：GoatCounter/Plausible 埋点 ｜验收：拿到真实浏览/收藏信号。
- **T12 TTS 通勤音频** ｜负责：前端/内容 AI ｜P2 ｜范围：选题雷达加"听一遍" ｜验收：移动端可播放。
- **T13 企业库性能** ｜负责：前端 AI ｜P1 ｜范围：分页/虚拟滚动 ｜验收：1505 卡移动端流畅。

---

## 6. 我（研究员）的几条独立判断

1. **最高杠杆是部署，不是加功能。** 你已经把 V20、收藏闭环、1505 企业做完了，卡在"没推上线"。这是最该先解决的——甚至比任何 Loop 都值钱。
2. **Loop 对你=定时小循环+人工护栏，不是常驻多智能体。** 参考文档这个结论我完全认同，不重复造轮子。
3. **警惕"幻觉自我强化"。** 任何权重/规则自动晋升必须你拍板；自动写 memory 只追加、不回写规则。这是 Loop 最容易翻车的点。
4. **一个你没点但我想提的**：你现有的 silver-auditor（事实核查 skill）目前是"写完后人工调用"。最大的内容风险是**一篇深写文章的事实错误**——可以把 auditor 接成"发布前必过的事实核查 Loop"（目标型 stop：核查不通过不许发）。这比优化评分更直接关系到你的公信力第一原则。
5. **别让"自我迭代"变成自我感动。** 参考文档点出的"伪闭环"（报告型、L3 空转）今天依然成立：循环 C 的死链/字段校验是真的能挡灾；但循环 A/B 的"建议权重"如果永远没人看，就是摆设。**所以派单里我把"人审"写成验收条件，不是客套。**

---

### 附：主题切换 bug 修复代码片段（给 T2）

```js
// 加载时：按存储或系统偏好，显式设一个类，去掉 media 反模式依赖
(function(){
  const r = document.documentElement;
  const saved = localStorage.getItem('theme');
  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
  r.classList.add(saved === 'light' || (saved === null && !prefersDark) ? 'light' : 'dark');
})();
function toggleTheme(){
  const r = document.documentElement;
  const toDark = !r.classList.contains('dark');
  r.classList.toggle('dark', toDark);
  r.classList.toggle('light', !toDark);
  localStorage.setItem('theme', toDark ? 'dark' : 'light');
}
```
配套 CSS：删除 `@media (prefers-color-scheme: dark){ :root:not(.light){…} }`，只保留 `:root{…浅色变量}` 与 `:root.dark{…深色变量}`。

---

*本文件为方案层，不含代码改动。两个联网研究智能体（Loop 原理核验 + 2025-26 新技术核验）正在补充权威来源链接，回来后并入本报告。*
