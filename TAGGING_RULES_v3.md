# 银脉企业库 · 标签生成管理规则（v3 · 唯一真相源 SSoT）

> **状态：v3（2026-07-15）生效中** — 基于 v2 + 评分推荐规则 + v12/v12.1/v12.2 三轮迭代经验全面重写。
> 前版：`TAGGING_RULES.md`（v2，2026-07-12）
> 用途：这份文件是标签的"**宪法**"。任何人（含自动化、另一个 AI）新增/修改/删除标签**都必须先读它**。
> **本文件与 `_rebuild_tags.py` 代码同步**：改了规则必须同步改代码并重跑全量。

---

## 〇、我犯过的错（错误模式 + 防再犯）

> 这节不是自我检讨表演，是**真正的避坑指南**。每条背后都有真实 bug。

| # | 错误模式 | 实际案例 | 防再犯规则 |
|---|---------|---------|-----------|
| 1 | **改了数据但没同步改校验规则** | v12 新建标签没注册同类词 → validate 报"无同类词的二级标签"FAIL | 新增/改名标签必须同时：(a)加 V12_NEWREG/L2_TO_L1 (b)加 SYNONYMS 同类词 (c)加 ALLOWED_LARGE（如果≥20） |
| 2 | **代码块顺序依赖导致旧名回写** | v12.1 的 3.5 改名块放在 BIGTAGS/v12 处理之前 → 后续处理用旧名字把新名字覆盖回去 | 所有**用户拍板的改名/新建/拆分**必须放在 **所有 v12 聚类/清理/剥离逻辑之后**、写盘之前 |
| 3 | **动态大标签剥离误伤具体标签** | 认知训练(19→30) 因≥20 被 STRIP_SET 当伞词溶解掉（从19家掉到1家） | 具体二级标签（非伞词）必须加入 `V12_STRIP_EXEMPT`；并在 `_v12_detect.py` `ALLOWED_LARGE` 冻结 |
| 4 | **fix 文件 JSON key 名写错** | fix_9 用了 `remove_tags`/`add_tags`，代码期望 `remove`/`add` → 65家企业 0 标签 | fix 文件格式严格：`{"remove":{企业名:[标签]}, "add":{企业名:[标签]}}`。跑完后必须 verify 0-tag 数 |
| 5 | **泛词删了但没给替代标签** | 健康监测(15)、医疗(6) 等直接删除，未逐家分配具体标签 → 企业变 0 标签 | **删任何标签前必须确认：该标签的所有成员企业是否已有其他标签兜底？** 没有 → 必须在 fix 文件里指定新标签 |
| 6 | **同类词构建块位置错误** | fin_syn 放在 3.5 改名之前 → 新标签不在 all_final 里 → 无同类词 | fin_syn 必须是**最后一个操作**（所有改名/新建/拆分之后、json.dump 之前） |
| 7 | **描述与标签严重不符未发现** | 口腔医疗(4)家企业描述全是"辅具/轮椅/助行器"，实际是康复器械不是牙科 | 走查 <5 标签时**必须读企业描述**，不能只看标签名 |

---

## 一、三层结构（先分清三个东西）

| 层级 | 是什么 | 展示 | 示例 |
|------|--------|------|------|
| **分类（category_l1/l2）** | 企业"做什么业务"的固定骨架 | **不前台展示**（保留在数据库） | category_l1=养老服务 / category_l2=养老院 |
| **一级标签（tag_l1）** | 分组容器，10个 | 前台筛选用 | 产业资本 / 养老服务 / 医疗健康 / … |
| **二级标签（tag_l2）** | 最小颗粒的具体词，可直接搜、有区分度 | **前台全量展示、可点筛选** | 助听器 / 跌倒监测 / VC / 认知症 |
| **同类词（synonyms）** | 只用于搜索命中，不展示、不单列 | 搜"老花镜"命中"眼镜" | 眼镜 ↔ [老花镜,老光眼镜,老花眼镜] |

**关键约束**：
- 一个企业可有**多个二级标签**（建议 ≤3 个），自动派生 tag_l1
- 二级标签 → 一级标签 = **多对一**（一个 L2 只归一个 L1，见 §10.3 优先级规则）
- 同类词 → 二级标签 = **多对一**

---

## 二、好标签的标准（达标才算过关）

1. **最小颗粒度**：二级标签是"能直接搜、有区分度"的最细能力词（认知症 / 临终关怀 / 助听器 / VC），**不是大伞词**（行业服务 / 消费品 / 文娱社交 / 平台 / 健康 这种笼统词不能当二级标签）。
2. **一级 ≠ 二级同名**：一级是容器、二级是具体词，两者**绝不能同名**。
3. **不与列表字段重复**：标签不能重复卡片上已有的结构化字段（融资状态 / 被收购 / 地区 / 成立年 等）。
4. **同义词已合并**：意思相同的词并成**一个**规范标签，别名全部塞进"同类词"层（只搜不显）。绝不让同义词各自单列成标签。
5. **每个二级标签 ≥ 3 家企业**：不足 3 家的（幽灵标签）必须并入近义标签或删除。（例外：经小爽明确保留的标签不受此限。）
6. **每个二级标签都有同类词**：至少覆盖常见搜索别名，保证搜得到。
7. **命名简洁**：尽量 ≤4 字；≥5 字的必须走查是否可拆/可缩短。（如"连锁养老运营"→"养老运营"，"影响力与并购投资"→"并购投资"）
8. **不带"健康"前缀**："健康"太宽泛，容易遗漏原有细分标签。正确做法：用更具体的词（慢病管理 / 体检筛查 / 健康管理），不用"健康XX"当独立二级标签。

---

## 三、坏标签的标准（出现即必须修）

- 一级 = 二级同名
- 企业数 > 100 的伞词（颗粒度太粗嫌疑，必须复查是否该拆/改名/是不是垃圾兜底桶）
- ≤ 2 家的幽灵标签
- 没有同类词的标签
- 与列表字段重复
- 语义错配（把"照护系统/ToB/就业"塞进"行业服务"这类归错组）
- **噪声词当标签**（废弃 / 综合养老 / 反共识 / 生态 / 平台 / 礼物）
- 国家/地区、品牌/产品名、单字碎片、医疗术语碎片（Medicare/EHR/IoT/AR/VR）
- **"系统"/"信息化"冗余**：如果企业已有 SaaS 标签，不再需要"XX系统"/"XX信息化"（冗余清理）
- **"X机器人"并列**：训练机器人/护理机器人/陪伴机器人等应拆为「业务领域 + 机器人」交叉属性（机器人作为横向标签）

---

## 四、标签怎么来（生成规则）

1. **来源（零丢失，三套不双计）**：一级分类 + 二级分类 + Excel 选题库（细分领域_1 / 细分领域_2 白名单）+ 系统自带标签。凡原始词找不到同义归并的，**保持原值当候选标签，绝不静默丢弃**。
2. **归并**：按"同义词合并"把别名并到规范标签，别名进同类词层。
3. **过滤**：套用第三节"坏标签"清单，删噪声/碎片/幽灵。
4. **产业资本（方案B）**：只保留**自身是投资机构/产业资本方**（VC/PE/CVC/REIT/险企/健康计划/上市产业集团/巨头），与"拿没拿过融资"无关。
5. **落地**：全量重打 → 跑检验清单 → 写回 `all_enterprises.json`。
6. **变更管控**：新增/改标签必须同步本文件 + `_rebuild_tags.py`，并经小爽确认，禁止静默引入。

### 4.1 投资类标签层级（v12.2 明确）

| 二级标签 | 定义 | 示例 |
|---------|------|------|
| **VC** | 风险投资/创业投资（早期阶段） | Battery Ventures, a16z, 千骥资本 |
| **产业基金** | 产业基金/PE/成长期基金（非纯VC） | BAI资本, 达晨财智, Primetime Partners |
| **并购投资** | 并购/PE/影响力投资（后期/控股型） | Apax Partners, Rethink Impact |
| **养老REIT** | 养老地产信托（持物业收租型） | Welltower, Ventas, Ventas |
| **保险** | 保险/险企（含养老社区运营） | 复星保德信, 太平养老, 泰康之家 |

> 规则：去掉"银发""老年服务""养老科技""影响力""地产"等前缀/修饰词，直取核心业务形态。

---

## 五、横向交叉属性标签（v12.2 新增机制）

某些标签不代表"业务领域"，而是**技术形态/交付模式**，应作为横向维度追加到相关企业：

| 交叉标签 | 追加条件（来源标签） | 说明 |
|---------|-------------------|------|
| **SaaS** | 养老运营系统/养老信息化/医疗信息化/数字慢病管理/任何软件平台型企业 | 软件即服务交付模式 |
| **AI** | AI医疗/AI行为跌倒监测/人形机器人/任何AI驱动型企业 | 人工智能技术属性 |
| **跌倒监测** | 可穿戴跌倒监测/非穿戴跌倒监测/AI行为跌倒监测 | 合并为统一属性标签 |
| **机器人** | 训练机器人/护理机器人/陪伴机器人/服务机器人 | 拆分为「业务域 + 机器人」组合 |

> 实现：`V12_CROSSTAG` + `V12_2_ROBOT_SPLIT`（见 _rebuild_tags.py 3.5d + 3.6d）

---

## 六、检验清单（每次重跑后必须全绿）

| # | 检查项 | 目标值 | 命令 |
|---|--------|--------|------|
| 1 | 一级 = 二级同名 | 0 | `validate_tags.py` |
| 2 | 幽灵标签（<3 家） | 0 | `validate_tags.py` |
| 3 | 与列表字段重复的标签 | 0 | `validate_tags.py` |
| 4 | 无同类词的二级标签 | 0（核心标签必须有） | `validate_tags.py` |
| 5 | 0 标签企业 | 0 | `validate_tags.py` |
| 6 | 单企业标签数 ≤ 5 | 0 超标 | `validate_tags.py` |
| 7 | 超大伞词（>100 家） | 逐个复查 | `validate_tags.py` |
| 8 | 企业内标签无重复 | 0 | `validate_tags.py` |
| 9 | 有标签企业必有 L1 | 0 缺失 | `validate_tags.py` |
| 10 | 全部标签在已知 canon 集合 | 0 孤儿 | `validate_tags.py` |
| 11 | ≥20 未冻结标签 | 0（需拆分） | `_v12_detect.py` |
| 12 | 弱简介企业 | 尽可能少 | `_v12_detect.py` |
| 13 | 伞词残留 | 尽可能少 | `_v12_detect.py` |

---

## 七、代码执行顺序铁律（_rebuild_tags.py 内部顺序）

```
1. 数据加载 + 基础清洗（MERGE/L2_RENAME/SYNONYMS/V8/V10/V11...）
2. v12 聚类合并（V12_REASSIGN 重指派）
3. v12 校验修正（fix_1~fix_9 文件应用）← 在此之前
4. 大标签/伞词剥离（首轮 STRIP_SET）
5. 迭代二次剥离（至稳定）
6. ┌─ ★ v12.1 改名/拆分/交叉（3.5a~3.5e）    ← 用户拍板改动放这里
7. ├─ ★ v12.2 全面优化（3.6a~3.6f）          ← 第二轮用户改动放这里
8. ├─ ★ v12.2 后二次伞词剥离（3.6g）         ← 清理 3.6 引入的残留
9. └─ ★ 构建权威同类词表 fin_syn（写盘前最后重算） ← 必须在 3.5/3.6 之后！
10. 写盘: all_enterprises.json + tag_synonyms.json + _l2_l1.json
```

**违反此顺序 = 必出 bug**（参考错误模式 #2, #6）

---

## 八、L2→L1 优先级规则（行业 > 领域 > 业务 > 产品）

当一个企业的某个二级标签可归属多个一级时，按以下优先级定其主 L1：

1. **行业（Industry）**：企业自身主营业务所属赛道（如助听器厂商主归"消费品"而非"医疗健康"）
2. **领域（Domain）**：企业聚焦的银发细分领域（如"认知症"企业优先归"医疗健康"）
3. **业务（Business）**：企业实际开展的业务线（如养老机构运营归"养老服务"）
4. **产品（Product）**：仅作为产品提供时（如外骨骼产品归"康复辅具"而非"智能科技"）

> **禁止**：不得因"该企业也卖 X 产品"就把 X 所在 L1 当成主 L1——产品优先级最低。

---

## 九、协作约定（小爽定，长期生效）

1. **UI/UX 小修直接改**：搜索框跳动、placeholder 文案、按钮样式等简单问题，AI 直接动手
2. **标签大改动先对齐**：涉及数据结构的变化，先给方案文档，小爽确认后再执行
3. **每次改完标签必须发完整结果**：不能只说"校验通过"。交付格式见第十节
4. **未确定的需求不动**：默认不动，等明确指示
5. **命名铁律**：禁用"最终版/完整版"；改用日期或 V 号迭代；旧版本保留不删
6. **【动你标注】**：凡改动用户手动整理的内容（如选题库分类），必须**显式标注理由**让用户拍板
7. **主动全量走查**：不要等用户点名才改——自己把同类潜在问题全量排查一遍再出方案

---

## 十、标签交付格式（小爽核对用，禁止省略）

每次标签改动后，交付的"完整大表"必须包含：

1. **一级标签列表 + 各自企业数**
2. **每个一级下的二级标签 + 各自企业数**
3. **每个二级标签的全部同类词（一个不省略）**
4. **改动前后对照**（争议标签必须逐条列企业名）
5. **校验结论**（9/9 或哪项 FAIL + 原因）
6. **detect 结论**（收敛/未收敛 + 原因）

> 交付物模板：`output/标签体系_全映射_{DATE}_v{N}.md`

---

## 十一、迭代经验日志（只追加，不覆盖）

### 2026-07-12（v2 基础版）
- 映射表只覆盖 ~46 词 → 大量二级分类静默丢弃（SODH/相亲/老年鞋）。教训：规则必须落 SSoT + 每次必跑检验清单
- **必查四条**：最小颗粒度 / 一级≠二不同名 / 不与字段重复 / 同义词合并到同类词层

### 2026-07-15（v12.1 → v12.2 两轮）
- **3.5/3.6 块必须放在所有聚类/清理之后**，否则旧名字被后续处理回写
- **认知训练 regression**：具体标签因成员数≥20 被动态大标签剥离误伤 → 加 V12_STRIP_EXEMPT
- **fin_syn 必须是最后一步**，否则新建标签无同类词
- **fix 文件 JSON key 名**：用 `remove`/`add` 不是 `remove_tags`/`add_tags`
- **删标签前必须确保成员有替代标签**，否则产生 0-tag 企业
- **口腔医疗 4 家全错标**：描述=辅具但标签=牙科 → 走查 <5 标签时必须读描述
- **"X机器人"应拆为交叉属性**：机器人成为横向标签（类似 SaaS/AI），显著降低单标签率
- **投资类标签去前缀**：银发/老年服务/养老科技/影响力/地产 等修饰词全部去除
- **泛词清单持续更新**：健康监测/医疗/智慧养老/护理平台/系统/信息化 等确认删除

---

## 十二、当前快照（v12.2 截止 2026-07-15）

| 指标 | 值 |
|------|-----|
| 总企业数 | 1321 |
| 二级标签总数 | 121（从 v12.1 的 154 精简至此） |
| 0 标签企业 | 0 ✓ |
| 单标签企业 | 1092（82.7%）|
| 多标签企业 | 229（17.3%）|
| validate | **9/9 全绿** ✓ |
| detect | **0 需拆分** ✓（16 伞词残留为清理度问题） |
| 一级标签（10 个）| 产业资本 / 养老服务 / 医疗健康 / 康复辅具 / 文娱社交 / 智能科技 / 消费品 / 渠道零售 / 金融保险 / 食品营养 |
| 最大二级标签 | SaaS(56) / 居家护理(54) / 机器人(41) / 适老化(35) / 康复器械(32) |
| 本轮新增二级标签 | VC / 产业基金 / 养老REIT / 并购投资 / 养老运营 / 认知症 / 机器人(交叉) / 慢病管理(合并) |
| 本轮删除标签数 | ~33 个（泛词/冗余/伞词/错标） |
