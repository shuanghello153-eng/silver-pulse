# 银发资讯库 · 规则方案（V1 · 对标企业库标准 · 2026-07-21）

> **性质**：这是研究员（我）交给小爽的**整体规则方案**，不是定稿，也不是数据改动。待小爽逐条拍板后，再据此执行 63 条资讯的逐条补全（自己联网写，不开狂工人，配门禁，出预览，部署等你拍板）。
> **对标标尺**：企业库 `all_enterprises.json`（1502 家 / 38 字段，已由另一 AI 做到质量过硬）+ `enterprise_score.py` 研究价值体系 + `企业字段字典_V1.md`。资讯库要达到同等“字段不水、理由不套、关联不裂”的标准。
> **铁律**：本方案**绝不改动企业库任何内容**，只学习其规则结构与质量标尺。

---

## 〇、为什么要有这套规则（定位与目标）

资讯库 = 小爽选题情报台的**前端资讯流**（`data/scored_latest.json`，当前 63 条，日期 7/1–7/8；原始 RSS 宇宙约 1031 条 `raw_*.json`）。它的唯一服务对象是小爽的**选题决策**——从海量信息里，筛出“值得写、能写成、对中国有借鉴”的银发资讯，并给出可执行的选题落点。

当前诊断（用证据，非感觉）：63 条 / 32 字段**机器填充率近 100%**，但内容注水三大坑，和企业库当年 `recommend` 的“假填充”一模一样：
1. `summary_cn` 是标题切片（如 id158：`银发快讯 | 同仁堂医养在港交所上市；…&nbsp;&nbsp;AgeClub`），不是真摘要；
2. `recommendation` 大量模板废话（如 id159：`产业资本：新一轮融资落地，资本正加码产业资本赛道；。中国银发企业可参照其融资节奏与估值逻辑…`）；
3. `entity_name` 12 条空（关联键断裂，资讯↔企业库交叉价值断掉）；外加 `event_type` 错分、`tags` 前端没渲染。

**本方案要解决的，就是让资讯库达到企业库那套“先有规则、再有填充、门禁兜底”的标准。** 原则：**借鉴不照搬**（资讯≠投融资）；**字段不重复**；**每个判断都有理有据**（为什么这个字段重要、怎么帮选题）；**零成本优先**（脚本能定的不调模型）。

---

## 一、企业库规则速览（对标标尺，供对照）

| 维度 | 企业库做法（标尺） | 资讯库如何借鉴 |
|---|---|---|
| 综合评分 | `research_value` 0–100 = (信号×30% + 信息量×30% + 差异化×20% + 可复制×20%)×10；分级 S/A/B/C | 同构：两层评分，综合分沿用四维加权结构，但把“信息量/差异化”换成资讯特有的“可写性/时效性” |
| 深度写作判定 | `worth_deep_write`：rv≥45 或命中≥2 标准（大企业/近期事件/差异化/可借鉴） | 对应 `final_score≥5.0` 入选 + `event_type` 动态事件优先 |
| 推荐理由 | 强模写一段自然流畅理由，融合四维判断，**不重复卡片结构化信息**（金额/轮次/日期/来源） | 同样强模逐条写，结构=洞察+选题角度+国内借鉴，门禁杀模板 |
| 银发判定 | `silver_verdict` 核心/相关/背景 + `silver_reason` | 资讯版 `silver_relevance` + `silver_reason`，门禁更严（资讯噪声多） |
| 标签 | `tag_l1`（8 一级）/ `tag_l2`（109 二级），L2 主要展示，L2→L1 唯一映射；从 v3 迭代到 v20 共 17 版 | 资讯标签走“横切维度”路线，不与分类重复；先骨架+门禁+随跑批积累，类比 L2 迭代 |
| 字段字典 | `企业字段字典_V1.md`：38 字段，✍️/🟡/🔴/⚰️ 图例，处置清晰 | 本方案 §6 给出资讯库字段字典与去重清单 |

---

## 二、资讯分类体系（分几类、收集重点、为什么）

### 2.1 三轴正交分类（根治“字段重复/标签=分类”）

资讯库只用**三个互不重叠的轴**描述一条资讯，任何两轴都不重复承载信息：

- **轴 A · `event_type`（纵向 · 发生了什么）**：融资 / 收购并购 / IPO上市 / 产品发布 / 政策法规 / 行业趋势 / 人事变动 / 其他事件。这是“事件类型”，用于排序锚点与推荐理由主维度。
- **轴 B · `domains`（横向 · 哪个子行业）**：**直接复用企业库 109 个 L2 二级标签**作单一真相源（养老服务/康复辅具/健康服务/认知症/数字疗法…），不重复造轮子、不另起一套。
- **轴 C · `tags`（横切 · 透镜，不是分类）**：资本 / 技术 / 市场 / 模式 / 政策·支付方 / 反常识 六个维度（详见 §5）。它回答“这条资讯从哪个透镜看最有价值”，**绝不**等于 event_type 或 domains。

> **为什么不堆叠更多分类**：三轴已正交覆盖“什么事件 / 哪个行业 / 什么透镜”。再加第四套分类 = 字段重复，正是企业库当年 `category_l1/l2` 被废弃的教训（前端已不读，改读 tag）。

### 2.2 收集重点与优先级（WHY 对齐选题目标）

| 优先级 | 事件类型 | 为什么是重点（对齐小爽选题） |
|---|---|---|
| **P0** | 资本事件（融资 / IPO / 收购） | 资本用真金白银投票 = 最强“赛道验证”信号；直接可用“银发投融资图谱”选题；且天然与企业库联动（报道的企业↔企业库条目） |
| **P0** | 政策法规 / 支付方 | 银发经济的“天”，长护险/支付端改革直接决定商业模式可行性；小爽“政策解读”类选题刚需 |
| **P1** | 产品 / 服务发布 | 直接揭示用户需求演进、竞品打法，是“拆解产品”选题的一手素材 |
| **P1** | 行业趋势 / 报告 | 单篇价值低，但 cluster 聚合后是“选题地图 / 宏观背景” |
| **P2** | 人事变动 | 个别高管变动对选题价值有限，除非标志性人物（创始人离职影响战略）才升格 |
| 兜底 | 其他事件 | 一律进二筛，无银发信号即删（见 §7 银发信号铁律） |

**跨优先级铁律**：① 海外优先（信源治理铁律：主盯 AGECLUB、海外一手源）；② 国内可借鉴性 `cn_fit` 给加成（同样信号，能平移国内的排前）。

---

## 三、评分计算（每一个评分怎么算）

### 3.1 两层评分架构

```
层 A  信号强度 signal_strength (0–10, 脚本确定性, 零成本)  ← 事件分量“闸门”
层 B  选题价值综合分 final_score (0–10, 四维加权)          ← 列表排序主依据
```

### 3.2 层 A · `signal_strength`（已有，沿用 `selection/signal_strength.py`）

| 分量 | 取值 | 说明 |
|---|---|---|
| 信源起点 `source_tier` | T1=0.5 / T2=0.3 / T3=0.1 | 用户拍板：**相关性已由前置闸门过滤，信源等级仅微弱起点**，事件本身分量远比出处重要 |
| IPO | +6 | 走向公开市场 |
| 融资 | 按金额分级（海外 $ / 国内 ¥ 阈值） | 大额融资信号强 |
| 收购 | 按金额分级（MA_TIERS） | 整合信号 |
| 时效 `recency` | 近1周2.5 / 近1月2.0 / 近2月1.5 / 近半年1.0 / 近1年0.5 / 更久0.25 | 新闻衰减 |

返回 `(final, breakdown)`，前端可展示“为什么这个分数”。**作用**：作为预筛选闸门——低信号（如 < 2.0）直接归 archive/背景，不占用选题注意力。

### 3.3 层 B · `final_score` 四维加权（借鉴企业库，适配资讯）

公式（与企业库 `research_value` 同构）：

```
final_score = 0.30·signal + 0.30·cn_fit + 0.20·writing + 0.20·urgency
```
- `signal` = `signal_strength` 归一（事件分量）
- `cn_fit` = 中国可借鉴性 / 选题契合度（**对应企业库 `copy_score` 可复制**）
- `writing` = 信息完整度 / 可写性（**资讯特有**：该事件是否有足够素材写成一篇）
- `urgency` = 时效性 / 紧迫度（**资讯特有**：新闻随时间衰减）

> **类比企业库**：企业库 `(信号×30% + 信息量×30% + 差异化×20% + 可复制×20%)×10`；资讯把“信息量/差异化”替换为“可写性/时效性”，结构完全同构——这就是“借鉴不照搬”。

### 3.4 分类阈值（沿用 `scorer.py`）

- `high`（重点选题）≥ 7.0
- `watch`（值得跟踪）≥ 5.0
- `archive`（背景归档）< 5.0

### 3.5 辅助维度（全量留存供解释，不单独排序）

- `novelty` 反常识度（0–10）：驱动“反常识”标签 + `writing` 加成。
- `centrality` 中心度：事件簇内中心性，驱动“同主题 N 篇”聚合展示。
- `dim_scores`：`{signal, writing, cn_fit, urgency, industry, novelty}` 全量留存，前端可展开看每一项为什么。

### 3.6 零成本优先

`signal_strength` 与 `dim_scores` 里的 signal / urgency / industry 用**脚本算**；`writing` / `cn_fit` / `novelty` 用**强模**（或 `score_skill.py` 规则兜底，无 key 也能跑）。强模选型：HY3 免费期至 2026-07-22，之后改用 DeepSeek V3.2（更便宜，63 条逐条写成本可控）。

---

## 四、推荐理由写法（怎么写、杀模板、字段不重复）

### 4.1 当前根因

`recommend.py` 的 `CHINA_REF_VARIANTS` 是“国内可参照其融资节奏”式套话模板，靠事件类型拼变体。证据：id159 `产业资本：新一轮融资落地，资本正加码产业资本赛道；。中国银发企业可参照其融资节奏与估值逻辑，关注同赛道国内玩家。` —— 只换赛道词，无具体角度、无具体对象。**根因 = 模板拼接，非强模**。

### 4.2 新标准：强模逐条写（类比企业库 `recommend` 用强模）

每条推荐理由 = **2–3 句自然流畅中文**，三段结构：

1. **句1 洞察**：这条资讯的信号点（用事件事实，不重复卡片已显式的金额/轮次/日期/来源/评分）。
2. **句2 选题角度**（展开 `topic_angle`）：具体能写什么角度（给小爽的选题落点）。
3. **句3 国内借鉴**（展开 `cn_takeaway`）：国内创业者/读者能学的具体什么（具体到赛道/模式/公司，非“国内可参照”）。

**示例（改造后 id158 同仁堂 IPO）**：
> 同仁堂医养登陆港交所，是中药老字号拆分康养业务独立资本化的标志性案例。可写成“百年药企如何讲银发新故事”，拆解其医养闭环与估值逻辑。国内同仁堂系及广誉远、片仔癀等都在观望同一条资本化路径。

### 4.3 硬性规则（直接对应企业库“recommend 不重复卡片结构化信息”同律）

- ✅ 不重复卡片已有字段（金额/轮次/日期/来源/评分 chip）。
- ✅ 禁“X赛道，国内可参照”式空话；必须落到**具体角度 + 具体对象**。
- ✅ 每条 unique，禁同模板跨条复用（门禁检测模板短语黑名单）。
- ✅ 报道企业的资讯：理由**随事件变**（同企业不同事件理由必不同），绝不复制企业库静态 `recommend`。

### 4.4 门禁脚本（避免重蹈企业库 `recommend` 先满后水的覆辙）

逐条写后跑校验：① 非空；② 不含模板黑名单短语（如 `国内可参照`/`可对标`/`值得关注` 后无具体实体）；③ 长度 ≥ 40 字；④ 未重复 `amount`/`date`/`source`。失败打回重写。

---

## 五、标签体系（交叉维度、多少类、类比企业库 L2 迭代）

### 5.1 原则

`tags` **只承载“横切维度”**，绝不与分类（`event_type` / `domains`）重复 → 直接解决“标签=分类”与“字段重复”。沿用 `tagger.py` 的 `detect_cross_tags`（强制 2–5 个）。

### 5.2 六大标签类（维度，非穷举词表）

| 维度 | 含义 | 典型触发 |
|---|---|---|
| 资本动作 | 钱往哪走 | 大额融资 / IPO / 并购 |
| 技术突破 | 新技术/新产品 | AI陪伴 / 康复机器人 / 数字疗法 |
| 市场信号 | 需求/用户侧变化 | 用户增长 / 下沉市场 / 消费觉醒 |
| 模式创新 | 商业模式/打法 | 订阅制 / 社区嵌入 / 保险+服务 |
| 政策·支付方 | 规则/钱从哪来 | 长护险 / Medicare / 监管试点 |
| 反常识 | 颠覆共识 | `novelty` ≥ 阈值自动打 |

### 5.3 类比企业库 L2 迭代

企业库 L2 从 v3 迭代到 v20 共 17 版，词表随数据生长。资讯标签同理：**先定六维度骨架 + 门禁 + 随跑批积累具体词**，不一次性定死词表（避免过早固化导致漏标）。维度数量锁定为 6，单条 2–5 个，强制截断（全打 = 无打）。

---

## 六、字段补全与去重（字段不能重复出现）

### 6.1 现有 32 字段去重清单

| 字段 | 处置 | 理由 |
|---|---|---|
| `domain`（单数） | 🔴 **废弃**，并入 `domains` | 与 `domains`（列表）重复，历史遗留 |
| `signal`（旧） | 🔴 **废弃** | 与 `signal_strength` / `signal_score` 重复 |
| `signal_score` | 🟡 作为 `signal_strength` 别名保留（向后兼容） | 同义，文档标注即可 |
| `signal_strength` | ✅ **权威信号分量字段** | 0–10 脚本分，前端展示 |
| `is_curated` / `is_selected` | 🟡 澄清：`is_selected`=自动过闸（fs≥5.0）；`is_curated`=人工/AI 标深写。**若二者等价则并掉**（待小爽拍板） | 避免语义重叠 |
| `view` / `viral` | 🟡 `view`=用户阅读数保留；`viral`=可算标，暂不强制 | 参与度信号，非核心 |

### 6.2 保留不动（算法产出，不碰）

`id` `title` `title_cn` `url` `source` `date` `region` `tier` `category` `event_type` `domains` `tags` `cluster_id` `cluster_size` `is_main` `final_score` `dim_scores` `novelty` `centrality` `summary` `signal_breakdown` `is_selected` `is_curated` `view` `viral`

### 6.3 重写（本方案核心，杀注水）

| 字段 | 处置 |
|---|---|
| `summary_cn` | ✍️ **重写真摘要**（这事是什么 + 对银发意味着什么），杀标题切片 |
| `recommendation` | ✍️ **重写**（§4 标准），杀套话 |

### 6.4 新增（资讯库相对企业库的差异化重点）

| 字段 | 含义 | 作用 |
|---|---|---|
| `entity_serial`（新增） | 主体若在 `all_enterprises.json` 存在，填 `#xxxx` | **打通两库关键跳板**：卡片一键跳企业库 |
| `silver_relevance`（新增） | 核心 / 相关 / 背景 | 资讯版银发关联强度（对应企业库 `silver_verdict`） |
| `silver_reason`（新增） | 为什么是这个强度 | 一句话依据（对应 `silver_reason`） |
| `topic_angle`（新增） | 具体可写角度 | 把“创业者价值落点”结构化 |
| `cn_takeaway`（新增） | 国内可借鉴点 | 对应企业库 `recommend` 的“可复制/国内可比” |

> **补全来源**：`entity_name` 用 `enrich_entity.py` 回填（企业库精确匹配 + 兜底规则，宁空不瞎填）；`entity_serial` 由 `entity_name` 反查企业库 `serial`；`silver_relevance` 按 §7 银发信号铁律判定。

---

## 七、筛选机制（关键词 → 企业库类似 → 碳模型预筛选）

三阶段漏斗，**严出**优先，绝不让噪声进选题台：

1. **阶段 1 · 关键词初筛**（`collector.is_relevant`，宽进）：关键词命中即收入，宁可错收。
2. **阶段 2 · 企业库类似二筛**（`relevance_screener.py`，严出）：
   - 强词命中 **或** 已知银发企业（`enterprise_names.ENT_NAME_SET`）命中 → 保留；
   - 否则判弱相关 → 拦，写入 `blocked_log.json`（**绝不删除原始数据，随时可回捞**）。
   - **银发信号铁律**（最高优先）：无银发信号（无关键词且无已知企业且无实质银发业务）的资讯**直接删除**，绝不进“背景”兜底桶。
3. **阶段 3 · 碳模型/低成本预筛选**（`signal_strength` 作前置闸门）：低信号（< 2.0）归 archive/背景，不占用选题注意力。**碳模型 = 零成本脚本闸门**，在昂贵的最终评分之前先滤掉无分量事件。

---

## 八、非企业资讯、多资讯展示、关联企业规则

### 8.1 报道企业的资讯，能否完全复用企业库规则？

- **评分框架：复用**。`signal_strength` + `final_score` 同构，维度同源（都含 `cn_fit`/可复制思想）。
- **推荐理由：不能一模一样**。企业库 `recommend` = **静态企业画像**（规模/模式/可复制）；资讯 `recommendation` = **动态事件价值**（信号/角度/时效）。同企业不同事件，理由必不同。维度同源、内容不同。

### 8.2 非企业资讯（政策 / 趋势 / 无明确主体）

- 仍可评分、可推荐；`entity_name` / `entity_serial` 留空；`silver_relevance` 判为 相关 / 背景。
- 推荐理由聚焦“政策影响 / 趋势启示”，不硬塞企业主体。

### 8.3 多资讯展示决策（同一 `cluster_id` 多条）

- `is_main=True` 那条为**主展示**，其余折叠为“同主题 N 篇”徽章。
- **展示哪条**：选 cluster 内 `final_score` 最高者（最值得写）；并列时取**最新**（`date` DESC）。其余作为关联阅读。
- 这直接回答“多资讯如何决定展示哪条”。

### 8.4 关联企业规则（资讯 ↔ 企业库双向）

- **资讯→企业**：`entity_name` 精确匹配企业库（`enrich_entity.py`）；再由 `entity_name` 反查 `entity_serial` 打通跳转。
- **企业→资讯**：`rebuild_association.py` 反向匹配——强匹配（企业名出现在新闻标题/正文，或新闻 `entity_name`==企业名）+ 主题匹配（企业 tags/业务关键词命中新闻**标题**，宁缺毋滥）。写入 `enterprise_scores.json[serial].related_news_ids`，**多对多**，只动 scores 不碰企业库真相源。

---

## 九、渠道信息存储规则（供另一 AI / 研究员更新）

另一 AI 负责从各渠道筛选好信息来源（找渠道）。定一套规则把渠道信息存某处，供其或我更新：

- **真相源**：新建 `data/sources.json`，字段：
  | 字段 | 含义 |
  |---|---|
  | `source_name` | 渠道名（与 `config.SOURCE_NAME_TO_TIER` 键一致） |
  | `url` | 入口 |
  | `region` | 国内 / 海外 |
  | `tier` | T1 / T2 / T3（信源治理铁律：评级从简） |
  | `category` | 媒介类型：协会 / 媒体 / 企业官网 / 研究院 / 监管 |
  | `health` | 活跃 / 休眠 / 失效（死源观察标注，**只增不删**） |
  | `added_by` | 谁加的：另一AI / 研究员 |
  | `added_at` | ISO 日期 |
  | `notes` | 备注 |
- **同步**：`tier` 映射同步回 `config.SOURCE_NAME_TO_TIER`（评分起点用）。
- **铁律**：渠道只增不删（死源标观察）；研究员/另一 AI 改 config 只 `commit main` **不 deploy**（部署权不在研究员）。

---

## 十、列表页排序依据（为什么这些字段重要、怎么帮选题）

| 排序层级 | 字段 | 为什么（对齐选题） |
|---|---|---|
| 1（主） | `final_score` DESC | 选题价值综合分，最高=最值得写，直接对齐“选题”目标 |
| 2 | `signal_strength` DESC | 同价值下，大事件优先（资本/政策分量） |
| 3 | `date` DESC | 新闻时效性，旧资讯即使高分也可能是背景 |
| 簇内 | `is_main` 排前 | 主展示优先，其余作“同主题 N 篇” |
| 银发关联 | 核心 > 相关 > 背景 | 一眼过滤噪声 |

**默认视图**：`high` + `watch`（final_score ≥ 5.0）可见；`archive` 折叠。
**筛选 UX**：按 `event_type` / `domains` / `tags` / `region` / `silver_relevance` 过滤；搜索框内联标签栏（沿用 generator.py）。

---

## 十一、待小爽拍板（我倾向的默认，否决即可）

1. **范围**：聚焦现有 63 条做 100 分（推荐）vs 把 1031 原始更多纳入？→ 我倾向 **63 条**（网站实际展示集，做出 100 分最有感）。
2. **`entity_serial` 跳转**？→ 我**强烈推荐**（情报台“资讯↔企业”交叉浏览核心价值）。
3. **`silver_relevance` 判定**？→ 我**推荐要**（资讯噪声多，过滤刚需）。
4. **`is_curated` / `is_selected` 是否合并**？→ 我倾向**保留 `is_selected` 为自动过闸，`is_curated` 留给人工深写标记**；若你认定二者等价就并掉。
5. **企业库那 2 处 `funding.amount` 错填**（#0001 AgeClub=`银创资本投资`、#0016 小年糕=`D轮融资（一线投资机构）`）→ 我不改企业库，可整理成审查表交你 / 另一 AI。
6. **强模选型**：HY3 免费期后改用 DeepSeek V3.2 写 prose 字段（summary_cn / recommendation / topic_angle / cn_takeaway），是否同意？

---

## 🔴 重点提醒（写资讯库时必守）

1. `summary_cn` 绝不等于标题切片——必须是从正文/外链读来的“这条新闻讲了什么”。
2. `recommendation` 禁“X赛道，国内可参照”式套话；每条必点**具体角度 + 具体对象**。
3. `entity_serial` 只填“确在 all_enterprises.json 中”的主体，宁空不瞎填。
4. `silver_relevance` 按实质判断，default 从严（无信号即删）。
5. **绝不改动企业库任何字段**——只学习、只读、只反查 serial。
