# 资讯库运营方案 V1（渠道 / 分工 / 自动化）

> 本文档只规划「长期怎么运转」，不改动任何代码与数据。资讯库规则本身见《资讯库规则手册_V3.md》。
> 状态：2026-07-22。方案待小爽拍板后执行。

## 0. 当前进度
- 63 条存量资讯已完成字段补齐：`summary_cn` / `recommendation`（四段式：洞察·选题角度·国内借鉴·风险）／ `topic_angle` / `cn_takeaway` / `silver_relevance` / `silver_reason` / `entity_serial` 全部落地。
- 前端 `generator.py` 已改企业库同款并本地重生 `output/index.html`（未部署）。
- 进行中：追更近期资讯（跑 `collector.py` 拉最近 7 天）→ 补齐新条目 → 一起部署。

## 1. 分工建议（我的看法，供拍板）
**结论：我完全同意「这个 AI 负责资讯更新，另一个 AI 负责找渠道」。**

理由：
1. 资讯更新是「规则 owner 的延伸」——评分 / 标签 / 前端 / 门禁都是我定的，由我跑闭环最不会走样，也不用反复跟另一个 AI 交代规则。
2. 另一个 AI 找渠道时本就要评估信源质量、迭代规则，它的产出（新渠道 + 初步筛选的资讯）直接喂给我，我做一次**轻校验（抽查硬伤）**即可入库，不重筛——不重复劳动、不双倍烧额度。
3. 企业库入库 AI 直接看我前端列表页挑企业即可，**不需要任何交接文件**（之前设想的 `pending_enterprises.json` 已取消）。换哪个 AI 接手企业库都不受影响。
4. 两个账号积分隔离：我只在「资讯更新」这条线烧额度，另一个 AI 在「找渠道/录新资讯」线烧，互不挤占。

边界：
- **我（资讯 AI）**：现有渠道采集 + 评分打分 + 字段补齐 + 前端 + 部署 + 每月整理「资讯里出现、但企业库还没有的公司」清单（供你转交企业库 AI，或企业库 AI 自己看前端）。
- **另一个 AI（渠道 AI）**：发现新渠道、评估渠道质量、录入初步筛选过的新资讯（走 `collector.py` 的 manual 注入或 `data/manual_news.json`）。
- **企业库 AI**：看前端列表页，自主挑企业入库。

## 2. 新渠道处理（先不执行，等你给清单）
新增渠道**只改一个地方**：`config.py` 的 `SOURCES` 字典（约 config.py:334）。格式：
```python
"渠道名": {
  "name": "渠道中文名",
  "l2_domain": "对应领域(如 养老/康复辅具)",
  "l2_channels": [("子源名", "url", "rss/google_news/manual")],
  "tier": 1/2/3,
  "region": "overseas/domestic"
}
```
`SOURCE_NAME_TO_TIER` 会自动从 `SOURCES` 派生，不用另改。

**建议流程**：你把新渠道清单（名称 + URL + 地区 + 可信度）给我 → 我加进 `SOURCES` → 跑 `python collector.py` 即生效。你也可让渠道 AI 直接维护一个 `data/sources.json` 真相源（便于它改而不碰 `config.py`），我再同步进 `config.py`。

⚠️ 你提到「最近新增了一些渠道」——**我目前没有这份清单**。请把渠道名 / URL 发我，或让渠道 AI 写入 `data/sources.json`，我再合并。在此之前我不擅自加渠道。

## 3. 近期资讯追更（执行中）
链路（已探查确认）：
```
python collector.py        # 拉最近 MAX_AGE=7 天，去重写 data/raw_今日.json
python score_and_merge.py  # 并入 data/scored_latest.json（只重算 signal/tags/event，不碰 prose）
python generator.py        # 重建 output/index.html
python deploy_ghpages.py   # 上线
```
- 新进来的文章 prose 字段为空 → 派子智能体按《规则手册_V3》补齐（同本次 63 条做法）。
- 超过 7 天的老漏采：用 `add_new_articles.py` 或 `data/manual_news.json` 注入。
- 银发信号铁律（无银发信号直接删）照常由 `relevance_screener` 把关。

## 4. 自动化任务（先不执行；且受账号绑定限制）
**关键事实：自动化与本账号绑定，另一个 AI 的账号看不到，也不能跨账号共享。**

本账号可建（等你拍板再建）：
- **每日采集 + 评分 + 生成**（不自动 deploy，或 deploy 需你授权）：定时跑 collector + score_and_merge + generator，新文章进库但先不部署，你 / 我复核后手动 deploy。
- 频率：每日一次足够（新闻时效以天计）。

不建 / 慎建：
- 自动 deploy：部署权在你，且代理 push 偶发失败，建议保留人工确认。
- 跨账号自动化：不可行，不建。

## 5. 待你确认
1. 新渠道清单（名称 + URL + 地区 + 可信度）——发我后我加进 `SOURCES`。
2. 是否建「每日采集 + 评分 + 生成」自动化（不自动 deploy）？
3. 部署权限：本次追更 + 补齐后，是否授权我跑 `deploy_ghpages.py` 一次性上线？
