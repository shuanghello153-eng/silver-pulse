# Silver Pulse 信源扩充与治理 · 整体规划（对齐稿 v1）

> 日期：2026-07-15 ｜ 角色：信源猎手 + 信源治理员 ｜ 状态：待小爽拍板后启动

---

## 0. 我对核心目标的理解（先对齐一句话）

你的核心目的不是"改网站长什么样"，而是：**让 Silver Pulse 的信源池持续变大、变好、变稳**——不断发现更优质的国内外银发经济信源，同时定期淘汰劣质/失效源，并用一套"评级 + 健康"机制让这件事能自己转起来。我接下来就扮演"信源猎手 + 信源治理员"这个角色，不碰评分/展示逻辑（那是另一摊）。

---

## 1. 现状盘点（今天刚核过代码，硬数据）

- 信源真相在 `silver-pulse/config.py` 的 `SOURCES`，**共 62 个**（不是 about.html 写的 37 个）。about.html 已严重滞后，必须同步。
- 评级分布：**T1=22 / T2=31 / T3=9**。
- 地区严重失衡：**海外 55 / 国内仅 7**。对"以海外为镜、照中国之路"的定位，国内侧（读者和创业者所在）太薄。
- 采集方式：**rss 18 / google_news 46 / manual 4**。其中 **20 个标了 `unreliable`（Google News 代理单点依赖）**——代理一抖，这些源就集体 0 产出。
- 已判死刑还没清理：4 个 `suspected_dead`（连续 3 周 0 产出）：`agetechnews`、`agetech_com`、`vcbeat_aging`、`third_act`。
- 9 个 `watch`（近期波动）：startuphealth、axios_health、businesswire、techcrunch、cncaprc、agetech_space、maryfurlong、inc5000、a2collective。
- 已有地基：`source_health.py`（健康检测）、`feedback_loop.py`（收藏回灌权重）、`scored_latest.json`（可算"进精选率"）。但**目前只测"抓到多少"，没测"进精选多少"= 只看健康度、不看质量**。

---

## 2. 我的角色与边界

- **我负责**：发现新源、评估新旧源、给增/删/升降级建议、维护评级、产出盘点/周报、保持 about.html 与代码同步。
- **我不擅自动**：删源、升 T1、改部署——这些先给你方案，你拍板后我再做（遵守"先对齐再执行"）。
- **不在我范围**：评分维度、展示 UI、企业库结构。

---

## 3. 评级体系：T1/T2/T3 怎么用 + 要不要补一层"质量观测"

现有 T1/T2/T3 是"权威性/覆盖层级"评级，已经够用，我建议**保留结构不动**，但补一个**质量观测维度**（不进终分，只进治理决策）：

- 相关性命中率（抓到的里多少真过银发闸门）
- 进精选率（过闸后多少进 Top 精选）
- 稳定性（连续 0 产出周数）
- 代理依赖（是否直连 RSS）

这样"评级"从二维（T档 + 健康）变成"T档 + 质量观测"，淘汰/升级才有数据依据，不靠拍脑袋。

---

## 4. 短期计划（1~2 周，先不动生产、低风险）

**第1步｜出《信源盘点总表》**：把 62 源按 T档 / 地区 / 采集方式 / 健康度 / 质量观测拉成一张表，标红 dead、标黄 watch、标蓝代理依赖。给你一眼看清家底。（这是"评估现有信息"的直接落地）

**第2步｜清理 4 个已死源**：建议对 `agetechnews / agetech_com / vcbeat_aging / third_act` 做"换直连 RSS 或降级/移除"。其中 `agetech_com` 是你之前指定 T1 的，我会先给换源方案，不擅自降。

**第3步｜补国内一手源（优先方向，待你确认）**：国内现在 7 个且多半代理依赖、`vcbeat` 已挂。候选方向：中国养老网、人民网/新华网养老频道、动脉橙、钛媒体银发、各省市养老协会/政策站、银发消费研究号等。我先出《国内信源候选清单》（含 URL / 是否直连 RSS / 预估质量）。

**第4步｜降代理依赖**：对 20 个 `unreliable` 源逐个用 feedparser 验证直连 RSS 可达性，能换直连的换直连，换不了的标"代理依赖-观察"。

**第5步｜建立"新信源发现"常态化动作**：每周用 WebSearch 扫描银发经济相关的协会 / 研究所 / 政府 / VC 博客 / 垂直媒体 / 视频号，产出《新信源候选清单》供你挑。

---

## 5. 中长期规划（自我迭代机制）

- **信源健康月报 + 自动建议**：把 `source_health` 从"采集后汇报"升级为"治理决策输入"——每月出月报，自动给出建议（谁该升 T1、谁该降 T3、谁该换直连、谁该删）。
- **质量评分模型落地**：用 `scored_latest.json` 算每个源的"进精选率 / 相关性命中率"，真正按质量排序，而非只看抓取量。
- **收藏反馈闭环打通**：你导出的 `feedback.jsonl`（⭐收藏）能反推"哪些源出的选题被你收藏最多"——这是最真实的信源质量信号，直接给源加权。
- **自动化**：把"信源发现 + 健康巡检"做成定时任务，每周一跑，主动把候选清单 + 月报推给你（不依赖你想起）。
- **评分恢复后**（若你重启 AI 评分），对 Top 源产出做质量抽检。

---

## 6. 自我迭代怎么"自己转起来"（落地动作）

1. **changelog**：每次增 / 删 / 升降级都写 `source_changelog.md`（日期 + 做了啥 + 原因），可追溯可回滚。
2. **信源周报**：每周给你一份——新增了啥、清理了啥、哪些源在掉血、下周要验证啥。
3. **about.html 同步铁律**：凡动 `SOURCES`，必须同步更新 about.html 的信源表（它是 SSoT，否则又会像现在这样脱节 37 vs 62）。
4. **重大变更先方案后执行**：删源 / 升 T1 先给文档你拍板；加候选清单 / 跑验证我直接做。
5. **护栏**：任何改动不自动改线上精选行为、不烧大积分、不动评分架构。

---

## 7. 需要你拍板 / 补充的几点（关键澄清）

1. **积分预算**：HY3 免费期还有效吗？加源会增加采集 token。我建议每次先 +5 个候选、验证稳了再进生产，控制增量。你意下如何？
2. **国内信源优先级**：是否把"补国内一手源"作为第一优先？还是维持"海外为主、国内精选"的现状？
3. **部署权限**：我改 `config.py` 后，是自动重新生成 `output/` 并部署 gh-pages，还是你手动来？（记得你之前把自动化暂停了）
4. **评级判据对齐**：T1/T2/T3 你的硬标准是什么？比如"被你指定 T1 但已挂的 agetech_com"——我建议先降回 T2 观察或换直连，你拍板。
5. **那 4 个 suspected_dead**：直接移除 / 换源，还是先降权观察一轮？

你回这几点，我就把第1步《信源盘点总表》和第3步《国内信源候选清单》先做出来给你挑。
