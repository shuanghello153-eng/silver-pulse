# 企业库运营手册 V8.4

> **版本 V8.4（2026-07-25）** · **基于 V8.3 修正**（小爽 07-25 第七轮反馈：维护规则归收尾 / 正文瘦身 / 合并重复 / 删啰嗦）

---

## 顶部 · 变更对照表（V8.4 vs V8.3，仅本次）

| # | 改动                   | 说明                                                   |
| - | -------------------- | ---------------------------------------------------- |
| 1 | 升版本动作 + 标记约定 + 一致性校验 | 从 §4.2「任务前步骤 B」**移回 §5.11 收尾**（这是文档维护动作，不是任务前动作）     |
| 2 | 流程表与节点分类合并           | §4.4（11 步名）+ §4.5.1（节点类型）合并为一表，消除重复                  |
| 3 | 强词清单移附录              | 正文 §5.1 只留一行引用，76 词完整清单移附录 C                         |
| 4 | 前端展示字段简化             | 正文只列 12 字段名 + 特殊规则一句话；详细表格 / 不再展示字段 / 筛选排序规则 → 移附录 C |
| 5 | 门禁自测去表格化             | 删编号列和"不通过后果"列，只列脚本名 + 检查要点                           |
| 6 | 删废话                  | 删"不要看是否出现这几个词"等此地无银式解释                               |
| 7 | 解释性文字排版统一            | 全部用 `> ``` 引用块代码块` 格式                                |
| 8 | 汇报内容精简               | 精简为真·三样，复盘归入 #③                                      |
| 9 | 附录 C 增强              | 收纳：强词清单 + 展示字段详情 + 不展示字段 + 筛选排序规则                    |

---

## 1. 网站定位

Silver Pulse（银脉）= 银发经济投融资资讯聚合 **+ 企业数据库**，本质是小爽的"选题情报台"——让小爽一眼看到"今天哪些企业 / 资讯值得写深度稿"。

每家企业 / 每条资讯的终点问题都是：中国银发经济创业者能从中借鉴什么？

---

## 2. 基本情况

- **唯一真相源**：`data/enterprise/all_enterprises.json` —— 直接改它 = 改真相。当前约 **1773 家**、40+ 字段。
- **部署**：独立 `gh-pages` 分支（main 放代码/数据，gh-pages 只放生成的网页）。LIVE：`https://shuanghello153-eng.github.io/silver-pulse/`
- **采集**：由**信源扩充 AI** 负责（RSS-first，44 信源，海外为主）。企业库不碰采集。

---

## 3. 个人职责

**你是企业库的唯一维护者，**&#x6570;据入库 + 前端渲染 + 部署+文档与记忆等，全部由你负责，**所有字段你都要维护**（只读 / 废弃字段除外，见附录 C）。

**小爽**：定方向、做最终审核与发布放行。你**不擅自入库、不擅自部署**；改小爽手标内容必须【动你标注】+ 显式理由交他拍板。

**信源扩充 AI**：全网搜集资讯渠道和企业名单，提交给资讯 AI、企业库 AI（=你）

**资讯 AI**：资讯一切维护（含入库、评分、推荐理由、前端渲染、部署、文档与记忆等）

**标签 AI**：负责标签体系体系`tag_l1`、`tag_l2`构建

---

## 4. 整体工作流程

所有任务走**同一套流程**，只是「从哪进、走到哪停」不同。不存在多套独立流程。

### 4.1 需求来源

| 来源                  | 走哪条流程               | 说明                                           |
| ------------------- | ------------------- | -------------------------------------------- |
| **A. 小爽给企业名单**      | → 完整 1→11 步         | 最常见                                          |
| **B. 每周资讯扫描发现企业名单** | → 完整 1→11 步         | 每周日 23:00 自动化扫描 附录 B                         |
| **C. 外部 AI 给企业名单**  | → 完整 1→11 步         | 附录 A、附录 B                                    |
| **D. 存量走查发现问题**     | → 从步骤 3 起（准入/去重已做过） | 纯 UI/UX 小改动可走「前端校验 → 直接部署 → 事后补审」，不必走完整审核关卡。 |

- 目前大部分任务都记录&#x5728;**《**&#x4F01;业库任务规划_V\*.md》（最新版本V\*），可自行领取。

### 4.2 入库主流程

（这部分应该是简述一下基本流程规则，同时把需要Ai执行的代码、衔接方式等列出来，AI看这个就应该能知道整个入库流程怎么做）

| #步骤                 | 类型            | 代码                                               | 衔接方式                                                                                   |
| ------------------- | ------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------- |
| 1、**银发相关性判断**（入库准入） | 脚本初筛+AI 二筛    | `selection/relevance_screener.py`                | `config.SILVER_STRONG_KEYWORDS` 命中 → 低模是非题                                             |
| 2、**全库去重**          | 脚本粗筛 + AI 裁决  | `scores_draft/rework/dedup_keyword_prescreen.py` | `dedup_keyword_prescreen.py` → 疑似候选人工比对                                                |
| 3、**补全所有字段**        | 脚本+AI         | AI 为主？？？                                         | 联网/翻译；引用网址顺手记录；**写推荐理由 recommend；算评分（**&#x4FE1;号强度(脚本) + 四维精评AI→ 写入候选.jso&#x6E;**）？？** |
| 4、                  |               | 脚本 + AI 走查                                       | `check_single.py` + `check_description.py` → 子智能体语义走查(≤2轮)                             |
| 5                   |               |                                                  |                                                                                        |
| 6                   | **前端展示校验**    | 脚本                                               | `gen_enterprise.py` 生成 → 肉眼看卡片                                                         |
| 7、                  | **质量门禁**      | Ai 走查                                            |                                                                                        |
| 8                   | **小爽审核**      | **人工卡点**                                         | 预览表 + 审核附言，永不自动化                                                                       |
| 9                   | **合并部署**      | 脚本                                               | `enterprise_build.py --candidates 候选.json --merge --deploy`                            |
| 10                  | **上线走查 + 抽查** | AI + 人眼                                          | 子智能体 + 抽查 5 家                                                                          |
| 11                  | **收尾**        | AI                                               | memory / git / 归档 / 升版本(如需) / 自检 / 汇报                                                  |

```
贯穿全程的工作产物 = 候选 JSON（候选.json）
├─ 步骤 1–9 产出的新企业数据先写进候选 JSON
├─ 门禁 / 预览 / 合并 全部读它
├─ 步骤 9 才合并进真相源(all_enterprises.json)
└─ 评分/门禁默认对候选.json跑，绝不在合并前改全库
```

### 4.3 编排脚本与防卡住

**编排脚本** `scripts/enterprise_build.py`：

```
python scripts/enterprise_build.py --candidates 候选.json            # 门禁自测（不合并）
python scripts/enterprise_build.py --candidates 候选.json --merge    # 门禁→备份→增量合并→生成
python scripts/enterprise_build.py --candidates 候选.json --merge --deploy  # 以上 + 部署
```

默认不带 `--deploy`，步骤 8 放行后才加。质量不受影响：串联的全是确定性节点；语义节点仍由 AI/小爽把关。

**不会卡住的保障**：

- 脚本报错 → 看报错修数据，重跑
- AI 拿不准 → 标记「待小爽定」，进步骤 8，**不阻塞其他企业**
- 子智能体超时/差 → 换 prompt 或自己做一遍再让它复查
- 小爽驳回 → 只修有问题部分，不从头来
- **单家不阻塞批量**（一批 20 家有 3 家争议，先推 17 家，3 家「待定」）

所有 AI 判断统一用最便宜模型（0805前 HY3 免费可用）。

---

## 5. 每步骤详细规则

（这部分内容应该是给我看的！让我了解详细流程规则，不那么重要且占篇幅多的内容放到目录里了）

### 5.1 步骤 1 · 银发相关性判断（两阶段）

**阶段一 · 初筛（强词脚本）**

`config.SILVER_STRONG_KEYWORDS`（76 个中英文强词），被 `selection/relevance_screener.py` 使用。命中 → 进阶段二；未命中 → 直接拒绝（除非特殊理由）。

> 完整 76 词清单见 **附录 C.1**。代码在 `config.py:2696` 的 `SILVER_STRONG_KEYWORDS`。

**阶段二 · 低模判断（AI 是非题）**

符合任一即保留：

- 直接面向 **50 岁+或 60 岁+** 人群（C 端）
- 面向 **Medicare Advantage 参保老人（C 端）**
- 为上述人群的服务方提供产品 / 服务

**直接拒绝**：综合电商（养老仅「规划中」）、品牌 PR、Web3、泛再生医学、泛医疗 VC、泛融媒体。

> 关键词门禁是辅助，**最终以实质业务判断为准**；不确定联网核查，默认从严。

### 5.2 步骤 2 · 全库去重

**比对对象必须是全库 `all_enterprises.json`**

**粗筛**：`python scores_draft/rework/dedup_keyword_prescreen.py --name "企业名"` 或 `--candidates 候选.json`

原理：去括注 → 去公司后缀 → 去城市前缀 → 核心词提取 → 全库子串匹配 → 输出疑似重复。

**精确裁决**：

| 情形            | 处理                     |
| ------------- | ---------------------- |
| 一致（信息无差异）     | 跳过                     |
| 增量（相似但信息有补充）  | 合并进已有记录，补增量；拿不准标「待小爽定」 |
| 冲突（疑似同家但业务矛盾） | 联网复核，或交小爽裁决            |

### 5.3 步骤 3 · 补全所有字段

- **重点字段**：
  - **信号强度：**`signal_strength.py`：企业与资讯共用 `compute_signal_strength()`。渠道加权+时效加权+IPO/融资/收购按金额/地区分级。
  - 四维评分：`merge_batch_scores.py`，`total_score = 信号强度×30% + 信息×30% + 差异×20% + 复制×20%`（每维评分取值 0~10，前端 ×10 显示封顶 100）
  - **标签 `tag_l2`**：从 `data/enterprise/_l2_l1.json` 现有体系选 1~3，不自造新词
  - **搜索索引** = `name`(+name_cn) + `description` + `tag_l1`/`tag_l2`（含同类词）+ `recommend`。

**引用网址记录**：补字段时把引用网址写入字段（website_url / crunchbase_url / source_urls 等）；月度入库后可跑 `python scripts/collect_source_urls.py` 统计高频站点 / 渠道去重，输出至`handoff/news_inbox/`。

### 5.5 步骤 6 · 前端展示校验

联网 / 翻译补齐附录 C 里所有在用字段，**补不到** → 标 **「待补充」**，**禁止编造**

**必填最低门槛**：`serial` / `name` / `region` / `description(≥20字)` / `source` / `ingest_time` / `update_time`

**展示字段（12 个，有值才渲染，空值一律隐藏），**&#x8DD1; `gen_enterprise.py` 生成，肉眼检查卡片。

| 字段                           | 展示位置   | 说明                                                                                                                               |
| ---------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------- |
| name + name_cn               | 卡片头部   | 中文名优先，英文名副                                                                                                                       |
| tag_l2                       | 头部标签栏  | 只展示二级标签（一级不展示）                                                                                                                   |
| total_score                  | 头部评分徽章 | **0~10 分制存储，前端 ×10 取整封顶100**；hover 显示四维（信号·信息·差异·复制）。`merge_batch_scores.py`，`total_score = 信号强度×30% + 信息×30% + 差异×20% + 复制×20%` |
| description                  | 卡片主体   | 介绍：说清「做什么 / 服务谁 / 怎么赚钱」（事实）`check_description.py`                                                                                |
| recommend                    | 卡片主体   | 推荐理由：一段自然流畅的话，把信息量 / 差异化 / 可复制融进去讲，禁止机械三段式、重复列表。 讲「为什么值得写」（判断）`check_single.py`                                                  |
| funding_latest               | meta 行 | 最新融资（需含数字才显示；金额 `norm_amount` 转换 M→万、B→亿，币种符号保留）                                                                                 |
| funding_total                | meta 行 | 累计融资（需含数字才显示；同上）                                                                                                                 |
| investors                    | meta 行 | 投资方                                                                                                                              |
| payor_model                  | meta 行 | 支付模式（如"商业保险支付+个人自费"），有值才显示                                                                                                       |
| founded                      | meta 行 | 成立年份                                                                                                                             |
| website_url / crunchbase_url | meta 行 | 外链                                                                                                                               |
| 🔥 徽标                        | 头部     | "近期有动态"（仅非媒体企业、且 30 天内有被报道）                                                                                                      |
| 近期动态/报道                      | 卡片底部   | 关联新闻列表（媒体企业显示"近期报道"）                                                                                                             |
| deep_article_links           | 卡片底部   | 深度文章链接（有值才显示，样式类似近期动态报道，放在卡片最后一排；无则整块隐藏）                                                                                         |

`name(+name_cn)` / `tag_l2` / `total_score`(×10显示) / `description` / `recommend` / `funding_latest` / `funding_total` / `investors` / `payor_model` / `founded` / `website_url&外链` / `🔥徽标&近期动态` / `deep_article_links`

> 特殊规则：融资金额需含数字才显示，金额 norm_amount 转 M→万/B→亿；🔥 徽标仅非媒体企业且 30 天内有被报道时显示。
>
> 完整展示字段详情（含位置/说明）、不再展示的字段及原因、筛选器、排序规则 → 见 **附录 C.2**。

- **搜索索引** = `name`(+name_cn) + `description` + `tag_l1`/`tag_l2`（含同类词）+ `recommend`。

**关卡 2 · 子智能体走查（≤2 轮）**：

派 general-purpose 子智能体，指令要点：

1. description 是否中文为主、说清做什么/服务谁/怎么赚钱、无模板套话
2. recommend 是否有真判断（信息量/差异化/可复制三方面）、不复述 description 基本事实
3. 两者是否重复
4. 银发信号判定是否合理
5. 字段齐全（尤其 description / source / ingest_time）
6. 标签是否在 `_l2_l1.json` 词表中

循环：第一轮 → 你修复 → 第二轮（换 prompt 或同一个重跑）→ 最多两轮 → 仍有问题标「待小爽定」。每轮之间由你负责修改，不让子智能体自己改。

**关卡 3 · 自己审一遍**：打开预览表逐行看，确认走查问题修好、无漏网。

### 5.7 步骤 8 · 小爽审核

发**全字段预览表**（自动生成：`python scripts/build_preview.py --candidates 候选.json --out 预览表.xlsx`）。银发信号判定 / 去重结果两列留空由你填。

**审核附言（必写，三部分）**：①遇到的问题 ②需要协助 ③建议。不写「无」。

**驳回处理**：个别误 → 改字段重跑门禁重提；多家共性问题 → 批量改重提；规则分歧 → 给判断+理由交小爽拍板。驳回 ≠ 从头来，只修有问题部分。

### 5.8 步骤 9 · 合并部署

首选一条命令：`python scripts/enterprise_build.py --candidates 候选.json --merge --deploy`

手动分步（等价，出问题时定位）：

1. 编辑 `all_enterprises.json`（合并 = 编辑真相源）
2. 跑 `gen_enterprise.py` 重生成 `output/`
3. `deploy_ghpages.py` 部署
4. `git commit` 留痕

### 5.9 步骤 10 · 上线走查

- **走查子智能体**：合并前后都派，背景设「国内小白读者」，查 description / recommend / 银发判定 / 字段齐全
- **上线后抽查**：至少抽 5 家看渲染 / 排序 / 中文介绍
- **回滚**：技术类问题直接改+重部署；规则或数据库实质变更走收尾确认，必要时回滚。严禁现场手改线上

### 5.10 步骤 11 · 收尾

做完一批企业后，按顺序执行：

**1.&#x20;**&#x6253;开 `企业库任务规划_V*.md`，完成移「已完成」、新增登「进行中」、遗留登「待办」。

2\. \*\*检查规则是否变了\*\* → 变了走下面「升版本动作」（含四问复盘）；没变跳过。

**写 memory（两份）**

> ```markdown
> - 每日工作日志 `.workbuddy/memory/YYYY-MM-DD.md`：记「本轮做了什么」（append-only）
> - 项目长期记忆 `.workbuddy/memory/MEMORY.md`：只记跨轮不变的长期事实（版本号/铁律/约定）；版本升了同步更新版本号
> - 两者都在 `silver-pulse/.workbuddy/memory/` 目录，**不进 git**
> ```

**2. git 提交留痕**：所有改动 commit（不一定要 push）。

**3. 归档清理（月度）**：每月检查 `_deprecated/` 下 1 个月前的临时文件 → 删或续归档；根目录散落脚本移 `_deprecated/`。

**4. 升版本（仅当规则/流程/字段表发生变化时才做，不变则跳过）**

> ✏️【调整 V8.4】**升版本动作从 §4.2 移回此处**——因为这是文档维护动作，应在收尾时执行，不是任务前的准备工作。
>
> ```
> 1. 判版本号：功能变更 → 大版本+1；修正优化 → 小版本+0.1；纯错字不升
> 2. 顶部写变更对照表：只列本版相对上版的变动，每条标 🆕/✂️/✏️
> 3. 正文标改动：每个调整处紧跟一个引用块说明原因；同时删掉上一版的所有解释块
> 4. 与上一版逐节比对：上版有的本版保留或显式说明为何移除
> 5. 版本沿革（附录 E）补一行
> 6. 旧版移入 `_archive_企业库旧文档/`；运维目录只留最新版 + 任务规划 + 行为准则
> 7. 更新 MEMORY.md 的当前版本号
> 8. git 提交（与代码改动同一次 commit）
> ```

> 📌 **标记约定（每次改文档必守）**：任何改动哪怕一字，都用引用块标注 🆕【新增】/ ✂️【删除】/ ✏️【调整】并写原因；正文保持纯净不带符号；下版整体删除这些解释块。
>
> 📌 **文档代码一致性（改代码或改文档都执行）**：改了代码（门禁/评分/字段/脚本）→ 同步查手册对应节；改了手册（规则/流程/字段表）→ 同步查代码实现；两者同一次 commit。不一致以「经判断的最新正确版本」为准（代码也可能错）。

**5. 自检项（汇报前快速勾过）**

> ```markdown
> - [ ] 标记完整性：所有改动都有 🆕/✂️/✏️ 引用块，正文纯净零符号
> - [ ] 对照表准确性：顶部对照表列出了本版全部变动，无遗漏
> - [ ] 字段/门禁/前端表：与上一版逐项核对，无遗漏无多余
> - [ ] config.py 同步：强词清单等双写位置（代码+手册）一致
> - [ ] memory 双份：日志 + 长期记忆都已更新
> ```

**6. 汇报小爽**

```
① 需拍板/协作的点
② 我的下一步计划
③ 我的建议/想法 + 本次复盘（解决了什么 / 为什么发生 / 怎么避免再发生 / 落在哪一层）
```

> 复盘规范见《企业库行为准则》最新版（`企业库运维/企业库行为准则.md`）。

**7. 最后一步：派子智能体走查全文**

派子智能体把以上步骤 1–11 全部走查一遍，如有问题自行改正确保无误后再结束本轮任务（防止 memory / git / 汇报这类软性步骤漏掉）。

---

## 附录 A · 协作机制

### A.1 外部 AI 名单交接

信源扩充 AI 交来的企业名单放在 `handoff/enterprise_inbox/`（JSON 格式，含 name / website / business / funding / relevance 等字段）。你读取后按正常入库流程走。

> 信源 AI 也可能往 `handoff/news_inbox/` 交付资讯类内容，那个归资讯 AI 管，你不碰。

### A.2 共享文件夹规范

共享目录 `handoff/` 下大家平等协作：

- **各自管自己的文件**，不删不改别人的文档
- **交付**：企业候选 → `handoff/enterprise_inbox/`；资讯 → `handoff/news_inbox/`
- **提建议**：通过 `handoff/cross_ai_notes/`（文件头标注 to/from/status）
- **文件命名**：建议格式 `{来源}_{内容}_{日期}.json`（如 `W13_B_企业_2026-07-25.json`）

## 附录 B · 自动化任务设计

两个 ACTIVE 自动化（均接入 `handoff/.scan_progress.json` 幂等逻辑，断点续扫，保证全量覆盖存量）：

| 任务      | 时间            | ID                       | 做什么                                       |
| ------- | ------------- | ------------------------ | ----------------------------------------- |
| 资讯扫描入库  | **每周日 23:00** | automation-1784779809249 | 扫资讯 → 提新企业 → 入库到预览发审                      |
| 跨AI笔记查看 | **每周日 22:00** | automation-1784882846607 | 跑 cross_ai_digest.py → 处理给企业库的笔记 → 标 done |

> 幂等机制：先读 `.scan_progress.json` 取上次进度 → 从断点续扫 → 完成后更新进度文件。无论停几周、手动跑过与否，都能最终覆盖全部历史存量。旧 5 个采集自动化 PAUSED。

## 附录 C · 参考数据

### C.1 强词清单（76 个，中英文）

**存放位置**：`config.py:2696` 的 `SILVER_STRONG_KEYWORDS`（代码级权威）。其他 AI 如需用，从本附录复制或 `import config` 读。发现漏词通过 `cross_ai_notes` 提建议，由企业库 AI 评估更新。

**中文（33 个）**：  
银发 / 养老 / 老年 / 老龄化 / 长者 / 退休 / 适老 / 适老化 /  
居家护理 / 居家照护 / 护理 / 家庭护理 / 家庭健康 /  
康复 / 慢病 / 认知症 / 痴呆 / 阿尔茨海默 / 失智 / 失能 / 长寿 /  
护理院 / 养老院 / 敬老院 / 养老社区 / 康养 / 医养 / 助老 /  
陪护 / 康护 / 颐养 / 老年病 / 养老产业 / 银发经济 / 养老服 /  
养老险 / 养老金融 / 养老科技 /  
社会健康因素（SODH 中文）

**英文（43 个）**：  
senior / elderly / aging /  
aged care / dementia / alzheimer /  
long-term care / long term care /  
nursing home / assisted living /  
home care / caregiver / caregivers / caregiving /  
hospice / hospice care / palliative care /  
home health / home health care / skilled nursing care /  
medicare / medicaid / retirement /  
senior living / pace program / value-based care /  
older adult / older adults / silver economy /  
age tech / agetech / senior care / elder care /  
memory care / senior housing /  
SODH（Social Determinants of Health）

### C.1.1 四维评分

综合评分 = 信号强度×30% + 信息量×30% + 差异化×20% + 可复制×20%

前端展示数值，四个维度分别*10

1、信号强度（0~10 分）  
各项相加，封顶 10 分（\`selection/signal_strength.py\`）：  
.信源起点：T1=0.5，T2=0.3，T3=0.1（阶段2 已预筛过银发相关性，信源等级没那么重要，只作微弱起点）。  
. IPO = +6。  
.融资（海外美元，≥）：10 亿=5，1 亿=4，5000 万=3，1000 万=2，500 万=1，100 万=0.5，其余=0。  
.融资（国内元，≥）：10 亿=6，1 亿=5，5000 万=4，1000 万=3，500 万=2，100 万=1，50 万=0.5，其余=0。  
.收购（海外美元 / 国内元，≥）：100 亿=6，50 亿=5，10 亿=4，1 亿=3，5000 万=2，1000 万=1。  
.时效（每月重算）：近 1 周=2.5，近 1 月=2，近 2 月=1.5，近半年=1，近 1 年=0.5，更久=0.25。

2、信息量（0~10）

搜这家企业 / 事件前几页信息的数量和质量（信息越多越好写，通常是大企业 / 热点）。

3、差异化（0~10）

商业模式反不反常识、企业起落是否反复巨大。

4、可复制（0~10）

基于企业库 + 可联网搜网上信息，判断国内有无同类赛道、赛道里有没有上亿融资 / 上市公司、支付方差异（美国 Medicare / 自费 vs 中国医保 / 自费，日本近似中国）→ 能不能平移、是空白还是红海。

### C.2 前端展示字段详情

**展示字段（12 项，有值才渲染）**：

| 字段                           | 位置     | 说明                               |
| ---------------------------- | ------ | -------------------------------- |
| name + name_cn               | 卡片头部   | 中文名优先                            |
| tag_l2                       | 头部标签栏  | 只展示二级标签                          |
| total_score                  | 头部评分徽章 | 0~10 存储，前端 ×10 封顶 100；hover 显示四维 |
| description                  | 卡片主体   | 中文介绍（主字段）                        |
| recommend                    | 卡片主体   | 推荐理由                             |
| funding_latest               | meta 行 | 最新融资（需含数字才显示；M→万、B→亿）            |
| funding_total                | meta 行 | 累计融资（同上）                         |
| investors                    | meta 行 | 投资方                              |
| payor_model                  | meta 行 | 支付模式                             |
| founded                      | meta 行 | 成立年份                             |
| website_url / crunchbase_url | meta 行 | 外链                               |
| 🔥 徽标 & 近期动态                 | 头部/底部  | 仅非媒体企业且 30 天内有被报道                |
| deep_article_links           | 卡片底部   | 有值才显示，无则整块隐藏                     |

**不再展示的字段（及原因）**：

- `highlights` — 与 description 60%+ 重复
- `business_tags` — 雷同度高
- `tag_l1` / `category_l1` / `category_l2` — 用户要求只保留 tag_l2
- `region` — 太粗糙
- `source` — 对选题决策无直接价值
- `competitors` — 数据为空（0 家有值）
- `events` — 与融资/meta 重复
- `business_model` — 已被 business_tags 替代

**特殊规则**：各字段各自 `if` 隐藏；空值 / "未搜到" / "未披露" / "未知" 一律隐藏。

**筛选器**：tag_l2 标签 pills、region（国内/海外）、一级/二级分类、时间窗口、🔥 近期动态、📖 收藏 / 👁 未读 / 🙈 已隐藏。

**排序**：前端可切换 动态↓ / 评分↓ / 融资金额↓。评分用 total_score（×10 显示）。**默认排序**：`last_news_map` 四来源 → 有动态排前 → 同日期评分高排前 → 无动态按评分高排前。

### C.3 企业库字段全表（41 个）

| 组 | 字段                 | 类型       | 必填 | 说明                      |
| - | ------------------ | -------- | -- | ----------------------- |
| A | serial             | string   | ✅  | 唯一编号 #XXXX，取现有最大值 +1    |
| A | name               | string   | ✅  | 企业名（英文优先）               |
| A | name_cn            | string   | —  | 中文名                     |
| A | website_url        | string   | —  | 官网                      |
| B | description        | string   | ✅  | ≥20字中文介绍                |
| B | recommend          | string   | ✅  | 推荐理由                    |
| B | tag_l1             | string   | ✅  | 一级标签（从 \_l2_l1.json 反推） |
| B | tag_l2             | string[] | ✅  | 二级标签（1~3个）              |
| B | payor_model        | string   | —  | 支付模式                    |
| C | funding_latest     | object   | —  | 最新融资                    |
| C | funding_total      | object   | —  | 累计融资                    |
| C | investors          | string[] | —  | 投资方                     |
| C | stage              | string   | —  | 融资阶段（不参与搜索索引，可展示）       |
| C | founded            | number   | —  | 成立年份                    |
| C | region             | string[] | —  | 地区                      |
| C | employee_count     | object   | —  | 员工数                     |
| D | source             | string   | ✅  | 数据来源                    |
| D | ingest_time        | string   | ✅  | 入库时间                    |
| D | update_time        | string   | ✅  | 更新时间                    |
| D | signal_strength    | number   | —  | 信号强度 0~10               |
| D | info_score         | number   | —  | 信息量 0~10                |
| D | diff_score         | number   | —  | 差异化 0~10                |
| D | copy_score         | number   | —  | 可复制 0~10                |
| D | total_score        | number   | —  | 总分 0~10（四维加权）           |
| E | crunchbase_url     | string   | —  | CB 链接                   |
| E | reference_urls     | string[] | —  | 参考链接                    |
| E | source_urls        | string[] | —  | 来源网址                    |
| E | deep_article_links | string[] | —  | 深度文章链接                  |
| E | news_count         | number   | —  | 近期报道数                   |
| F | highlights         | string   | —  | （已停止独立展示）               |
| F | business_tags      | string[] | —  | （已不展示）                  |
| F | category_l1        | string   | —  | （废弃，用 tag_l1）           |
| F | category_l2        | string   | —  | （废弃，用 tag_l2）           |
| F | competitors        | string[] | —  | （数据为空）                  |
| F | events             | object[] | —  | （与融资重复）                 |
| F | business_model     | string   | —  | （已被替代）                  |

## 附录 D · 代码地图

| 脚本             | 路径                                               | 用途                            |
| -------------- | ------------------------------------------------ | ----------------------------- |
| 初筛             | `selection/relevance_screener.py`                | 强词命中判断                        |
| 信号强度           | `selection/signal_strength.py`                   | compute_signal_strength() 纯函数 |
| 去重粗筛           | `scores_draft/rework/dedup_keyword_prescreen.py` | 关键词子串匹配去重                     |
| Description 门禁 | `scores_draft/rework/check_description.py`       | D1-D4                         |
| Recommend 门禁   | `scores_draft/rework/check_single.py`            | R1-R10                        |
| 评分合并           | `merge_batch_scores.py`                          | 重跑四维+总分                       |
| 编排流水线          | `scripts/enterprise_build.py`                    | 门禁→备份→合并→生成→部署                |
| 预览表生成          | `scripts/build_preview.py`                       | 候选.json → Excel 全字段预览表        |
| 网址统计           | `scripts/collect_source_urls.py`                 | 高频引用站点 + 渠道去重                 |
| 跨AI笔记          | `scripts/cross_ai_digest.py`                     | `--ai 企业库AI`                  |
| 页面生成           | `gen_enterprise.py`                              | 企业卡片 HTML 生成                  |
| 部署             | `deploy_ghpages.py`                              | gh-pages 部署                   |

## 附录 E · 版本沿革

| 版本       | 日期        | 核心变化                       |
| -------- | --------- | -------------------------- |
| V1–V7.4  | —         | （见归档文件）                    |
| V7.5     | 07-24     | 主流程重构（**未采纳**，已归档）         |
| V7.6     | 07-24     | 基于 V7.4 优化（已归档）            |
| V7.7     | 07-24     | 恢复 V7.4 误删细节（已归档）          |
| V8.0     | 07-25     | 最终精炼：删自检清单/标记升级/强词 76（已归档） |
| V8.1     | 07-25     | 标记规范升级+结构重组+自动化重设计（已归档）    |
| V8.2     | 07-25     | 基于 V8.0 最终修正（已归档）          |
| V8.3     | 07-25     | 维护规则归步骤+符号还原+自动化修正（已归档）    |
| **V8.4** | **07-25** | **维护规则归收尾/正文瘦身/合并重复/删啰嗦**  |
