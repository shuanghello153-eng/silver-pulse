# 数据质量监控方案 · Silver Pulse（v1，2026-07-16）

## 0. 一句话定位
**我（领导视角）own 一台"只读测量仪"**：自动扫描真相源、量化数据健康度、及时暴露问题。**只读不写**，与所有并行窗口零冲突。

## 1. 职责边界（划清，避免撞车）

| 工作 | 归属 | 理由 |
|---|---|---|
| 字段完整度 / 分类分布 / 资讯新鲜度 / 主体漂移 / 趋势 | **我（本监控）** | 纯测量，不需要任何业务规则知识 |
| 标签合规校验（跨一级、词表膨胀、同类词） | **标签库团队** | 他们 own 规则与映射，最清楚怎样改 |
| 评分 / 推荐理由 | **评分团队** | 已在重构（5 角色 + 子智能体校验） |
| 企业库字段补全 / 打标 / 入库新企业 | **企业库 / 信源团队** | 写入权在他们 |

**铁律（本监控绝不破）**：
- 只读 `data/enterprise/all_enterprises.json` 与 `data/scored_latest.json`，绝不 WRITE。
- 不改 `config.py`、不重写标签、不评分。
- 对其他团队只"列信号、不处理"——报告第六节的清单是移交依据，不是我改。

## 2. 已交付（v1）
- `monitor_data_quality.py`：核心脚本，stdlib only，管理端 python 可跑。
- `data/monitoring/数据质量监控_YYYY-MM-DD.md`：人类可读周报。
- `data/monitoring/history.json`：趋势基线（已存 2 次快照，下次起可看涨跌）。

## 3. 监控维度明细
| 维度 | 指标 | 作用 |
|---|---|---|
| 字段完整度 | 19 个字段填充率% | 看出哪块数据最秃，给补全排优先级 |
| 分类分布 | 一级分类 / 阶段 / 地区 / 成立年代 | 看出赛道集中度、海外/国内比 |
| 资讯健康度 | 条数、日期范围、距今天数、精选占比、主体漂移 | 看出资讯是否停更、选题链路是否断 |
| 趋势 | 与本快照对比填充率涨跌 | 看出其他团队的补全是否在生效 |
| 漂移信号 | 资讯主体不在企业库的清单（分真实/噪声） | 给信源团队精准补库清单 |

## 4. 自动化
- **每周一 09:00** 自动跑（本地写 md，不需 GitHub 部署 → 绕开代理阻塞）。
- 跑完向我汇报 3 行：企业/资讯总数、最秃 3 字段、漂移数（真实 vs 噪声）。

## 5. v1 实测暴露的真问题（移交对应团队）
- 完整度最差 5 项：`payor_model` 3.5% / `investors` 26.2% / `stage` 26.4% / `funding_latest.amount` 30.9% / `news_coverage` 32.0% → 企业库团队优先补。
- 资讯停更：最新 2026-07-08，距跑报日 8 天 → 信源/采集团队。
- 主体漂移 36 条（24 真实 + 12 噪声）→ 企业库/信源团队补库。
- 阶段词表不一致（`成长期`/`成长`/`成熟期`/`成熟`/`A轮` 并存）→ 企业库团队统一枚举。

## 6. 后续可迭代（待你确认 / 代理通后）
1. **阈值告警**：某字段填充率跌破 X% 自动标红提醒。
2. **隐藏 dashboard 页**：把报告渲染成站内页（需部署权，代理通了再做）。
3. **字段级趋势图**：history.json 画折线，看补全进度。
4. **资讯去重率 / 源健康度**：统计各 RSS 源近期产出，辅助信源治理。

> 本方案不触碰评分、标签规则、企业库写入，纯独立测量，可长期运行不干扰其他窗口。
