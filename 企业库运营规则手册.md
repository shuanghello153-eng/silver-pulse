# 企业库运营规则手册（V1 · 2026-07-23 收口）

> **本文件是企业库（入库 / 补全 / 评分 / 推荐 / 部署）的唯一收口文档。**
> 做任何企业库改动前，**先读本文件 + 第 1 章列出的权威文档，再动手**。绝不再自造并行规则 / 并行门禁。
> 规则有变更时，本文件升 V 号（V1→V2…），旧版保留不删。

---

## 0. 为什么需要这份手册

规则此前散落在多份文档，且 2026-07-23 曾出现 AI **自造并行门禁**（`data/enterprise/_quality_gate.py`，8 条），与已定规则对不上，造成重复劳动与混乱。本手册把"该信哪些规则、当前实况、协作流程、代码位置"一次收口。

**本次（2026-07-23）纠偏要点**：
- 真实门禁是 `scores_draft/rework/check_single.py`（针对 `recommend` 字段，R1–R10），不是自造的 `_quality_gate.py`。
- "模板话术 / 英文描述" 是 **2026-07-16 就已记录在案的老问题**（见 `走查_C`），不是新发现；应增量修，不该发起"全量重走查"。
- 翻译应脚本 / 批量，不该派子智能体逐家。

---

## 1. 权威规则来源（按优先级读，不要自己造规则）

| 管什么 | 文件 | 路径 |
|---|---|---|
| 企业库全字段返工流程与规则（自称"唯一规则真相源"） | PROCESS.md | `silver-pulse/scores_draft/rework/PROCESS.md` |
| 推荐理由硬门禁 R1–R10（**真实门禁**） | check_single.py | `silver-pulse/scores_draft/rework/check_single.py` |
| 推荐理由写作标准 | WORKER_PROMPT_V4.md | `silver-pulse/scores_draft/rework/WORKER_PROMPT_V4.md` |
| 重洗写手补充（门禁自测 + 草稿格式） | REWORKER_TASK.md | `silver-pulse/scores_draft/rework/REWORKER_TASK.md` |
| 评分 + 信息补全 + 28 字段处置 | 评分信息补全执行手册_V1.md | `silver-pulse/评分信息补全执行手册_V1.md` |
| 模板污染 220 家清单 + 信息准确性校验（只读） | 走查_C_信息准确性_2026-07-16.md | `silver-pulse/output/走查_C_信息准确性_2026-07-16.md` |
| 站点评分 / 阈值 / 部署 / 架构 | RULES.md | `silver-pulse/RULES.md` |
| 评分四阶段权威（"文档1"） | 评分推荐规则_2026-07-12版.md | `silver-pulse/output/评分推荐规则_2026-07-12版.md` |
| 标签体系（"文档2"） | TAGGING_RULES_v3.md | `silver-pulse/TAGGING_RULES_v3.md` |
| 项目交接 / 协作 / 代码地图 | PROJECT_STATUS.md | `silver-pulse/PROJECT_STATUS.md` |

> ⚠️ **铁律**：做任何企业库改动前，先读本手册 + PROCESS.md + check_single.py。绝不再自造"质量门禁"类并行规则。

---

## 2. 当前实况快照（2026-07-23）

- **真相源**：`data/enterprise/all_enterprises.json`，**1773 家**（serial `#0001`~`#1850`，中间有跳号）。
- **渲染字段**：`description`（主）；`desc_cn` 已于 **2026-07-23 由小爽明确弃用**（保留字段不删数据，但代码不再依赖）。
  - ⚠️ 旧文档（手册 V1 / PROCESS.md / check_single.py）仍把 `desc_cn` 当活跃字段 → 这些旧文档需同步标注（见 §6）。
- **评分字段**：`signal_strength / info_score / diff_score / copy_score / research_value`。
  - 已有企业 **禁止重算（只读不写）**；新增企业按脚本算 `signal_strength`。
- **已知未决（老问题，非本次新引入）**：
  - 模板话术：全库约 **170 处**"渠道招商 / 代理分销 / 产业链上下游"类占位。git 证实提交前已存在 168 处，是 2026-07-16 `走查_C` 记录的 220 家模板污染残余 / 同类。
  - 英文描述：仅 **15 家**英文主导，全部 `region=海外`（来源：那本书 / 选题库V4），**0 家 AgeClub 国内批是英文**。
  - 这些应在"描述补全"专项里按 batch 增量修，不是全量重走查。

---

## 3. 真实门禁 vs 曾自造的并行门禁（纠偏）

- **真实门禁 = `check_single.py`**（针对 `recommend` 字段）：
  R1 单字符串 / R2 字数 50–240 / R3 禁模板套话 / R5 三维覆盖（信息量·差异化·可复制）/ R-name 禁企业名 / R-redundancy 禁重复列表字段 / R-integrity / R-noabs / R-jargon / R10 跨企业雷同。
- **描述层质量**：应基于 `走查_C` 的模板清单 + 手册的字段处置，做"描述是否为占位模板 / 是否过短 / 是否英文主导"的检查——这是**已知待办**，不是新门禁。
- **曾自造的 `data/enterprise/_quality_gate.py`（8 条：脏标记 / 长度 / recommend非空 / tag_l1 / 格式 / serial / 英文 / 模板）**：
  ❌ 不在任何已定规则里。部分规则针对已不存在的问题（脏标记当前 0 条）；模板规则过窄，抓不到实际 170 处。
  **结论：该脚本非权威，应退休**（移到 `_deprecated`，不再作为部署门禁）。
- **部署前真正该跑的**：① `check_single.py` 对新增 / 改写 `recommend`；② 描述层检查（`走查_C` 口径）；③ 生成器编译（`py_compile`）。

---

## 4. 字段处置总表（更新到 2026-07-23）

引用 `评分信息补全执行手册_V1` §4，叠加 2026-07-23 决策。

| 类别 | 字段 |
|---|---|
| **我（研究员）写入** | `recommend` / `description`（主）/ `signal_strength`（新企业）/ `info_score/diff_score/copy_score`（新企业，否则不碰）/ `research_value`（新企业）/ 补全类（`name_cn/founded/stage/website_url/crunchbase_url/funding_latest/funding_total/investors/highlights/news_coverage`）/ `deep_article_links` / `related_news_ids` / `update_time` |
| **只读不写** | `tag_l1/tag_l2`（另一 AI 维护）/ `business_tags`（默认）/ `category`（派生只读）/ `source` / `region` / `serial` / `name` / `payor_model` / `ingest_time` / `needs_review` / `tags` |
| **废弃不维护** | `business_model` / `business_model_cn` / `tags` |
| ⚠️ **desc_cn** | 2026-07-23 弃用。旧文档（手册 V1 / PROCESS.md / check_single.py）仍引用它 → 需标注此变更（本手册为最新） |

---

## 5. 协作交互

### 5.1 与小爽
- **方案先对齐再执行**：结构性改动出方案含 WHY + 牵连清单；对齐阶段不写数据、不部署。
- **凡改用户手标内容须【动你标注】** 显式标理由。
- **部署前必须**：① 详细说明本次规则 / 改动；② 把**所有字段全量预览表**发给小爽审（不止部分字段；CSV 宽就拆多表，不以"不好看"砍字段）；③ 小爽放行后才 deploy。
- **新企业入库前**：把该批企业全字段预览表发小爽审，不自己直接入库。
- 不擅自发布（小爽是最终守门人）。
- 主动给判断 + 理由（独立主见），不把球踢回。

### 5.2 与其他 AI / 子智能体
- **研究员（我）**：企业库全字段（含评分 + recommend + description 补全）、信源治理、资讯→企业入库候选发现。
- **标签 AI**：维护 `tag_l1/tag_l2`（我只读不写；merge 保留其字段）。
- **资讯侧 AI**：承担资讯评分 / 聚类等（边界以用户最新指示为准；2026-07-23 明确：评分权重与推荐理由属研究员本侧）。
- **防冲突架构**：所有子智能体只写草稿文件（`scores_draft/*`），主代理单线程 merge 回 `all_enterprises.json`，保留他人字段只 patch 我的键 → 不可能覆盖别人。
- **翻译**：优先脚本 / 批量（库有 `selection/translate.py`；日常流水线用 HY3 批量翻译），**不派子智能体逐家翻译**（贵）。

---

## 6. 文档同步责任（防旧文档误导未来）

- 任何企业库规则变更，必须同步更新：本手册（V 号）+ 若涉及评分则 `RULES.md` + 若涉及标签则 `TAGGING_RULES` + `gen_about.py` / `about.html`。
- `desc_cn` 弃用这一变更，需在 `评分信息补全执行手册_V1` / `PROCESS.md` / `check_single.py` 注释里加标注（它们仍引用 `desc_cn`）。

---

## 7. 资讯更新（每月例行）

**目标**：把每月资讯里关联到、但库里还没有的企业捋出来，按入库流程补进库。

1. 跑 `selection/rebuild_association.py` 关联资讯 ↔ 企业（`related_news_ids`）。
2. 扫当月 `scored_latest.json`，提取实体名不在 `all_enterprises.json` 的企业 → 生成"待入库候选清单"。
3. 对每个候选过【银发信号铁律】：无信号直接删；有信号进入"新增企业入库流程"（§8）。
4. 同步：库内已有企业若有新大事件（融资 / 收购 / 政策），刷新 `funding_latest` / `news_coverage` / `last_news_map`。
- 研究员职责 = 信源猎手 + 守门员（找源 + 健康评级 + 入库候选发现）；打标签归标签 AI。
- 注：日常自动化（采集 + 评分）原每日跑，现按用户要求 PAUSED；月度人工跑这步即可。

---

## 8. 新增企业入库流程（端到端，例：小爽发 100 家名称）

1. **准入**：对每家过【银发信号铁律】；无信号直接丢弃（不进兜底桶）。
2. **去重（全局！）**：对**全库** `all_enterprises.json` 做 exact + fuzzy 比对；已存在 → 合并不新增；新记录必须带 `description`（>20 字）否则拦截（防空壳，见 2026-07-23 去重根因 bug）。
3. **补字段（联网）**：用 WebSearch / WebFetch 补 `description`（中文主字段）/ `name_cn` / `founded` / `stage` / `funding_latest` / `funding_total` / `investors` / 国内竞品 / 官网等。不编；查不到标"待补充"。
   - 海外企业原始英文描述 → 翻译为中文（`description` 主字段要中文可读）：用脚本 / 批量翻译（`selection/translate.py` 思路或 HY3 批量），**不派子智能体**。
4. **写 recommend**：按 `WORKER_PROMPT_V4` + 覆盖四维（信息量 / 差异化 / 可复制 + 信号已在列表），过 `check_single.py` 门禁（R1–R10）；不写公司名、不重复列表字段、不套话。
5. **评分**：新企业按 `selection/signal_strength.py` 算 `signal_strength`（0–10，脚本）；`info_score/diff_score/copy_score` 若无可补则联网评；`research_value` 综合；**已有企业绝不重算**。
6. **草稿隔离**：子智能体只写 `scores_draft/draft_<serial>.json`，主代理 merge。
7. **自测**：`check_single.py` 对新增 recommend；描述层检查（`走查_C` 口径：非模板 / 非过短 / 非英文主导）；serial 唯一。
8. **预览 + 对齐**：把本批企业**全字段**预览表发小爽审（不止部分字段）。
9. **部署**：小爽放行后 → `gen_enterprise.py` + `gen_about.py` + `generator.py` 三生成器 → `deploy_ghpages.py`。部署前说明本次规则 / 改动。
10. **留痕**：commit + 在 `.workbuddy/memory/YYYY-MM-DD.md` 追加；更新本手册若规则有变。

---

## 9. 代码位置地图

| 用途 | 路径 |
|---|---|
| 真相源 | `data/enterprise/all_enterprises.json` |
| 推荐门禁 | `scores_draft/rework/check_single.py` |
| 返工流程 / 写作标准 | `scores_draft/rework/{PROCESS,WORKER_PROMPT_V4,REWORKER_TASK}.md` |
| 评分 + 补全手册 | `评分信息补全执行手册_V1.md` |
| 模板污染清单（只读） | `output/走查_C_信息准确性_2026-07-16.md` |
| 站点规则 | `RULES.md` |
| 评分权威 | `output/评分推荐规则_2026-07-12版.md` |
| 标签体系 | `TAGGING_RULES_v3.md` + `data/enterprise/{tag_synonyms,_l2_l1}.json` + `validate_tags.py` |
| 生成器 | `generator.py` / `gen_enterprise.py` / `gen_about.py` / `ui_common.py` |
| 关联资讯 ↔ 企业 | `selection/rebuild_association.py` / `selection/enrich_entity.py` |
| 翻译（脚本 / 批量） | `selection/translate.py`（+ 日常流水线 HY3 批量） |
| 评分脚本 | `selection/enterprise_score.py`（旧，待退役）/ `selection/signal_strength.py` |
| 部署 | `deploy_ghpages.py` |
| 交接 | `PROJECT_STATUS.md` |
| ⚠️ 非权威并行脚本（待退休） | `data/enterprise/_quality_gate.py` |

---

## 10. 铁律清单（防重复犯已解决的错）

1. 做任何企业库改动前，先读本手册 + PROCESS.md + check_single.py，**绝不自造并行规则 / 门禁**。
2. 模板污染 / 英文描述是 2026-07-16 已知的旧账，按 batch 增量修，**不发起"全量重走查"**。
3. 已有企业评分**禁止重算（只读不写）**；只算新企业。
4. 入库去重必须 **against 全库**；新记录必带 `description`（>20 字）。
5. 无银发信号不入库（银发信号铁律）。
6. 翻译用脚本 / 批量，**不派子智能体逐家**。
7. 部署前发规则说明 + 全字段预览给小爽审；不擅自入库 / 发布。
8. 凡改用户手标内容须【动你标注】。
9. 子智能体只写草稿，主代理单线程 merge，保留他人字段。
10. 文档变更同步本手册（V 号）+ 关联文档，不悄改。

---

## 11. 待办 / 未决

- [ ] 退休 `_quality_gate.py`（移 `_deprecated`），部署门禁改回 `check_single.py` + 描述层检查。
- [ ] 描述补全专项：约 170 处"渠道招商"类模板 + 15 家英文（海外）→ 按 batch 增量修（先发预览给小爽）。
- [ ] 旧文档（手册 V1 / PROCESS.md / check_single.py）标注 `desc_cn` 已弃用。
- [ ] 确认"另一 AI"在资讯侧 / 标签侧的确切边界（用户最新指示）。
- [ ] 该手册随规则变更持续升 V。

---

_本手册 V1 创建于 2026-07-23，依据 PROCESS.md / RULES.md / check_single.py / 评分信息补全执行手册_V1 / 走查_C / PROJECT_STATUS.md 实读 + git 真相核对 + 小爽 2026-07-23 纠偏。后续迭代升 V2…并保留旧版。_
