# 资讯库规则手册（最终版 · 带徒弟式）

> 给接手这份工作的 AI / 徒弟：按 0 → 12 顺序读，每一步都是前一步的输入。所有判断已定稿，执行时严格照此，不要自创维度、不要重造企业库已有的东西。

##0. 这本手册怎么用
- 顺序读：筛选 → 分类 → 信号 → 评分 → 推荐 → 门禁 → 字段 → 展示 → 跳转 → 交接。
- 凡涉及"是否收 / 怎么评"，先走筛选漏斗，再走评分，最后门禁卡质量。
- 本手册与企业库规则 90% 同构；**唯一差异在 §4 信号强度（多事件累加）**。其余直接复用企业库逻辑。
- **总体原则（同步契约）**：企业库后续任何改动（新增事件类型、改标签集、调评分权重/阈值、换前端组件），新闻库**同步跟改**，因为两者共用同一套后端逻辑契约。唯一不跟的点：信号强度里的"多事件累加"是新闻特有（企业库旧信号只看融资），除非企业库也 adoption 多事件信号。其余（四维加权、推荐理由写法、门禁、标签展示、前端组件）一律锁步。

##1. 目标与一句话逻辑
资讯库是给小爽做银发经济选题情报的。一条资讯的生命周期：
**渠道来料 → 一筛(关键词) → 二筛(银发信号铁律) → 信号强度(多事件累加,0–10闸门) → 四维评分(0–100排序/推荐) → 门禁(质量卡) → 前端展示 → 关联企业入库交接**。
三条铁律：无银发信号直接删；重复字段不展示；推荐理由杀模板。

##2. 筛选漏斗（两道，零模型成本优先）
- **一筛 关键词**：行业词初筛（养老/老年/银发/适老/康养/长护险/康复/辅具…），沿用企业库词表。
- **二筛 银发信号铁律**：有银发关键词或业务实质确属银发经济才保留；综合电商 / 通用 AI / 泛医疗 VC / 活动 PR 等无信号即删（不进"行业服务"兜底桶）。
- 过二筛才进入评分。

##3. 分类体系（三轴，互不重复）
我们刻意让三个轴正交，避免字段重复——这是上一版被指出事件筛选和横切标签大量重叠的根因，本版已根治；同时按用户意见**砍掉独立的"银发关联"轴**（两道筛选漏斗已经保证全是银发内容，再分核心/相关/背景当筛选纯属重复）。
- **event_type（纵向·发生了什么）**：融资 / IPO / 收购并购 / 政策法规 / 产品发布 / 行业趋势 / 合作 / 市场调研 / 其他。
  - 行业趋势 = 深度报告 + 行业洞见 + 行业图谱 **合并**（优先级不高，不分细）。
  - 市场调研 / 行业榜单 / 消费洞察 等需求侧信号归入"市场调研"，+1（企业库无对应静态属性，故单列）。
  - 人事变动 → 归入"其他"（除非标志性人物，极少见）。
  - **不单独设"海外案例"**——海外用 region 区分。
- **domains（横向·哪个领域）**：直接复用企业库二级标签（109 个 L2），不重造。
- **region（地区）**：全部 / 国内 / 海外（默认全部）。

> 为什么只有三轴：event_type 答"什么事"，domains 答"哪个赛道"，region 答"哪里的"。三个轴各管一维，前端筛选互不重叠，**不再有第二个"资本/政策/技术/银发关联"标签层**。横向维度严格复用企业库 L2，绝不另起一套标签。

##4. 信号强度 signal_strength（0–10，闸门 + 多事件累加）
- 它是资讯**自己的复合事件信号维度**，**不对应企业库的"企业规模"**——企业库旧信号只看融资/IPO/MA，我们扩展成了多事件。评分/推荐时把它当一个整体维度走，不必纠结它内部是哪种事件（按用户意见：不管具体是什么，按信号强度走）。
- 起点（信源等级微弱起点，沿用企业库）：T1 0.5 / T2 0.3 / T3 0.1。相关性已由二筛过滤，信源仅微弱起点。
- 累加项（同一条资讯命中多个事件，分数相加，封顶 10）：

| 事件 | 加分 |
|---|---|
| IPO / 上市 | +6 |
| 融资（海外 ≥$50M +5 / 国内 ≥¥1亿 +4 / 其他 +3） | 金额分级 |
| 收购并购（大体量 +5 / 中小 +3） | 金额分级 |
| 政策法规 / 支付方（长护险等） | +3 |
| 产品发布 | +2 |
| 行业趋势 / 合作 / 市场调研 | +1 |

- recency（时效，含在信号内）：近1周 2.5 / 近1月 2.0 / 近2月 1.5 / 近半年 1.0 / 近1年 0.5。
- **国内加成（代码属实）**：融资分级表里，同档金额下国内(CNY)比海外(USD)高 1 分（如 1亿：海外5/国内6）。这是信号强度内部的正常倾斜，**不另设"海外镜鉴"加分**（海外靠 region 自己选）。
- **闸门**：signal_strength ≥ 5.0 视为过闸（is_selected），进入前端；其余归档。

##5. 四维评分 final_score（0–100，同构企业库 research_value）
> **关键澄清（用户点名追问）**：企业库 `research_value` = `base_value(0–70, 由 V1–V4 四个维度相加)` **+** `event_boost` **+** `mirror_bonus` **+** `evidence_depth`，四者**相加后 min(100)**（代码 `rv = min(100, base + boost + mirror + depth)`）。**event_boost / mirror_bonus / evidence_depth 不属于那"四个维度"——它们是叠在四维之上的独立附加层。**
> 新闻库**只复用四维结构，不搬这三层**，原因：
> - `event_boost`（企业近期有融资/IPO/MA 事件→加分）：新闻本身就是"事件"，信号强度已编码事件重要性，无需再给新闻"加成"。
> - `mirror_bonus`（仅海外企业 +5 的"以海外为镜"）：用户明确新闻不另设海外加成，海外靠 region 筛选自己选；新闻信号强度只在融资分级表里对国内(CNY)比海外(USD)同档高 1 分（代码属实），不再额外加海外镜鉴分。
> - `evidence_depth`（0–12，按资料厚度破并列）：企业库用它破同分并列；新闻用**时间**破并列（越新越前），不需要厚度维度。

| 维度 | 取法 | 对应企业库 |
|---|---|---|
| 信号强度分量 | §4 的 0–10 缩放 | 资讯自有 V1（**不映射企业规模**，是资讯自己的复合事件维度） |
| 信息量 | 谷歌搜实体名前几页看密度：>20 条相关深度 9–10 / 5–20 条 6–8 / 1–5 条 3–5 / 孤例 1–2 | V2 信息丰富度 |
| 差异化可复制 | 模式是否新颖、国内能否借鉴 | V3 模式差异化 |
| 国内可借鉴 | 对国内从业者的参照价值 | V4 标杆可借鉴 |

- 四维加权：信号强度 0.3 / 信息量 0.3 / 差异化 0.2 / 国内可借鉴 0.2 → ×10 取整（与企业库权重结构同构，把"规模"换成"信号强度"，"可写性/时效"并入信息量）。
- 分级：S(≥80) / A(≥60) / B(≥40) / C(<40)。worth_deep_write 四标准同企业库。
- **不设独立"时效性"维度**：已含于 §4 recency；新闻按时间排，用户近两周 + 评分看即可。
- **并列破 tie**：final_score 相同 → 按 date 新→旧。

##6. 推荐理由 recommendation（杀模板）
- 强模逐条写，结构 = 四维解释：①信号强度（高/低，主导事件若明显可点一句）②信息量（素材是否够写成文）③差异化（模式新在哪）④国内可借鉴（国内同行怎么参照）。
- 门禁：禁"国内可参照其融资节奏"式模板拼接；禁重复卡片已有字段（标题/领域/信号值）；长度 ≥ 40 字；非空。
- **真实坏范例**（现有 scored_latest.json，id=162《阿里腾讯押注10亿！估值飙至70亿的"通用机器人"，会是未来养老的答案吗？》）：
  `宏观趋势数据中的微观机会点，值得拆解为具体选题方向。` ← 只换赛道词、无实体、无角度，纯模板（同模板出现 5 次）。
- **真实好范例**（改写，模仿企业库 `recommend` 字段写法：点名实体 + 给角度 + 指可借鉴 + 提风险）：
  `阿里、腾讯下注的通用机器人估值冲到70亿，但养老刚需在跌倒监护、移位搬运而非"通用"。写作可抓"资本热≠场景准"的错位：国内宇树、智元已卷通用人形，银发落点却在康复外骨骼(傅利叶)与看护机械臂。风险在C端付费弱、机构采购周期长；可复制的是"机器人+养老渠道"的绑定打法，而非技术本身。`
- **企业库 `recommend` 真字段参照**（#0036 迈步）：`资本连续押注养老康复(联想四轮、尚元入股)，自研柔性驱动外骨骼获二类证、专利两百多件，估值约10-15亿。差异在把硬件与训练方案打包进养老院渠道；傅利叶、程天已同赛道，可学其临床绑定与注册证打法。风险在C端支付。` ← 注意：企业库字段叫 `recommend`（新闻叫 `recommendation`，字段名不同，但**写法同款**：具体、点名、给角度、讲可复制与风险）。

##7. 门禁与校验（质量卡）
- recommendation 非空 / 非模板黑名单短语 / ≥ 40 字 / 未重复 amount-date-source。
- summary_cn 非空、非标题切片（必须是真摘要）。
- entity_name 非空（报道企业的资讯）；`银发关联`字段已砍，不再校验。
- signal_strength / final_score 合法数值。

##8. 字段规范（去重 + 关联）
- **废弃**：单字 `domain`（并入 `domains`）、旧 `signal`（用 `signal_strength`）；`signal_score` 作 `signal_strength` 别名保留。
- **已废弃**：`is_curated` / `is_selected` / `view` 字段在前端的视图区分用途（已删精选/全量视图，所有资讯统一展示；这些字段可保留在 JSON 中作历史标记，但前端不再使用）。
- **新增关联字段（只加一个，杜绝重复）**：`entity_serial`（反查企业库 #xxxx，打通两库、支撑资讯→企业库跳转）。
  - **不新增** `silver_relevance` / `silver_reason`（两道筛选已保证全是银发内容，再存"核心/相关/背景"纯属重复，用户已砍掉该维度）；**不新增** `topic_angle` / `cn_takeaway`（这两块内容本就写在 `recommendation` 四维度里，单列即重复）。
- 列表展示字段不重复：event_type / domains / region 各出现一次（无银发关联轴）。

##9. 前端展示（与企业库保持同款 · 已改完）
> **同步契约见 §0**。以下为**已落地状态**（generator.py 已改、output/index.html 已重生）。

| 项 | 状态 |
|---|---|
| 默认排序 | 时间↓，选项 评分↓/时间↓/融资金额↓ |
| 评分展示 | 单总评分数字(final_score取整) + 悬停浮层四维拆解 |
| 标签 | **一行合并**：event_type + domains(L2最多2个) + tags(最多3个)，置于标题上方 |
| 关联企业 | 有 entity_serial 时显示 chip → 企业库列表(新标签) |
| 融资金额排序 | 有（资本事件按金额降序，缺失排末尾） |
| 收藏 | 复用企业库组件 ✓ |
| 地区筛选 | 全部/国内/海外，**放在时间行末尾**（不独占顶栏） |
| 时间筛选 | 全部/近1周/近2周/近1月/近3月/**近半年**/**近一年** |
| 视图 | **单一列表**（删掉"精选/全量"tabs，不再区分 curated/all 视图） |
| 垃圾标签 | **已删除**"主体:xxx"和"同主题"（不再出现在卡片底部） |

- **统一筛选交互**：事件类型 chips + 标签行(domains+tags合并) **全部平铺可见**（不藏下拉）+ 排序 + 时间(含地区)。一套交互。
- **卡片结构**（从上到下）：日期+来源 → 标签行(event_type+domains+tags+关联企业chip) → 标题 → 摘要 → 推荐理由(★) → 评分(悬停)+收藏 → 操作按钮(备注/已读)。**无底部垃圾标签**。
- **整卡可点** → 跳资讯详情页；不单独放"读解读"按钮（整卡即跳转）。
- 排序含融资金额（资本事件按金额降序）。

##10. 关联跳转
- 企业库 → 资讯：跳资讯详情页。
- 资讯 → 企业库：跳企业库列表页（带筛选），新标签。
- 资讯 → 资讯：整卡跳本库资讯详情。

##11. 协作与交接（两个 AI + 本地文件，无自动化）
角色：
- **我（存量 + 规则 owner）**：63 条补全、本手册、前端、门禁；处理渠道 AI 发来的初步筛选资讯（**抽查校验后入库，不重筛**）。
- **渠道 AI（找渠道录新资讯）**：找渠道、录新资讯、大模型预筛，发初步结果给我；与我共享本地盘。
- **企业库入库 AI**：**直接看新闻库前端列表页（已筛选展示）自行挑企业入库，我无需专门交接文件**（用户明确：不要给入库 AI 专门交接，它自己看前端即可）。
- **同步契约（重要）**：企业库规则/前端若后续调整，新闻库须**相应同步调整**（见 §0/§9 总体原则），保持两库逻辑锁步。
- **不建跨账号自动化**：文件都在本地，任何 AI 可接手；月度"待入库企业"由用户拿前端列表手动交给入库 AI，不写自动化。

##12. 速查表
- 事件类型 ↔ 信号强度分值：见 §4。
- 四维 ↔ 企业库：见 §5。
- 门禁清单：见 §7。
