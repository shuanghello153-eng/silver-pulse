# 资讯库 · 评分与推荐方案（2026-07-22）

> 这份只解决两件没定的事：**综合分怎么算**、**推荐理由怎么写**。UI/UX、删"银发相关性"字段、砍四维深度评分——都已确认，不在这里重复。
> 所有英文字段名后面都带中文，方便你审。

---

## 〇、名词对照（English → 中文，以后我都这样标）

| 英文字段 | 中文 | 谁算 | 花不花积分 |
|---|---|---|---|
| `signal_strength` | 信号强度分 | 脚本 | 免费 |
| `value_score` | 价值分（原来我瞎写的 editorial_value，就是这个） | AI 二筛顺手给 1 个数 | 极省（本来就要读正文） |
| `heat` | 热度（同一件事有几个来源报道） | 脚本 | 免费 |
| `recency` | 时效（发表时间新旧） | 脚本 | 免费 |
| `composite` | 综合分（最后展示、用来排序和自清的那个总分） | 脚本合成 | 免费 |
| `summary_cn` | 中文摘要 | AI 二筛顺手写 | 极省 |
| `recommendation` | 推荐理由 | AI 二筛顺手写 | 极省 |
| `entity_serial` | 关联企业编号（链到企业库那家公司） | 脚本匹配 | 免费 |

**一句话原则**：整条流水线，**AI 只在"二筛读正文"这一次调用里，顺手吐 3 样东西——摘要、价值分、推荐理由**。其余全是脚本。这就是省积分的根。

---

## 一、先回答你的核心疑问

### Q1：AI 打分是随便打吗？按什么打？
不是随便打。AI 只打**一个数**：`value_score` 价值分（0-10），而且给它**明确的锚点标尺**（见第三节表格），不是"你看着给"。它只判断脚本判断不了的东西——**这条内容多有嚼头、国内能不能学**，不判断"事件多大"（那是信号强度，脚本已经算了）。

### Q2：为什么不能两个分直接加权平均？（你举的 IPO 例子）
你说得完全对。如果 `综合分 = 0.5×信号 + 0.5×价值`，一个 IPO 信号=10、价值=5，就只有 7.5，甚至更低——**大事件被拖下来了**。所以我**不用平均**，改用"**取高**"（见第四节）：哪个维度强就以哪个为准，另一个只做微调。这样 IPO 信号=10 就稳稳在高位，不会被价值分拖累。

### Q3：信号强度到底还要不要脚本算？还是让 AI 综合看？
**要脚本算，不能全交给 AI。** 理由：AI 全权打分正是你担心的"随便打"；脚本信号强度是客观锚（同样的 IPO 永远同样的分），把 AI 的主观分锚住。**分工**：脚本管"事件多大/多热/多新"（客观、免费），AI 管"内容多值得写"（主观、省着用）。

### Q4：时效（发表时间）要不要算进分？
要，而且脚本就能算（第四节 `recency`）。越旧的资讯对"现在要不要跟"价值越低，轻度衰减即可（不是硬删，还有时间筛选兜底）。

### Q5：一件事多个来源报道，算加分吗？
算。你说得对——多源=热度高。我们本来就在做"同事件聚类"（把同一件事的多个来源合并成一张卡、其他链接放卡片下面），**顺手数一下有几个来源**就是 `heat` 热度分，免费。

### Q6：摘要能不能用脚本/RSS 提取？（我做了实验）
**不能。** 我把今天新抓的 24 条的 RSS 自带摘要全调出来看了：**22 条就是"标题+来源名"重复**（AgeClub、中国老龄协会、Home Health Care News 全这样），2 条稍长也只是把标题换句话说。结论：**RSS 摘要没有信息量，等于没写**。所以摘要必须模型写——但用便宜快模型（HY3 免费到 8 月初，之后 DeepSeek V3.2），不用强推理模型，且和二筛合并成一次调用，成本压到最低。

### Q7：RSS 和非 RSS 要不要分开处理？
**不分。** 你说得对，我上一版想复杂了。所有资讯都一样：**抓正文 → 二筛（模型读正文，一次性产出 摘要+价值分+推荐理由）→ 脚本合成综合分**。RSS 只是"发现入口"，内容一律回原文抓。唯一例外是飞书供需表这种**本身就是结构化字段**的（公司/需求/赛道齐全），可直接用字段拼摘要，不必抓网页——但这属于数据形态差异，不是 RSS/非RSS 的区别。

---

## 二、资讯 vs 企业库：到底哪里一样、哪里不一样

| | 企业库 | 资讯库 |
|---|---|---|
| 研究对象 | 一家公司（稳定几年） | 一条新闻（发生一次、会过期） |
| 推荐理由本质 | 分析的**成果**（四维融合成 80-200 字人话） | 一句话**提示**（≤60 字，为什么值得看一眼） |
| 评分 | 四维精评（信号/信息量/差异化/可复制）→ research_value | **信号（脚本）+ 价值（AI 1 个数）+ 热度/时效（脚本）→ 综合分** |
| AI 调用量 | 每家联网精评 1-3 次搜索 | 每条 1 次（二筛顺手） |
| 北极星 | 国内创业者能学到什么 | **一样**——国内创业者能学到什么 |

**相同**：筛选北极星（国内可借鉴）、反模板门禁纪律、差异化测试。
**不同**：资讯不做深度四维、推荐是"一句话提示"不是"一段分析"、绝大部分靠脚本。

**我确实读了企业库的门禁规则**（`scores_draft/run_v2/WORKER_BRIEF_V3.md`），它的精华我直接继承到资讯（见第五节黑名单和差异化测试），只是把"80-200 字四维融合"压缩成"≤60 字一句话"。

---

## 三、价值分 value_score 的评分标尺（AI 唯一要打的分，0-10）

**AI 只判断"内容嚼头 + 国内可借鉴 + 叙事空间"，明确排除"事件大小/金额/信源权威"（那些归信号强度，不重复算）。**

| 分 | 含义 |
|---|---|
| 9-10 | 反常识/模式创新，能直接对标国内做一篇深度选题（如"保险+医疗闭环暴雷") |
| 7-8 | 有明确差异点或清晰的国内借鉴角度，可写 |
| 5-6 | 有信息但偏常规、区域性、单点，写起来平淡 |
| 3-4 | 弱信号、趋势综述、无具体可学的点 |
| 1-2 | 擦边银发、几乎没有独立选题价值 |

**打分铁律**（继承企业库）：
1. 排除事件大小/金额（那是信号强度）；只看"内容值不值得写"。
2. **差异化测试**：想象同类另外两条资讯，你的价值分理由能不能区分它们？不能就说明打得糊。

---

## 四、综合分 composite 怎么算（脚本合成，免费，可解释）

分四步，全脚本：

```
第1步  信号分 signal   = 事件类型基准 + 金额加成         （脚本 0-10）
       事件基准：IPO 8 / 大额融资(≥亿) 8 / 收购并购 7 / 重大政策 7
                / 一般融资 6 / 产品发布 5 / 行业趋势 4 / 其他 3
       信源微调：T1 +0.3 / T2 0 / T3 -0.3

第2步  价值分 value    = AI 二筛给的那个数                （AI 0-10）

第3步  主分 core       = 0.7 × max(signal, value)
                       + 0.3 × min(signal, value)
       —— 关键：用「取高」不用「平均」。哪个维度强就按哪个，
          另一个只做 30% 微调。这样 IPO(信号10) 不会被拖下来。

第4步  综合分 composite = clamp( core + 热度加成 - 时效衰减 , 0, 10 )
       热度 heat：同事件来源数  2个 +0.5 / 3-4个 +1.0 / ≥5个 +1.5
       时效 recency：≤7天 -0 / 8-30天 -0.3 / 31-90天 -0.8 / >90天 -1.5
```

**举例验证（证明没有 IPO=5 的问题）：**
- 同仁堂医养 IPO：信号 8、价值 7 → core=0.7×8+0.3×7=7.7，近期+热度 → **≈8.2**，高位 ✓
- 某小公司反常识模式：信号 3、价值 9 → core=0.7×9+0.3×3=7.2 → **≈7** 也能冒头 ✓
- 常规区域快讯：信号 4、价值 4 → **≈4** 沉底 ✓

**展示**：卡片主数字显示 `综合分 8.2`，下面小字露出因子 `信号8 · 价值7 · 3源`，不做黑箱。

**长期自清**：`综合分 < 4` 标记为可删/归档候选，时间一长低价值自动沉底——正是你要的"分数能代表价值就能删"。

---

## 五、推荐理由 recommendation 到底是什么、怎么写

### 是什么（一句话说清）
推荐理由 = **一句话，指出这条资讯里"最值得你看一眼的那个具体点"**——一个具体结论、一个动作指向、或一个反差。
它**不是**选题角度、**不是**四段式、**不是**风险分析。就一句。

### 怎么写
- 长度 ≤60 字，1 句。
- 句式：**什么变了 → 谁该关心 / 能学什么**。
- **必须含一个具体结论或动作指向**。

### 铁律黑名单（继承企业库，出现即不合格）
禁：值得关注 / 有标杆意义 / 是重要依据 / 提供参照 / 直接依据 / 切入X赛道 / 结合本地资源 / 部分环节可借鉴 / 有一定差异点/亮点。
**差异化测试**：想象同类另两条，你的推荐读起来能不能区分？不能就重写。

### 好 vs 坏
- ✅ "山东把长护险覆盖率目标提到 70%，支付端扩容，直接利好上门护理创业。"
- ✅ "同仁堂把中药医养资产单独送港交所，国内老字号'康养分拆上市'第一个可拆解的样本。"
- ❌ "对国内中药系康养证券化有标杆意义。"（虚空，没说结论）
- ❌ "政策全文首次披露，是判断县域养老机会的直接依据。"（只说"有依据"，不给依据是什么）

### 写多少、按什么顺序（回答你）
- **前期全写**——因为二筛读正文时顺手就写了，边际成本极低。
- **优先级从综合分高→低**（算力有限时先保高分条目的质量）。
- **显示门槛前期设低（全显示）**：你先看整体；若发现一堆低价值的也写了推荐、显得吵，我再把"显示推荐"的门槛往上抬（例如只在 综合分≥6 显示）。**低起步、易上抬**，正是你说的考量。

---

## 六、校验闸门 validate_news.py 要改成（回答"为什么必填整数"）

字段为什么必填：**因为综合分要靠它算、卡片要靠它排序**，缺了这条就没法排、没法自清。改动：
- 删 `silver_relevance` 银发相关性（二筛已是准入闸门，冗余）。
- `summary_cn` 中文摘要：必填、20-200 字、禁出版方套话（据悉/本文/聚焦）、禁虚空词。
- `value_score` 价值分：必填、1-10 整数。
- `signal_strength` 信号强度：脚本产出、0-10。
- `composite` 综合分：脚本算、0-10。
- `recommendation` 推荐理由：前期全填；≤60 字；禁黑名单套话。
- `entity_serial` 关联企业编号：有则校验格式 `#xxxx`。

---

## 七、需要你拍板的（就 2 个，其余我定了）

1. **主分权重 0.7/0.3 取高**——你认不认这个"取高不取平均"的思路？（这是解决 IPO 被拖累的关键）
2. **自清删档线**：`综合分 < 4` 还是 `< 5` 标可删？（我倾向 <4，先松后紧）

你点头这两个，我就：改 `validate_news.py` → 写评分脚本（信号/热度/时效脚本部分）→ 二筛管道（摘要+价值分+推荐一次调用）跑存量 62 条 + 新抓 24 条 → 校验 → 一次性上线（企业库零回归）。
