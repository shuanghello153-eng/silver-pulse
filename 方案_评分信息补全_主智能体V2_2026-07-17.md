# 方案 · 企业库全量评分推荐 + 信息补全 + 标签审查（主智能体 V2 · 2026-07-17）

> 状态：**待小爽确认，未动任何数据、未部署。**
> 配套依据：文档1《评分推荐规则 2026-07-12 版》、文档2《TAGGING_RULES_V20》（仅供参考）、前车产物（apply_merge.py / gen_excel_final.py / new30 数据包）。
> 本方案已结合"底层代码实读 + 真实数据探查 + 前车崩溃教训"重写，旧版方案_评分与信息补全_2026-07-16.md 保留对照。

---

## 〇、现状盘点（起点，避免重复前车错误）

我实读了代码与数据，确认了 4 件事，作为方案起点：

1. **真相源 = `data/enterprise/all_enterprises.json`（1502 家）**，实时最新（打标 AI、信源 AI 都直接写它）。文档1 写 1328 家已过时——信源治理又入库了一批。
2. **"文档1 评分没实现"的冲突已被前车解决**：他的 `apply_merge.py` 里 `research_value = (信号×0.3+信息量×0.3+差异化×0.2+可复制×0.2)×10` 与文档1 四阶段公式**完全一致**。所以本任务基准 = 文档1 公式（代码已实现），我**不重造评分引擎**。
3. **前车已落下两批半成品**：
   - **pilot 25 家**：评分+补全**已合并进 all_enterprises.json**（含 payor_model、纠正 funding、desc_cn 等）。
   - **new30（30 家）**：数据已抓（`new30_packets.json`），research/rec 子智能体**只跑了一部分就崩溃**，分**未合并**。
4. **崩溃根因 = 并发过载**：一次性派了 `research_part0~5`（6个）+ `rec` + `validator` 等一大堆子智能体 → 软件卡死、模型调用失效。→ 本方案**硬卡并发上限**。

---

## 一、项目背景理解

### 1.1 这个库是什么、为谁服务
Silver Pulse 是小爽的「银发经济选题情报台」。她早上打开网站，要从 **1502 家企业**里一眼挑出"值得写、且能借鉴到中国"的标的。企业卡片上那行**推荐理由 + 研究价值分**，是她最先看的东西，直接决定"这家我写不写"。

### 1.2 这次任务的三个动作，本质是"同一遍工作"
1. **评分 + 推荐理由（按文档1）**：给每家企业算"值不值得研究"的分，并写出一段"为什么值得写、对中国有什么借鉴"的自然语言理由（**每家 3 个版本**，供你挑）。
2. **信息校验 + 字段补全**：评分过程必须"对企业做全面信息搜索"——搜出来的事实正好用来**补全缺失字段**（中文名、成立年、融资、投资方、简介、亮点、新闻覆盖等）。
3. **标签审查（只读、不改）**：标签由另一个 AI 专职维护；我只产出一张"建议新增/删除/优化"的表交你拍板，绝不静默改。

三者关联：评分的"信息量/差异化/可复制"三维**必须联网搜过**才有依据 → 顺手把字段补全；补全后的字段又让下一轮评分更准。是同一遍闭环，不是三件独立事。

### 1.3 "做对"的验收标准
- 分数和理由能让小爽一眼判断"这家我写不写"；
- 字段真实、可追溯（每条关键数据有来源链接，不编）；
- 不和其他正在跑的 AI / 未部署代码冲突；
- 全量 **不漏一家、不幻觉一家**。

---

## 二、详细执行方案

### 2.1 评分流程（四阶段，落地版）

**阶段1-2（准入）**：企业库已先验收入库（含银发信号铁律过滤），跳过资讯初筛；企业侧只做"银发信号"复核——发现非银发企业标 `silver_verdict`（核心银发/泛医疗擦边/非银发），**不删库**（删是另一个 AI 的活），只列审查表。

**阶段3 信号强度（脚本，0~10，确定性、零 AI）**：
- 读企业自有的 `funding_latest`（融资/收购）+ `funding_total` + 上市标志 + 关联资讯日期。
- 规则（对齐 `selection/signal_strength.py`）：IPO/上市 +6；融资按海外美元/国内元双阈值表分级；收购按金额分级；时效按最新事件日期动态算（近1周2.5/近1月2/近2月1.5/近半年1/近1年0.5/更久0.25）。
- 合计 clamp 0~10。**主智能体一次性算完全部 1502 家**（前车已算好 `all_signals.json`，我复用+复核），不联网、可重复。

**阶段4 AI 四维（联网，0~10，本任务核心）**：每个 worker 子智能体对一家企业：
1. **联网搜前几页**：官网/年报/招股书/Crunchbase/新闻，评估三维——
   - **信息量**：资料多寡、是否上市/热点（好不好写）；
   - **差异化**：商业模式反不反共识、起落是否剧烈；
   - **可复制**：国内有无同类赛道、有无上亿融资/上市公司、支付方差异（Medicare/自费 vs 医保/自费）→ 空白还是红海。
2. **综合分** `research_value = round((信号×0.3 + 信息量×0.3 + 差异化×0.2 + 可复制×0.2) × 10)`，0~100。
3. **推荐理由（3 个版本）**：见 2.1.4 硬规矩。

**落库**：四维分 + `research_value` + `recommend`（字典含 v1/v2/v3）+ `update_time`（ISO 日期）写入各企业。

#### 2.1.4 推荐理由三版本的硬规矩（你最在意的质量点，写进 worker 模板）
- **(a) 言之有物**：每个版本都把"信号强度/信息量/差异化/可复制"四维判断融进去讲"为什么值得写、从中国什么角度切入"，不空喊"值得关注"。
- **(b) 避免重复卡片字段**：凡卡片/列表已展示的结构化信息（融资金额、轮次、标题、摘要、来源、日期、官网）→ **不重复陈述**；若对判断关键，只作论据点到为止。
- **(c) 简洁不啰嗦**：一顿废话不带，一段自然流畅的话。
- **三版本差异定位**（避免"同一段改三个说法"）：
  - v1 = **创业者视角**（最强调"国内能抄/不能抄、卡点在哪"）；
  - v2 = **反常识/故事性视角**（最强调差异化、起落、为什么和直觉相反）；
  - v3 = **热点/时效视角**（最强调近期事件、为什么是现在、信息密度够不够写）。
  - 三版共享事实底座，但切入角度不同，供你挑。

### 2.2 信息校验策略（边搜边校验）
- 每家企业联网搜到的关键事实（成立年、融资、投资方、官网、中文名、简介）**与库内逐条比对**：
  - 库内空 → 填；库内错（如 `funding_latest.amount` 被错填成"投资方名"）→ 纠；
  - 库内与权威源冲突 → 以权威源为准，附来源 URL。
- **不编**：搜不到标"待补充"，不填假数（铁律）。
- 证据留存：每条补全附来源 URL，进 `news_coverage.latest_news` 或 `deep_article_links`（深度文，0~2 条）。

### 2.3 字段补全计划（我的独立意见 + 处置）
| 字段 | 处置 | 理由（我的判断） |
|---|---|---|
| signal_strength / info_score / diff_score / copy_score / research_value | **写** | 本轮核心交付 |
| recommend（含 v1/v2/v3）/ update_time | **写** | 本轮核心交付 |
| desc_cn / founded / stage / name_cn / funding_latest / funding_total / investors / highlights | **补/纠** | 评分直接产物，边际成本低、产出高 |
| news_coverage | **追加不覆盖** | 另一流程负责；我只追加发现的新讯，不动他人爬虫数据 |
| value_score | **桥接写** = research_value | 旧前端回退兼容，防崩 |
| tag_l1 / tag_l2 | **只读不写** | 另一 AI 维护；错标只列审查表 |
| business_tags（customer/role/channel） | **本轮只读；建议列审查表**（详见 2.4） | 标签审查范围，不直接改 |
| payor_model | **待你确认**（见疑问 Q2） | 前车为 pilot25 写了；字段字典标"别人字段不动"，我倾向继续写（对"可复制"维度极关键），需你点头 |
| business_model / business_model_cn | **废弃，不重写** | 见 2.4 我的意见 |
| category_l1 / category_l2 / tags | **废弃，不碰** | 旧体系，前端已不读 |
| source / region / serial / name / ingest_time / needs_review | **不碰** | 他人字段/主键 |

### 2.4 标签审查方案（只读、不改、出表）
输出一张审查总表（企业 + 建议 + 证据 + 是否标【动你标注】），**绝不写回**。覆盖四类：

- **business_model 自由文本（我的意见：废弃，不补充）**：文档1 明标已废弃；V20 标签系统已完全替代其职能；你之前也指出"大量重复、没辨识度、没价值"。重写 1210 条 = 黑洞成本、零产出。**结论：留旧值当 legacy，不碰。** 若评分中发现它误导，列入审查表。
- **business_tags.role（我的意见：有价值，应补，但本轮只建议不改）**：它是结构化字段（对应 V20"角色"维度），目前约 177 家缺失。role 缺失会削弱"可复制/国内对标"判断。**我倾向：本轮把它列入"建议补充"审查表，不静默写**（因你明确"标签审查不直接修改"）；若你授权，后续批次可直接补并标【动你标注】。
- **tag_l1 / tag_l2（另一 AI 维护）**：只读；发现明显错标（如保健品桶里混进 Oak Street Health 类）标【动你标注】+ 理由交你。
- **category_l1 / category_l2（已弃用）**：确认弃用，本轮不维护；如要低成本兼容，未来可从 tag 派生镜像（零成本），现在不做。

### 2.5 并行子智能体架构（波次制 + 硬卡并发，避前车卡死）
- **分片**：1502 家按信号强度降序排队；先 Tier1（高信号≈300-500 家，质量优先），后 Tier3（长尾≈1000 家，轻量补全）。
- **波次并发上限（硬规则）**：每波 **最多 3 个研究 worker + 3 个 validator = 6 并发子智能体**；30 家对齐批次更保守用 **2 worker + 2 validator = 4 并发**。杜绝前车"一把梭几十个"的卡死。
- **草稿隔离（防冲突核心）**：worker 只写独立草稿文件（`scores_draft/draft_<serial>.json` 或按 batch），**绝不碰 all_enterprises.json 本体**；主智能体每波单线程 merge。
- **validator 独立**：每批配 1 个独立 validator 子智能体，专门挑刺（幻觉/格式/理由合规），与 worker 无共享上下文。

### 2.6 校验与"无遗漏"四道防线（你最核心的关切）
1. **manifest 全覆盖**：开工建 1502（或本批）serial 清单，每批打勾，结束核"已处理 == 总数、无遗漏、无重复"。
2. **双重校验**：worker 自校验 → validator 独立复核 → 主智能体全局复扫（schema/量纲/范围/算术自检）→ 你终审。
3. **不并发写本体**：所有子智能体只写草稿，主智能体单线程 merge，根除"两个 AI 同时写大文件互相覆盖"。
4. **可断点续跑**：每波落盘即 checkpoint（前车 `apply_merge.py` 已支持 before/after 快照），某批崩了只重跑该批。
- 主智能体（我）终审动作：重读 all_enterprises.json 最新版 → 只 patch 我的键 → 跑全局复扫 → 抽审每批 3~5 家 → 出 diff 报告 + 标签审查总表 + 未达标清单 → 交你。

### 2.7 冲突规避（和打标/信源/UI/前车已写数据）
- **打标 AI（tag_l1/l2）**：绝对不碰写入，只产建议表。
- **信源 AI（加企业）**：不删不加企业；merge 时保留其新增记录。
- **UI AI（gen_enterprise.py）**：当前有未提交改动；我**本轮绝不重新生成 HTML 或部署**，产物落数据 + 本地 Excel，等你和 UI 编辑都确认后再统一部署。
- **前车已写数据**：pilot25 已合并的评分，我**不覆盖**（除非你要求重跑）；new30 我接着他的数据包完成并合并。

### 2.8 30 家对齐批次的具体做法（本次先交付这个）
目标：用一批高质量、带 3 版本理由的 30 家，让你"对齐"质量与格式，再铺开全量。
- **范围**：复用前车崩溃前挑好的 `new30_serials.json`（30 家，信号强、银发相关），**接着他的 `new30_packets.json` 数据继续**，不重抓全量，省成本也避卡死。
- **流程**：
  1. 主智能体复核 `all_signals.json` 里这 30 家的信号分（前车已修过 bug，复用）；
  2. 派 **2 个研究 worker**（各 15 家）联网核实+产出四维分+3 版理由+字段纠正；**2 个 validator** 独立复核；
  3. 主智能体 merge（只写我的键）→ 全局复扫 → 抽审；
  4. 复用前车 `gen_excel_final.py` 改造成你要的 2 表（+ 标签审查表）：
     - **Sheet1 前后对比**：编号/名称/银发判定/信号前→后/研究价值前→后/信息量/差异化/可复制/一句话定位(后)/推荐理由 v1(后)/v2(后)/v3(后)——**描述全列、不省略**；
     - **Sheet2 全字段**：这 30 家**数据库里所有真实字段**逐列列出；
     - **Sheet3 标签审查**：business_model/role/tag_l1/l2 的建议新增/删除/优化表。
- **交付物**：更新后的 `all_enterprises.json`（仅这 30 家 patch 我的键）+ `SilverPulse_评分信息补全_30家_对齐版.xlsx` + 标签审查表 + diff 报告。

### 2.9 全量铺开（对齐确认后）
- 按 2.5 波次制，先 Tier1（≈300-500 家高分）全量联网打分+补全+标签审查，再 Tier3（≈1000 家长尾）轻量补全+脚本信号分+模板化理由。
- HY3 免费期至 2026-07-22；若做不完，07-22 后切低成本模型续跑。
- 每批都走 2.6 四道防线；周期性 checkpoint 交你抽审。

---

## 三、我对几个关键问题的独立判断（先给看法，你来拍板）

- **business_model 自由文本**：**废弃，不重写 1210 条**。成本极高、产出极低，标签系统已替代。留 legacy。
- **business_tags.role**：**有价值，应补**，但本轮作"建议"列审查表（尊重你"标签不直接改"）；若授权，后续直接补并标【动你标注】。
- **"一级/二级分类弃用"**：我理解为 **`category_l1/l2`（旧选题库分类）弃用**；`tag_l1/l2` 是另一 AI 在维护的活跃标签，本轮只读审查。请确认此理解。
- **payor_model**：前车为 pilot25 写了；它对"可复制/国内借鉴"维度极关键。我**倾向继续写**，但字段字典标它"别人字段不动"——需你确认。
- **recommend 存储**：沿用前车结构 `recommend = {rec_v1, rec_v2, rec_v3, 四维分, signal, research_value}`（字典）。前端冻结期不影响；未来前端取 `rec_v1` 作展示串。
- **并发上限**：硬卡 6（全量）/ 4（对齐批），是前车崩溃的直接教训，也是"并行但不卡死"的平衡点。

---

## 四、需要你确认的疑问（阻塞项，回复后我开工）

**Q1 · 30 家对齐批次的范围**
前车崩溃前挑好的 `new30_serials.json`（30 家，信号强）数据已抓。我建议**接着他的 new30 完成**（不重抓、省成本、正好是"他好像跑完了"那批）。还是你想让我换一批**全新的 30 家**来对齐？

**Q2 · payor_model 写不写**
前车为 pilot25 写了 payor_model；字段字典标它"别人字段不动"。我倾向**继续写**（对可复制维度关键）。你同意我写这 30 家的 payor_model 吗？还是严格只写纯评分/补全字段、payor 也进审查表？

**Q3 · business_tags.role 本轮怎么处理**
你明确"标签审查不直接修改"。我建议 role 缺失/错配**只列审查表、不写回**。还是你希望我**直接补 role 并标【动你标注】**（更快但动了标签字段）？

**Q4 · pilot 25 家是否要重跑理由**
前车已把 pilot25 合并进库，但你认为"他给的推荐理由不是很好"。本次对齐我只做 new30；**pilot25 的理由要不要我一并重跑成 3 版本**？（若要，我会重算并覆盖那 25 家的 recommend，其余字段保留。）

**Q5 · "一级/二级分类弃用"确认**
我理解为 `category_l1/l2` 弃用、`tag_l1/l2` 仍由另一 AI 维护且我只读。对吗？还是你指的"弃用"包含 `tag_l1/l2`？

**Q6 · 全量铺开节奏**
30 家对齐确认后，是否**自动续跑全量 1502**（波次制+周期 checkpoint），还是每个大阶段都先停一下等你 sign-off？免费期 07-22 截止，我的建议是对齐后自动续跑 Tier1，Tier3 前再停一次。

---

*待你回复 Q1–Q6 后，我出拆解版执行清单（每批 serial 范围 + worker/validator prompt 模板），再开工 30 家对齐批次。*
