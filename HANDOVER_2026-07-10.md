# 🔔 Silver Pulse（银脉）项目交接文档

> **创建时间**：2026-07-10 01:05
> **创建原因**：HY3 免费积分超限（429 限流，重置 07-10 15:29），换账号交接给新 AI
> **给新 AI 的第一句话**：请完整阅读本文档后，立即设置自动化 + 长程任务，今晚持续工作，把待办事项尽量清完。当前消耗的是自费积分，请高效工作、不做无关任务。

---

## 一、项目是什么

**Silver Pulse（银脉）** = 银发经济投融资资讯聚合看板 + 企业数据库，服务商业写作者（小爽）选题决策。本质是「选题情报台」，不是资讯汇编站。核心交付 = 深度选题（企业研究）。

三层愿景：资讯看板 → 企业数据库 → 供需对接

- **线上地址**：https://shuanghello153-eng.github.io/silver-pulse/
- **技术栈**：Python 生成静态 HTML，GitHub Pages 托管（gh-pages 分支），无后端
- **三页面**：index.html（资讯+选题雷达）/ enterprise.html（企业库）/ about.html（规则说明）
- **项目根目录**：`G:\workbuddy\2026-06-28-23-34-20\silver-pulse`
- **Python 路径**：`C:\Users\shuan\.workbuddy\binaries\python\versions\3.13.12\python.exe`

---

## 二、当前状态快照

### 数据资产
| 资产 | 数量 | 说明 |
|---|---|---|
| 企业库 | 1180 家 | `data/enterprise/all_enterprises.json`，17字段schema |
| 信源 | 50 个 | `config.py` SOURCES（T1=13/T2=30/T3=7），含今晚新增的10个直连RSS |
| 资讯 | 63 条 | `data/scored_latest.json`，含 cluster_id/cluster_size/is_main |
| 企业-资讯关联 | 54 家有动态 | `enterprise_scores.json` 的 related_news_ids（从0重建到54） |
| 研究价值分级 | S=8/A=26/B级若干 | 天花板 83（刚从68拉上来），头部有区分力了 |

### 部署状态
- ✅ gh-pages 已上线，最新 commit `389be98`（推到 gh-pages 分支）
- ✅ 三页 HTML 已全量重生并提交 main（`75a491b`）
- ⚠️ **工作区有未提交改动**（分身崩溃遗留，见下方注意事项）

### 自动化任务状态
| ID | 名称 | 频率 | 状态 | 说明 |
|---|---|---|---|---|
| `automation-1783586255120` | 执行型推进 | 每小时整点 | **PAUSED** | 429限流导致暂停，需重新激活 |
| `automation-1783589798589` | 资产扩充 | 每小时半点 | **PAUSED** | 同上 |
| `1782915193510` | 每周资讯更新 | 周日11:00 | ACTIVE | 核心流水线：采集→评分→生成 |
| `1783335516292` | 标签池迭代 | 周日17:30 | ACTIVE | 依赖11:00数据 |
| `1783384937323` | 摘要推送 | 周日19:00 | ACTIVE | 精选摘要 |
| `1783507424052` | 自愈补跑 | 周日21:00 | ACTIVE | 兜底补漏 |

> ⚠️ 两条 HOURLY 已 PAUSED（因 429 限流）。**新 AI 拿到手后第一件事：重新激活它们**（换账号后不再有 429 问题）。

---

## 三、今晚已完成的工作（不要重做！）

### 已提交到 main 的 commit（时间倒序）
| commit | 内容 | 分身 |
|---|---|---|
| `8aaa8a8` | 文档精简（项目全景交接文档→1页）+ TASKS状态更新 | DOC分身 |
| `75a491b` | 全量重生HTML+部署gh-pages(389be98) | 主会话 |
| `7120454` | UI前端大改(按钮收纳/收藏样式/标签对齐/已读未读) 救回崩溃分身残留 | 主会话救援 |
| `094fd0a` | 企业研究价值加"资料厚度"分量(天花板68→83/S级0→8家) | 主会话 |
| `85d8e85` | 企业库扩充+29家(到1180) | SRC分身v1 |
| `d972b1c` | T42核验脚本 verify_deliverables.py | DATA分身v1 |
| `bdca907` | 合并权威文档任务清单 TASKS.md | 主会话 |
| `71c018f` | 新增21家企业+4个一手源(AARP/Alz/NIA/AgeUK) | SRC分身v1 |
| `669ad48` | T50关联挂载到run_daily防覆盖 | DATA分身v1 |
| `7354832` | B1/B5 value_score分布报告 | DATA分身v1 |
| `558a1c3` | B2推荐理由引用维度分 | DATA分身v1 |
| `be4dd9b` | T50企业-资讯关联重建(0→43家) | DATA分身v1 |
| `d7fc0a1` | 建立TASKS.md单一真相源 | 主会话 |
| `4fefadd` | 事件簇对照卡+信源降权(T1=0.5/T2=0.3/T3=0.1) | 主会话 |
| `fe3634e` | 三阶段重构已落地部分(反向词收紧/跨源去重/信号强度公式) | 主会话 |

### 分身崩溃未完成（需要重做的）
| 分身 | 任务 | 状态 | 崩溃原因 |
|---|---|---|---|
| UI-2 | T05不再显示+T06备注+T08精选统一+T09用词+T41 about重写 | ❌ 429崩溃 | 可能留下部分改动在工作区 |
| DATA-2 | T34标签重构+T29低模二筛骨架+T33正文抓取方案 | ❌ 429崩溃 | 可能留下部分改动在工作区 |
| SRC-2 | T31 Google News修复+T32一手源+T51企业库到1300+ | ❌ 429崩溃 | 可能留下部分改动在工作区 |

---

## 四、评分体系最新决策（核心！）

> ⚠️ 产品方案文档里写的"6维评分"**已被覆盖**。以小爽 2026-07-09 晚亲自定的方案为准。

### 三阶段五维架构

```
阶段1：初筛（脚本，0成本）
  → 强命中银发词 OR 命中已知银发企业
  → 反向词已删泛词（child/teen等），保留真儿科词（pediatric/pregnancy）
  → 跨源去重（DedupStore，标题归一化+URL哈希，2周窗口）

阶段2：低模二筛（0722前hy3/后DeepSeek V3.2，极低成本）
  → 判断银发经济相关性：是否直接面向老年人群/给老人提供服务或产品/Medicare用户
  → 不符合→踢出，写入 blocked_log.json
  → 每次跑完自动抽查前20条，汇报误杀风险
  → 【代码骨架未建，需新AI搭建】selection/relevance_screener.py

阶段3：信号强度（脚本，0成本，已落地✅）
  → selection/signal_strength.py
  → 信源起点 T1=0.5/T2=0.3/T3=0.1（已降权）
  → IPO=6, 融资金额分级(海外美元/国内元), 收购分级, 时效分级
  → 0722后仅 signal_strength≥5 喂给AI

阶段4：强模五维（0722前hy3/后按文章长短收费的模型）
  → 信息量(0~10) × 30%
  → 差异化(0~10) × 30%  ← 暂不变（小爽说可复制性太虚，差异化更实在）
  → 可复制(0~10) × 20%
  → 综合评分(0~10) × 20%
  → 默认只喂正文，仅 signal_strength≥8 的头部才联网搜索
  → 输出：分类/标题/摘要/标签/事件/领域 + 五维评分依据
  → 【代码未建，需新AI搭建】
```

### 企业评分（已落地✅）
- `selection/enterprise_score.py`：base_value(0-70) + event_boost(0-30) + mirror_bonus + **evidence_depth(0-12)** ← 今晚新增
- 分级：S≥75 / A≥65 / B≥55 / C≥45
- 天花板已从68拉到83，S级8家、A级26家
- 关联重建：`selection/rebuild_association.py`，已挂载到 `run_daily.py:54`（enterprise_score之后）

### 成本校准（实测）
- DeepSeek V3.2（硅基流动）：输入2元/百万token、输出3元/百万token
- 每条约 0.0016–0.003 元（小爽原估0.001偏低）
- HY3 免费期至约 2026-07-22

---

## 五、待办事项及优先级

> 完整清单见 `TASKS.md`。以下按优先级摘录。

### 🔴 P0 — 今晚马上做（代码类，可自主完成）

| 编号 | 任务 | 说明 | 文件域 |
|---|---|---|---|
| **T05** | "不再显示"标记+找回 | 资讯/企业每条加隐藏按钮，localStorage，筛选栏加"显示已隐藏"开关 | 前端 |
| **T06** | 备注功能 | 每条加备注输入框，localStorage，列表显示前20字，导出包含 | 前端 |
| **T08** | 精选统一规则 | 资讯页和企业库页"精选"概念统一，文案一致 | 前端 |
| **T09** | 评分用词统一 | 综合分→"评分"，研究价值分→"研究分"，about同步 | 前端 |
| **T41** | about.html 重写 | 改成小爽视角人话版，信源数更新为50 | 前端 |
| **T34** | 标签体系重构 | 标签只留交叉维度(IPO/收购/反常识/政策利好等)，不与分类重复；清洗现有63条 | 数据 |
| **T29** | 低模二筛代码骨架 | selection/relevance_screener.py，模型抽象接口(支持hy3/DeepSeek切换)，blocked_log+auto_audit | 数据 |
| **T33** | 正文抓取方案 | collector.py 加 extract_full_content() 骨架，设计存储方案 | 数据 |
| **T31** | Google News 容错 | GN源加try/except+更长超时+备胎源 | 信源 |
| **T51** | 企业库扩到1300+ | WebSearch发现新银发企业，按S1-S4标准 | 信源 |

### 🔴 P0 — 部署与验证

| 编号 | 任务 | 说明 |
|---|---|---|
| **T33-deploy** | 重生HTML+部署gh-pages | 今晚改动已部署(389be98)，但分身崩溃后可能有新改动需要重新部署 |
| **T42** | 核验脚本跑一遍 | `selection/verify_deliverables.py` 验证 T50/T12 等 |

### ⏸️ P1 — 需小爽拍板/需API key（不要自主做）

| 编号 | 任务 | 卡点 |
|---|---|---|
| B4 | 权重校准 | 需小爽标的好/中/差样本 |
| T35 | 收藏回灌 | 需小爽导出 feedback.jsonl |
| T46 | 医疗/保险相邻白名单 | 需小爽定边界 |
| 阶段4强模 | 五维评分实测 | 需 API key（HY3 或 DeepSeek） |
| 0722降级 | Token降级方案 | 小爽定7/15再做 |

---

## 六、关键文件结构

```
silver-pulse/
├── config.py                 # 信源/评分权重/阈值/去重/反向词（单一真相源）
├── collector.py              # RSS采集 + is_relevant + DedupStore去重
├── score_and_merge.py        # 信号过滤+聚类+信号强度 → scored_latest.json
├── run_daily.py              # 每日编排(21步)
├── generator.py              # 生成 index.html（资讯+选题雷达）
├── gen_enterprise.py         # 生成 enterprise.html（企业库+事件簇对照卡）
├── gen_about.py              # 生成 about.html（规则说明）
├── ui_common.py              # 共享CSS/JS（收藏/标签/时间筛选/已读未读）
├── deploy_ghpages.py         # git plumbing部署到gh-pages
├── TASKS.md                  # ⭐全量任务清单（单一真相源，挑活读这个）
├── 产品方案与决策文档.md      # 权威方案文档（T01-T44分类任务清单）
├── 决策记录_2026-07-09.md    # 近期决策记录（D-01~D-17已确认 + S-01~S-04待设计）
├── 项目全景交接文档.md        # 1页快速上手（今晚精简过）
├── selection/
│   ├── signal_strength.py    # 信号强度公式（已落地✅）
│   ├── enterprise_score.py   # 企业研究价值分（已加evidence_depth✅）
│   ├── rebuild_association.py # 企业-资讯关联重建（已落地✅）
│   ├── recommend.py          # 推荐理由（已加分数依据✅）
│   ├── enrich_entity.py      # 企业名抽取
│   ├── cluster.py            # 事件聚类
│   ├── l1_prefilter.py       # L1预过滤
│   ├── aggregator.py         # 聚合器
│   ├── tagger.py             # ⚠️新文件未提交（untracked）
│   ├── verify_deliverables.py # 核验脚本
│   └── relevance_screener.py # 【待建】低模二筛
├── data/
│   ├── scored_latest.json    # 63条资讯（含cluster/signal_strength字段）
│   ├── enterprise/
│   │   ├── all_enterprises.json  # 1180家企业
│   │   └── enterprise_scores.json # 研究价值分+related_news_ids
│   ├── autonomous_progress.log # 自动化认领日志（防git撞车）
│   ├── dedup_store.json      # 去重记录
│   └── blocked_log.json      # 【待建】低模拦截记录
└── output/                   # 部署用HTML镜像（已部署到gh-pages）
```

---

## 七、注意事项（重要！）

### 1. 工作区有未提交改动
分身崩溃后遗留了这些 modified 文件（可能是部分改动）：
```
M about.html, collector.py, config.py, gen_about.py, gen_enterprise.py,
M generator.py, index.html, output/about.html, output/index.html,
M score_and_merge.py, selection/recommend.py, ui_common.py
M 方案_评分体系重设.md
?? selection/tagger.py
```
**新 AI 先检查这些改动是否完整可用**：能用就 commit，不能用就 `git checkout -- <file>` 还原。

### 2. 429 限流问题
- HY3 免费积分超限，重置时间 2026-07-10 15:29:30 UTC+8
- 换账号后不再有此问题
- 两条 HOURLY 自动化已 PAUSED，**换账号后第一件事：重新激活**

### 3. 文件域隔离（多分身并行时必须遵守）
- 各分身只 `git add` 自己的文件，**严禁 `git add -A`**
- 禁跑完整 `run_daily.py`（会抢 generator/data）
- 认领锁：`data/autonomous_progress.log`，CLAIM 后 40 分钟内被别的会话 CLAIM 且未 DONE 的任务跳过

### 4. Git 部署坑
- Windows 下 `git mktree` 经 stdin 传参会把 `\n` 变 `\r\n` 导致文件名带 `\r`、Pages 404
- `deploy_ghpages.py` 已改为写临时文件（LF字节）绕过
- GITHUB_TOKEN 未设，但 git credential store 里有 PAT

### 5. 小爽的偏好
- 称呼"小爽"，平等关系
- 只讲客观事实、说人话、回复精简
- 重复内容不重述直接给方案
- 直接执行、事事有回应、主动发挥有观点
- 低成本优先不降质
- 核心理念：**以海外为镜照中国之路**；深度企业分析 > 资讯流水
- 选题四标准：S1大企业(有年报) / S2热点事件 / S3反常识差异化模式 / S4国内可比性
- 只做选题发现+初步研究，不写稿（另一AI写）、不发布
- 0722前 HY3 免费期，可全量重评零成本

---

## 八、给新 AI 的立即行动指令

### 第一步：检查工作区
```bash
cd "G:/workbuddy/2026-06-28-23-34-20/silver-pulse"
git status --short
# 检查 modified 文件是否完整可用，能用的 commit，不能用的还原
```

### 第二步：重新激活自动化
```
automation-1783586255120 → status: ACTIVE（执行型，整点，读TASKS.md挑UI/DATA）
automation-1783589798589 → status: ACTIVE（资产型，半点，读TASKS.md挑SRC）
```
用 `automation_update` 工具，mode="update"，把 status 改成 "ACTIVE"。

### 第三步：设置长程任务
建议新增一条每日长程任务（比 HOURLY 更适合做深度工作）：
- 频率：每天 2-3 次（如 09:00 / 14:00 / 22:00）
- 内容：读 TASKS.md → 挑 🔴 待做 → 落地+commit → 更新 TASKS 状态
- 与 HOURLY 错开，HOURLY 做小修补，长程做需要多步骤的任务

### 第四步：派分身并行干 P0 任务
参考文件域隔离原则，派 3-4 个分身：
- UI 分身：T05/T06/T08/T09/T41（前端）
- DATA 分身：T34/T29/T33（数据/评分）
- SRC 分身：T31/T51（信源/企业库）

### 第五步：全量重跑 + 部署
分身完成后：
```bash
# 重生3页HTML
python generator.py && python gen_enterprise.py && python gen_about.py
# 部署
python deploy_ghpages.py
# 提交产物
git add output/*.html && git commit -m "build: 全量重生+部署"
```

---

## 九、沟通协作原则

1. **七段式回复**：事事有回应，知行合一，要事第一
2. **双向指正**：发现小爽的想法有问题要直说，不要一味附和
3. **删除前确认**：不擅自删除文件/任务
4. **信任但验证**：分身报告的成果要核验（跑脚本/查git log），不谎报
5. **成本意识**：当前消耗自费积分，不做无关任务，高效工作
6. **评分体系纪律**：只做"已确认需求 D-xx"，绝不碰"待拍板 S-xx"
7. **多账号协作**：靠 TASKS.md + 产品方案文档 + 决策记录 + MEMORY.md 交接

---

## 十、自动化任务持续运行机制

### 当前问题（已解决）
之前两个 HOURLY 只写文档不落地代码——**根因是 prompt 任务池里"写文档"零风险，"改代码"有冲突风险，agent 挑软柿子捏**。已重写为"每次必须有可验证落地成果+commit"。

### 持续运行设计
1. **任务队列 + 认领锁**：`autonomous_progress.log` 防止多会话抢同一活
2. **每次必有落地**：代码/数据改动 + git commit，禁止只写文档
3. **频率按任务性质**：落地型每小时、积累型错峰
4. **长程记忆蒸馏**：发现写进 `.workbuddy/memory/`，跨会话不丢
5. **全清后空跑**：任务全做完每轮只报告"暂无可推进项"，不耗资源

### 建议新增的长程任务设计
```
名称：Silver Pulse 每日深度推进
频率：FREQ=DAILY;BYHOUR=9,14,22
内容：读TASKS.md→挑高优先级🔴→做多步骤任务(如标签重构/二筛骨架/正文抓取)→commit→更新TASKS
与HOURLY区别：HOURLY做小修补(单文件改动)，长程做多步骤任务(跨文件)
```

---

*本文档由主会话 AI 于 2026-07-10 01:05 创建，确保新账号 AI 可无缝衔接。如有疑问，先读 `TASKS.md` 和 `产品方案与决策文档.md`。*
