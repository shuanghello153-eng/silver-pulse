# 银发企业库 · 评分推荐 + 信息补全 · 执行手册（V1 · 师傅带徒弟版）

> **文档定位**：本手册是「企业库全量评分推荐 + 信息补全 + 标签审查（只读）」这项任务的**唯一工作准则与交接文档**。
> 写给谁看：一个刚接手这个库、什么都不懂的徒弟。读完全文，应能独立从 0 到 1 接手并把这个任务做对。
> **版本规则**：本手册由本任务负责人（研究员）独立维护，带版本号（V1/V2…）。**绝不修改小爽发的《评分推荐规则 2026-07-12 版》（文档1）与《TAGGING_RULES_V20》（文档2）**——那两份是别的 AI 在用/维护的，本手册只引用它们。
> 创建：2026-07-16。依据：文档1（评分权威）+ 底层代码实读 + 真实数据探查。

---

## 目录
1. 需求 · 背景 · 目标（徒弟先搞懂"为什么做"）
2. 现状盘点：代码 vs 文档的 4 处冲突（徒弟必须知道的"坑"）
3. 评分规则全文（文档1 四阶段 · 逐条列出 + 人话翻译）
4. 企业字段清单与每个字段的处置决策（28 字段全列 + 我的理由）
5. 执行流程（把代码翻译成"人话"：阶段3怎么算、阶段4 AI 怎么评、理由怎么写、信息怎么校验）
6. 并行架构（澄清：不是 50/80 个智能体同时跑）
7. 优先级与分批策略（先拉谁、后拉谁、为什么）
8. 校验与低成本试错（pilot 25 家 + 四道防线 + 你终审）
9. 与其他 AI 的冲突规避（为什么"覆盖别人"在架构上不可能）
10. 交付物与后续前端步骤（前端现在不动，以后怎么接）
11. 负责人对本轮关键疑问的判断（直接回答小爽这轮的问题）

---

## 1. 需求 · 背景 · 目标

### 1.1 这个库是干什么的
Silver Pulse 是小爽的「银发经济选题情报台」。她每天早上打开网站，要从**上千家企业**里一眼挑出"值得写、且能借鉴到中国"的标的。企业卡片上那行**推荐理由 + 研究价值分**，是她最先看的东西。

### 1.2 这次任务的三个动作，本质是"同一遍工作"
1. **评分 + 推荐理由（按文档1）**：给每家企业算"值不值得研究"的分，并写出一段"为什么值得写、对中国有什么借鉴"的自然语言理由。
2. **信息校验 + 字段补全**：评分过程要"对企业做全面信息搜索，信息来源充分"——搜出来的东西正好用来**补全缺失字段**（中文名、成立年、融资、投资方、新闻覆盖、亮点、深度文章链接等）。
3. **标签审查（只读、不改）**：标签是另一个 AI 在维护（见 §2/§9），我**不碰 `tag_l1/tag_l2/business_tags` 的写入**，只产出一张"建议新增/删除/优化"的表交小爽拍板。

三者关联：评分的"信息量/差异化/可复制"三维**必须联网搜过**才有依据 → 顺手把字段补全；补全后的字段又让下一轮评分更准。是同一遍闭环。

### 1.3 "做对"的验收标准
- 分数和理由能让小爽一眼判断"这家我写不写"；
- 字段真实、可追溯（每条关键数据有来源链接，不编）；
- 不和其他正在跑的 AI / 未部署代码冲突；
- 全量 1502 家，**不漏一家、不幻觉一家**。

---

## 2. 现状盘点：代码 vs 文档的 4 处冲突（徒弟必看的坑）

> 小爽的铁律："必须看底层代码，不能只信文档"。照做后，发现文档和代码对不上。**所有方案以代码实测 + 文档意图对齐为准。**

### 2.1 数据真相源 = `data/enterprise/all_enterprises.json`
- **1502 家**（文档1 写 1328——信源治理 W1-W4 又入库了一批，库是实时最新的）。
- 实时读取即为最新（打标 AI 直接写这个文件，标签已是最新；另一位做信源的 AI 也在往里加企业）。
- 字段实测填充率（非空/1502）：见 §4 表。

### 2.2 冲突 1（最关键）：评分代码 ≠ 文档1
- **文档1** 规定"四阶段"：阶段3 信号强度(0-10,脚本) + 阶段4 AI 四维(信息量/差异化/可复制,0-10) → 综合 = 信号×30% + 信息量×30% + 差异化×20% + 可复制×20%（**0-10 量纲**）。其中**阶段4 在文档1 里标着 `❌ 待建`**——意思是"规则写好了但还没人实现"。**本任务就是去实现它。**
- **实际在跑的代码** `selection/enterprise_score.py`：一套**独立的** `research_value`(0-100)，公式 V1规模+V2信息+V3模式+V4可借鉴 + 事件boost + 海外镜像 + 证据厚度。但它**不联网、且 V2/V3/V4 读的是已废弃的 `tags` 字段 + `business_model`**，等于空壳；它写的是**独立文件 `enterprise_scores.json`**，不是 `all_enterprises.json`。
- **前端 `gen_enterprise.py`** 读 `enterprise_scores.json` 的 `research_value`(0-100) 画徽章，回退才用 `all_enterprises.json` 的 `value_score`。
- **结论**：文档1 的评分是"设计稿待建"，旧代码是自成一派且依赖废弃字段。**本任务基准 = 文档1 四阶段**（小爽明确"评分按规则走"）。旧 `enterprise_score.py` 在本任务完成后应退役。

### 2.3 冲突 2："一级/二级分类弃用"指哪个
- 库里有**两套**分类：
  - `category_l1/l2` = 选题库旧分类（config 里 `ENTERPRISE_CATEGORIES` 的 8 一级/70 二级）。
  - `tag_l1/tag_l2` = V20 刚重做的核心标签（8 一级/约 95 二级，另一个 AI 维护）。
- 小爽澄清：**`tag_l1/tag_l2` 才是她说的"一级/二级标签"，由另一个 AI 维护，本任务只读不写**；`category_l1/l2` 旧分类**归本任务处理**（可废弃、也可顺手派生填充，见 §4/§11）。
- 关键事实：`category_l1` 的 8 个值（养老服务/康复辅具/消费品/文娱社交/食品营养/行业服务/金融保险/投资机构）**与 `tag_l1` 的 8 个一级完全同名**；且前端 `gen_enterprise.py` 分类徽章**早已改用 `tag_l1/tag_l2`**（代码行 244 确认），`category_l1/l2` 现在**前端根本不读**。→ 处理旧分类零前端风险。

### 2.4 冲突 3：`business_model` 文档说废弃，但库里 80% 填充且部分可读
- 文档1 §二 把 `business_model/business_model_cn` 列"⚡️已废弃，不补全"。
- 但 V20 §2.3 说"技术维度默认不标，SaaS/AI 降级不标或并入产品词"，且它目前是卡片上可读信息。
- **本任务判断**：废弃 `business_model` 自由文本（见 §4/§11 详细理由），不主动重写全部 1210 条；`role`（在 `business_tags.role`）保留作输入。

### 2.5 冲突 4：别的 AI 正在动代码，但线上未部署
- `git status` 显示 `gen_enterprise.py`/`ui_common.py` 有未提交改动（别人在重写 UI，但小爽确认其工作"只是交互 UI，不涉及字段展示"）。
- 历史教训：`PROJECT_STATUS` 记载上一个做"全量 news_coverage"的 Agent **崩溃没做完**，所以现在 news_coverage 只有 ~480 家（31%）。→ 本任务必须**分片、可断点续跑、每批落盘**，不能一把梭。

---

## 3. 评分规则全文（文档1 四阶段 · 逐条列出 + 人话翻译）

> 以下规则**逐字摘自文档1**，确保不漏。文档1 原文以"资讯和企业走同一套"表述；企业侧准入（阶段1/2）已先验收入库，本任务只做企业侧对齐，详见 §5。

### 3.1 阶段1 初筛（脚本）✅ 已落地
1. 强词命中 **或** 命中已知企业库（`all_enterprises.json`）→ 准入；其余不准入。
2. 已删除弱词、已删除反向词（反向词会误杀"老幼共居"等真银发场景）。
3. 去重：标题归一化 + URL 哈希，2 周窗口内重复文章不再进入。
4. 抽查检验：每次跑完自动抽样，报告每条命中的强词 / 已知企业，写 `data/run_logs/relevance_audit_*.json`。
5. **强词清单（约 60 个，中英混合）**：养老/老年/老人/银发/长者/长辈/退休/适老/适老化/照护/康养/医养/助老/陪护/康护/颐养/老年病/长寿/居家护理/居家照护/康复/慢病/认知症/痴呆/阿尔茨海默/失智/失能/护理院/养老院/敬老院/养老社区/养老产业/银发经济/养老金融/养老科技；英文 aging/elderly/senior/senior care/elder care/aged care/dementia/alzheimer/long-term care/nursing home/assisted living/home care/caregiver/caregiving/hospice/medicare/medicaid/retirement/senior living/pace program/value-based care/older adult/silver economy/age tech/memory care/senior housing。
- **企业侧对齐**：企业库已先验收入库（含银发信号铁律过滤），阶段1 对企业直接"准入"。但本任务打分时若发现某企业**实质非银发**（综合电商养老仅规划中、品牌PR、Web3、泛再生医学、泛医疗VC等），按"银发信号铁律"标记 `needs_review` 并列入审查表，不静默删（删库是另一个 AI 的活，见 §9）。

### 3.2 阶段2 低模二筛（便宜 AI）✅ 功能已落地，默认关
符合任一即相关：
- ① 直接面向老年人群（C 端），或面向 Medicare / Medicare Advantage 用户；
- ② 为上述 B 端企业提供产品/服务的上游供应商或服务商（B2B）。
泛医疗/泛健康/一般医院并购/儿科孕产/年轻人产品，即使带"养老健康"词也不算。
- **企业侧对齐**：企业已准入，阶段2 对企业基本放行；本任务不重跑阶段2（默认关），除非某家企业实质存疑（进阶段4 时由 AI 判断）。

### 3.3 阶段3 信号强度（脚本，0~10 分）✅ 已落地
各项相加，封顶 10 分（`selection/signal_strength.py`，经代码核对与文档1 完全一致）：
1. **信源起点**：T1=0.5，T2=0.3，T3=0.1（企业无信源等级概念 → 本任务对企业取 **0**，或按 `source` 健康度，待定；不影响主分，因企业信号来自事件而非出处）。
2. **IPO** = +6。
3. **融资（海外美元，≥）**：10亿=5，1亿=4，5000万=3，1000万=2，500万=1，100万=0.5，其余=0。
4. **融资（国内元，≥）**：10亿=6，1亿=5，5000万=4，1000万=3，500万=2，100万=1，50万=0.5，其余=0。
5. **收购（海外美元/国内元，≥）**：100亿=6，50亿=5，10亿=4，1亿=3，5000万=2，1000万=1。
6. **时效（每月重算）**：近1周=2.5，近1月=2，近2月=1.5，近半年=1，近1年=0.5，更久=0.25。
7. **喂 AI 门槛**：0722 前全部喂；0722 后只喂 ≥5 分的。
- **企业侧对齐（本任务怎么算企业信号强度）**：企业的"事件"来自 `funding_latest`（融资/收购）和 `funding_total`。算法：IPO 标志（上市/IPO 关键词）→ +6；`funding_latest.amount` 按海外美元/国内元双阈值给分；若为收购（`funding_latest.acquisition`）按收购阈值；时效以"该融资/收购日期 或 最新关联资讯日期"算 recency。合计 clamp 0~10。这一步**纯脚本、零 AI、确定性**，由主智能体一次性算完全部 1502 家（见 §5.1）。

### 3.4 阶段4 强模精评（强 AI，免费期零成本）❌ 待建 → **本任务实现**
AI 一次通读企业 / 资讯全部资料，同时完成：
1. **信息量（0~10）**：搜这家企业 / 事件前几页信息的数量和质量（信息越多越好写，通常是大企业 / 热点）。
2. **差异化（0~10）**：商业模式反不反常识、企业起落是否反复巨大。
3. **可复制（0~10）**：基于企业库 + 可联网搜网上信息，判断国内有无同类赛道、赛道里有没有上亿融资 / 上市公司、支付方差异（美国 Medicare / 自费 vs 中国医保 / 自费，日本近似中国）→ 能不能平移、是空白还是红海。
4. **综合评分 = 信号强度×30% + 信息量×30% + 差异化×20% + 可复制×20%**（是这个公式算出的结果，不是另外再打一个分）。
5. **输出评分依据（就是推荐理由，两者一回事）**：用一段自然、流畅的话，说清这条为什么值得写、能从中国什么角度切入，把信号强度/信息量/差异化/可复制四方面判断融进去讲，不刻意分段、不逐维列分。写完后自检：凡是卡片/列表已展示的结构化信息（融资金额、轮次、标题、摘要、来源、日期等），评分依据里不再重复陈述；若某条对判断关键，可作为论据点到为止，但以不与列表重复展示为准。

### 3.5 企业字段清单（文档1 §二，24 项，逐字列出）
补全时一个都不能丢。`←`=填充差待补；⚡️=废弃/规则变更。
- **A 基础信息**：1 serial 编号；2 name 英文名；3 name_cn 中文名←（约45%空）；4 region 地区；5 founded 成立年←（约25%空）；6 stage 成长阶段←（945家"未披露"）；7 website_url 官网←（约21%空）；8 crunchbase_url；9 description/desc_cn 描述。
- **B 投融资**：10 funding_latest 最新一轮融资（结构 `{round, amount, currency, date}`；收购记 `{acquisition:{date,amount,acquirer}}`；库里 amount 被错填成投资方名，需纠正）；11 funding_total 累计融资←；12 investors 投资方←（只记最新一轮/收购方，建议统一数组）。
- **C 商业模式与标签**：13 ⚡️ business_model/business_model_cn（已废弃）；14 business_tags 客户/角色/渠道（字典 `{customer,role,channel}`，全库1325家有）；15 tag_l1 一级标签（12项→现8项）；16 tag_l2 二级标签（58项→现约95项）；17 ⚡️ tags 旧标签（已废弃）；18 category_l1/category_l2 选题库旧分类（不动，保留）；19 source 数据来源。
- **D 评分与洞察（AI生成/待重做）**：20 news_coverage 新闻覆盖←（约79%空）；21 highlights 亮点←（质量差，多"成立于2016年"废话，需重写）；22 value_score 旧研究价值分（新评分落地后替换）；23 recommend 旧推荐语（按阶段4第5点重做）；24 related_news_ids 关联资讯（脚本自动匹配）。
- **补全范围（文档1 明示）**：13、17（已废弃）与 18（分类，保持不动）不补全；其余字段全部在阶段4 AI 通读时一轮补全/校正。

### 3.6 企业库规则（文档1 §四，逐字列出）
- **深度文章链接字段** `deep_article_links`：数组，**0~2 条**，按价值降序；无高质量文章时为空数组。用途：AI 通读时发现高质量第三方深度文（商业模式深度分析、创始人采访）存入，供小爽写稿前参考别人写法、找差异化。纯资讯快讯不纳入（走 `related_news_ids`）；只放深度/长文。
- **其余字段补全**：除深度文章链接外，其余企业字段全部在阶段3/阶段4 AI 通读环节一轮补全/校正（注：13 business_model、17 tags 已废弃，18 category 不动，这三处不补全；其余补）。
- **二级标签单归属（硬规则）**：所有二级标签只能属于一个一级标签（由另一个 AI 维护，本任务只读）。

---

## 4. 企业字段清单与每个字段的处置决策（28 字段全列 + 我的理由）

> 实测 `all_enterprises.json` 共 28 类键。下表是"本任务怎么对待每个字段"的权威决策。**原则：我的字段我写；别人的字段我不碰；废弃的留着但不再维护；缺的联网补。**

| # | 字段 | 实测填充 | 本任务处置 | 理由（我的判断） |
|---|---|---|---|---|
| 1 | serial | 100% | **不碰** | 唯一编号，入库流程维护 |
| 2 | name | 100% | **不碰** | 英文名标识 |
| 3 | name_cn | 39%(590/1502) | **补全** | 中文名缺失多；联网补，提升可读性 |
| 4 | region | 100% | **不碰** | 国内/海外，已准 |
| 5 | founded | 79%(1187) | **补全/纠** | 约21%空或错；联网补成立年 |
| 6 | stage | 100%(但945"未披露") | **补全** | 945家"未披露"=大缺口；通读判种子/天使/A轮/成长期/已上市… |
| 7 | website_url | 69%(1040) | **补全** | 缺失补官网 |
| 8 | crunchbase_url | 78%(1181) | **补全** | 缺失补 |
| 9 | description | 100% | **校验/补** | 部分模板占位（V20 §6.4 强调）；搜到更准官方描述则补，不改正确的 |
| 10 | desc_cn | 100% | **校验/补** | 同上 |
| 11 | category_l1 | 100% | **派生镜像** | 与 tag_l1 8 一级同名；从 tag_l1 派生（取首个），前端不读，留兼容。零研究成本 |
| 12 | category_l2 | 1500/1502 | **派生镜像** | 从 tag_l2 派生（取首个或映射）；同上 |
| 13 | tags（旧标签） | 100% | **不碰/废弃** | 文档1 §二标⚡️已废弃；由 tag_l1/tag_l2 替代。不维护、不读 |
| 14 | tag_l1 | 100% | **只读不写** | 另一个 AI 维护；读入用于"可复制"判断+理由上下文；错标列入审查表，不写回 |
| 15 | tag_l2 | 100% | **只读不写** | 同上 |
| 16 | business_model | 80%(1210) | **废弃/不主动维护** | 见 §11 详细理由：文档1废弃+V20标签替代+重复度高；保留值(legacy)但不重写全部；错误列入审查表 |
| 17 | business_model_cn | 88%(1329) | **废弃/不主动维护** | 同上 |
| 18 | business_tags | 88%(1325) | **只读为主** | 结构化 `{customer,role,channel}`；role 对应 V20 角色维度，是有用输入；默认只读，高把握时微调并标【动你标注】 |
| 19 | source | 100% | **不碰** | 数据来源标识 |
| 20 | payor_model | 100%(全"未披露") | **不碰** | 别人新加字段，不动 |
| 21 | funding_latest | 59%(893) | **纠+补** | **结构错填**（amount 被填成投资方名，样本 AgeClub 即如此）→按 `{round,amount,currency,date}` 纠正；收购记 `{acquisition:{...}}`；缺失补 |
| 22 | funding_total | 52%(790) | **补全** | 缺失补累计融资 |
| 23 | investors | 26%(588) | **补全** | 大缺口；统一数组，只记最新一轮/收购方 |
| 24 | highlights | 86%(1296) | **重写** | 取代"成立于2016年"废话→真实亮点（如"美国最大居家护理公司，覆盖X州，年营收$X"） |
| 25 | value_score | 68%(1021) | **覆盖为新分** | 旧分(0-100)；桥接前端 fallback，写新综合分×10（见 §10） |
| 26 | recommend | 86%(1302) | **覆盖为新理由** | 旧质量理由；按文档1 §3.4.5 重写 |
| 27 | news_coverage | 31%(480) | **补全** | 31%→目标100%；含 news_count/quality/latest_news；深度文进 deep_article_links |
| 28 | ingest_time / needs_review | 177 / 24 | **不碰** | 别人加的字段，不动 |
| — | **signal_strength**（新） | 0% | **新增** | 阶段3 脚本算，0-10，存 all_enterprises.json |
| — | **info_score / diff_score / copy_score**（新） | 0% | **新增** | 阶段4 AI 三维，各 0-10 |
| — | **research_value**（新） | 0%（all_enterprises内） | **新增** | 综合分×10 = 0-100，桥接前端 |
| — | **update_time**（新） | 0% | **新增** | ISO 时间戳，本任务每次改动即刷新（小爽批准新建，与 ingest_time 并存） |
| — | **deep_article_links**（新） | 0% | **新增** | 数组0~2，深度文链接（文档1 §3.6） |
| — | **related_news_ids**（新） | 0% | **脚本补** | 由 `rebuild_association.py` 自动匹配关联资讯 |

**一句话总原则**：本任务写入的字段 = `signal_strength / info_score / diff_score / copy_score / research_value / value_score(覆盖) / recommend(覆盖) / update_time / deep_article_links / related_news_ids` + 补全类（`name_cn/founded/stage/website_url/crunchbase_url/funding_latest/funding_total/investors/highlights/news_coverage/description/desc_cn`）。**只读不写** = `tag_l1/tag_l2/business_tags(默认)/category(派生只读)/source/region/serial/name/payor_model/ingest_time/needs_review/tags`。**废弃不维护** = `business_model/business_model_cn/tags`。

---

## 5. 执行流程（把代码翻译成"人话"）

### 5.1 步骤0 · 主智能体跑脚本算全量信号强度（确定性，零 AI）
- 读 `all_enterprises.json`，对每家按 §3.3 企业侧算法算 `signal_strength`（0-10）。
- 同时建立 **1502 家 serial 清单（manifest）**，作为"全覆盖"依据（见 §8）。
- 产出 `scores_draft/_signal_strength_all.json`（serial → signal_strength + 拆解），不碰本体。
- 这一步约几分钟，纯规则，可重复。

### 5.2 步骤1 · 优先级排序（先拉谁，见 §7）
- 按 `signal_strength` 降序 + 是否有 news_coverage/已上市，排出优先级队列。
- Pilot = 队列前 25 家（主智能体亲自做，不过子智能体，先校验方法论）。

### 5.3 步骤2 · 每个企业的阶段4 AI 工作（worker 子智能体）
对一家企业，worker 做：
1. **联网搜前几页**：官网/年报/招股书/Crunchbase/新闻/小爽给的信源。评估并打 0-10：
   - **信息量**：资料多寡、是否上市/热点（好不好写）。
   - **差异化**：商业模式反不反常识、起落是否剧烈。
   - **可复制**：国内有无同类赛道、有无上亿融资/上市公司、支付方差异（Medicare/自费 vs 医保/自费）→ 空白还是红海。
2. **综合分** = 信号强度×0.3 + 信息量×0.3 + 差异化×0.2 + 可复制×0.2（0-10），存档为 `composite_0_10`。
3. **推荐理由**（文档1 §3.4.5 + 小爽补充"详细但不啰嗦、不泛泛而谈"）：一段自然流畅的话，把四维判断融进去讲"为什么值得写、从中国什么角度切入"；**不重复卡片已展示的结构化信息**（金额/轮次/日期等），点到为止；要让人一看就知道价值。
4. **信息校验与补全**：搜到的关键事实（成立年、融资、投资方、官网、中文名）与库内逐条比对：库空→填；库错（如 funding_latest.amount 是投资方名）→纠；冲突→以权威源为准，附来源 URL。搜不到→标"待补充"，不编。
5. **写草稿**：只写本企业的"我的字段"（见 §4 总原则）到 `scores_draft/draft_<serial>.json`，**绝不碰 all_enterprises.json 本体**。

### 5.4 步骤3 · validator 独立复核（每个 batch 一个）
独立子智能体复核该批：有没有**幻觉**（理由/字段与来源 URL 对不上）、字段**一致性**（金额格式、数组、ISO 时间）、推荐理由**合规**（不重复卡片信息、不泛泛而谈）、信号强度**公式复核**。输出"通过/打回"清单，打回的由 worker 重做。

### 5.5 步骤4 · 主智能体 merge（单线程，每波结束）
- 重读 `all_enterprises.json` 最新内容（保留他人字段，尤其 `tag_l1/tag_l2`）。
- 对该波每家，patch 我的字段（见 §4 总原则），写回。
- 即 checkpoint（断点续跑依据）。

### 5.6 步骤5 · 关联资讯 + 深度文
- 跑 `rebuild_association.py` 补 `related_news_ids`（现成工具）。
- `deep_article_links` 已在 worker 阶段写入。

### 5.7 步骤6 · 全局校验 + 抽审 + 报告（见 §8）
- 100% 复扫（非抽样）：schema/量纲/范围/一致性。
- 每批抽 3-5 家人工级复核。
- 出"改动 diff 报告 + 标签建议总表 + 未达标清单"交小爽。

---

## 6. 并行架构（澄清：不是 50/80 个智能体同时跑）

> 小爽疑问："1000多家分30批=50个智能体，你能撑50个同时跑吗？"——**不能也不需要。** 下面说清真实架构。

### 6.1 分批数 ≠ 并发智能体数
- 1502 家 ÷ ~37 ≈ **40 批**。但**不是 40 批同时跑**，更不是 40×2=80 个智能体。
- 真实并行 = **波次制（wave）**：每波启动 **3 批 = 3 worker + 3 validator = 6 个并发子智能体**。这是我这个环境能稳稳支撑的并发上限（再多上下文/配额会紧张、互相拖慢）。
- 40 批 ÷ 3 ≈ **14 波**。每波结束主智能体 merge + 校验，再起下一波。
- **动态调整**：若环境实际承载好，可升到 4 批/波（8 并发）；若某波超时多，降到 2 批/波。由主智能体按实时情况调。

### 6.2 为什么不用 80 个智能体
- 本会话上下文/配额有限；worker 联网搜一家企业要多次 WebSearch，并发太高互相拖慢、易超时、难监控。
- 6 并发是"质量×速度"的平衡点；且每个 worker 只写独立草稿文件，merge 由主智能体单线程做，根除了并发写冲突。

### 6.3 草稿文件隔离（防冲突核心）
- 所有 worker/validator **只写 `scores_draft/draft_<serial>.json`**，绝不碰 `all_enterprises.json` 本体。
- 主智能体每波单线程 merge → 即使 6 个 worker 同时跑，也没有"两个 AI 同时写大文件互相覆盖"的风险。

---

## 7. 优先级与分批策略（先拉谁、后拉谁、为什么）

### 7.1 排序键 = signal_strength（降序）
- 阶段3 信号强度正捕捉"热点+规模"信号（IPO/大额融资/近期收购/近期事件），与文档1 选题标准（大企业+热点事件+差异化+国内可比）天然对齐。
- 队列：signal_strength 高 → 先拉；长尾（文娱/消费/早期无事件）→ 后拉、轻量补全。

### 7.2 三层
- **Tier 1（先拉，约前 300-500）**：signal_strength 高（IPO/大额融资/近期收购）。最值得先写，且信息最丰富（AI 好评）。
- **Pilot（本任务第一步）** = 队列前 **25 家**，主智能体亲自做，不过子智能体，先校验方法论（见 §8）。
- **Tier 2**：已上市 / 有 news_coverage 的中等企业，全量联网评。
- **Tier 3**：长尾（文娱/消费/早期），轻量补全（补 stage/investors/name_cn/news_coverage）+ 脚本信号分 + 模板化理由。

### 7.3 为什么先保质量再扩面
- HY3 免费期到 2026-07-22（约剩 6 天），1502 家全量联网打分+补全可能不够。
- 先把最高价值标的做扎实（Tier 1），符合"先服务小爽最该看的"，也降低"免费期结束只做了一半"的风险。0722 后切 DeepSeek V3.2（低成本）续跑 Tier 3。

---

## 8. 校验与低成本试错（pilot 25 家 + 四道防线 + 你终审）

### 8.1 低成本试错：先 pilot 25 家
- 主智能体亲自拉**队列前 25 家**（融资/收购信号最强），完整跑阶段4（搜+评+理由+补全）。
- **自检**：每家理由我亲自核——有没有幻觉（与来源对不上）、有没有重复卡片信息、分数与公式是否自洽。
- **交付**：一张表（serial/名称/信号强度/三维分/综合分/推荐理由/补全了哪些字段/来源链接），**全部我已校验过**。
- **你核对**：你看这 25 家的分和理由"对不对味"，提修改意见 → 我据此校准 worker/validator 的 prompt 模板 → 再放大到全量波次。**这一步是"对齐"不是"执行完毕"**——对齐好了才铺开，避免 1500 家返工。

### 8.2 四道防线（保证"正确、没遗漏"）
1. **manifest 全覆盖**：开工建 1502 serial 清单，每批打勾，结束核"已处理==1502、无遗漏"。
2. **双重校验**：worker 自校验 → validator 独立复核 → 主智能体全局复扫 → 小爽终审。
3. **不并发写本体**：所有子智能体只写草稿，主智能体单线程 merge，根除互相覆盖。
4. **可断点续跑**：每波落盘即 checkpoint，某批崩了只重跑该批（吸取 T40 崩溃教训）。

### 8.3 交付报告（全量完成后）
- 改动 diff 报告（改了哪些字段、哪些企业、哪些标了【动你标注】）。
- 标签建议总表（新增/删除/优化，含证据，绝不写回）。
- 未达标清单（搜不到/存疑企业，标"待补充"）。

---

## 9. 与其他 AI 的冲突规避（为什么"覆盖别人"在架构上不可能）

### 9.1 三个其他 AI（小爽说明）
1. 海外找信源、补企业；2. 打标签（`tag_l1/tag_l2`，另一个 AI 维护）；3. "领导"AI 做网站走查修补。
- 他们"好久没提交到线上"= git/HTML 没部署，但**数据文件（all_enterprises.json）是实时最新的**（打标 AI 直接写它）。所以我能读到他们的最新标签。
- "不小心覆盖别人"在架构上**不可能**：我只写 §4 总原则里的"我的字段"；`tag_l1/tag_l2/business_tags(默认)/category(派生只读)/payor_model/ingest_time/needs_review` 一律不写。merge 时重读最新文件、保留他人字段、只 patch 我的键 → 即使打标 AI 并发改了 tags，我的 merge 也不碰那些键。

### 9.2 前端 UI AI
- 小爽确认其工作"只是交互 UI，不涉及字段展示"→ 我的 DB 字段工作与其无交集。
- 我不改 `gen_enterprise.py`（UI 代码）和 `enterprise_scores.json`（前端数据feed）本轮（见 §10 冻结）。

### 9.3 部署
- 本轮**绝不自行部署**。产物落数据文件 + 表格交小爽；等小爽和 UI 编辑都确认后，再统一走 `deploy_ghpages.py`。

---

## 10. 交付物与后续前端步骤（前端现在不动，以后怎么接）

### 10.1 本轮交付
- 更新后的 `all_enterprises.json`（真相源，含全部新字段 + 补全）。
- 一系列表格/报告（pilot 表、diff 报告、标签建议总表、未达标清单）。
- 本手册 V1（持续维护）。

### 10.2 前端桥接（现在不动，以后做）
- 当前前端读 `enterprise_scores.json.research_value`(0-100)，回退 `value_score`。
- 我本轮把新综合分 ×10（0-100）写入 `all_enterprises.json.research_value`（新字段）+ 覆盖 `value_score`（桥接 fallback）。这样**即使前端现在不动，未来只要重生成 `enterprise_scores.json` 从 all_enterprises.json 取数，徽章即正确**，无需改量纲。
- **后续（小爽批准后）**：① 重生成 `enterprise_scores.json`（从 all_enterprises.json 取 research_value + related_news_ids）；② 视情况更新 `gen_enterprise.py` 阈值（ENT_RV_HIGH=48/ENT_RV_MID=26 是按旧 0-100 分位数定的，新分同量纲可直接沿用，需复核）；③ 退役旧 `enterprise_score.py`。

---

## 11. 负责人对本轮关键疑问的判断（直接回答小爽这轮的问题）

**Q：business_model / role 这些字段这次要不要一起更新，还是废弃？**
- **我的判断：废弃 `business_model` / `business_model_cn` 自由文本，不主动重写全部 1210 条。** 理由：① 文档1 §二明标⚡️已废弃；② V20 标签系统（领域/角色/产品形态/渠道/技术五维度）已替代其职能，且已铺满（tag_l1/l2 全 1502）；③ 数据重复度高、辨识度低（你亲述"基本都一样，没价值"）；④ 推荐理由由"标签 + 联网研究"独立生成，不读此字段。→ 保留现有值作 legacy（不删以免破坏旧引用），但**不维护**；若评分中发现明显错误/占位，列入审查表交你，不静默改。
- **`role`（在 `business_tags.role`）保留作输入**：它是结构化字段（1325 家有），对应 V20 角色维度，是有用信息。默认只读；若联网研究能高把握改进 role/channel，可微调并标【动你标注】交你，不默认全改。
- 若你坚持"要更新就全更新"，那必须给强理由——我的立场是**更新 1210 条自由文本成本极高、产出极低，不如把精力压在标签系统（已在维护）和评分理由上**。

**Q：category_l1/l2 旧分类怎么处理？**
- **派生镜像**：`category_l1 = tag_l1 的首个`，`category_l2 = tag_l2 的首个/映射`。零研究成本、前端不读、留兼容。不主动维护新分类逻辑（避免与 tag 系统重复劳动，违反 V20"改动代价高/不要重复"原则）。如果你希望我"顺便也按真实业务重分一遍"，那是另一项大工程，需单独对齐——本任务默认只做低成本派生。

**Q：update_time 字段？**
- 小爽批准**新建 `update_time`**（ISO 时间戳），本任务每次改动即刷新。与别人加的 `ingest_time` 并存、不覆盖。

**Q：前端先不动？**
- 是。DB 做完你校验表格；前端（gen_enterprise.py + enterprise_scores.json）等你和 UI 编辑确认后再由我接（见 §10）。

**Q：并行架构 30 批=50 智能体？**
- 不是。见 §6：40 批 ÷ 波次，每波 3 批 = 6 并发子智能体，主智能体单线程 merge。峰值 6~8 个，不是 50/80。

**Q：会不会覆盖别的 AI 的工作？**
- 架构上不可能（见 §9）。我只写我的字段，merge 保留他人字段。

**Q：HY3 免费期拉满？**
- 是。你已授权火力全开；免费期至 2026-07-22，之后切 DeepSeek V3.2 续跑。

---

*本手册 V1 创建于 2026-07-16，依据文档1 + 代码实读 + 数据探查 + 小爽 2026-07-16 澄清。后续迭代升 V2…并保留旧版。任何规则改动记 WHY。*
