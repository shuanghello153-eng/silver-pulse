# 银发资讯库 · 字段字典与展示方案（V1 提案 · 2026-07-21）

> **性质**：这是研究员（我）交给小爽的**对齐提案**，不是定稿。待小爽拍板后再动数据。本文件不修改任何数据文件。
> **对标**：企业库字段字典_V1.md（另一 AI 已按此把 all_enterprises.json 1502 家做到 38 字段、质量过硬，作 100 分标尺）。
> **范围**：资讯库 = `data/scored_latest.json`（63 条，日期 7/1–7/8）。它是 index.html 资讯流实际展示的数据。原始 RSS 宇宙约 1031 条（raw_*.json），但那 63 条是"精选+评分"后的展示集。

---

## 〇、当前诊断（用证据，不是感觉）

资讯库 63 条 / 32 字段，**机器填充率几乎全 100%**。但抽看内容，发现 3 个**和企业库当年 `recommend` 一模一样的"假填充"陷阱**——字段有值，内容却是水/模板：

1. **`summary_cn` 是假的**：直接把标题尾巴 + 源名当摘要，不是真正的摘要。
   - 例 id159 `summary_cn` = `融资数千万，一年冲刺3000家！中老年赛道的细分生意，为何让资本押注？&nbsp;&nbsp;AgeClub` —— 这就是标题切片，不是"这条新闻讲了什么"。
2. **`recommendation` 大量模板废话**：只换赛道词，结构雷同。
   - id159：`产业资本：新一轮融资落地，资本正加码产业资本赛道；。中国银发企业可参照其融资节奏与估值逻辑，关注同赛道国内玩家。`
   - id162：`宏观趋势数据中的微观机会点，值得拆解为具体选题方向。`
   - id172：`健康服务：新品亮相。新产品发布揭示用户需求演进，国内团队可对照自身产品路线图。`
3. **`entity_name` 12 条空**（id 129/159/172/174/175/180/182/189…）：这是**资讯 ↔ 企业库的关键关联键**（同仁堂、腾讯…），空了就链不上，情报台的交叉价值断掉。

**附带 bug（非内容但影响质量）：**
- `event_type` 错分：id172「城乡三级养老服务网络建设管理指引发布」被标成`产品发布`，实为`政策法规`。
- 卡片 `tags` 没渲染：前端 `data-tags` 全空（如 id158 卡片 `data-tags=""`），只显示了 `domains`。记录的 `tags` 字段有内容但被前端丢掉了。

> ✅ **结论**：资讯库的问题不是"字段缺失"，是"内容注水 + 关联键缺失 + 展示没把有价值字段亮出来"。这正好是企业库走过的老路，门禁要跟上。

---

## 一、资讯库字段集设计（在现有 32 字段基础上补/修，不推翻评分基础设施）

图例：**✍️ 本轮要补/改** ｜ **🟡 保持，不主动动** ｜ **🔴 有坑，重点看**

### A. 基础信息（🟡 保留不动）
| 字段 | 含义 | 处置 |
|---|---|---|
| `id` / `title` / `title_cn` | 主键 / 原标题 / 中文标题 | 🟡 不动 |
| `url` / `source` / `date` | 链接 / 来源 / 日期 | 🟡 不动 |
| `region` | 国内/海外 | 🟡 不动（影响"可复制"判断） |
| `event_type` | 事件类型 | 🔴 **修正错分**（如政策法规被标产品发布） |
| `tier` / `category` | 信源层级 / 精选分级 | 🟡 不动 |
| `cluster_id` / `is_main` / `cluster_size` | 同主题聚类 | 🟡 不动（已有"同主题"徽章） |

### B. 摘要与推荐（✍️ 重写，本方案核心）
| 字段 | 含义 | 当前 | 处置 |
|---|---|---|---|
| `summary_cn` | **真正的 1–2 句中文摘要**（这事是什么 + 对银发意味着什么） | 假（标题切片） | ✍️ **重写**，杀标题切片 |
| `recommendation` | **选题推荐理由**（为什么值得写 + 什么角度 + 国内创业者能学什么） | 模板废话 | ✍️ **重写**，杀"X赛道，国内可参照"套话 |

### C. 关联与判定（✍️ 新增/补，资讯库相对企业库的差异化重点）
| 字段 | 含义 | 处置 |
|---|---|---|
| `entity_name` | 主体名（公司/机构/政策主体） | ✍️ **补满 63 条**，能识别主体的都填 |
| `entity_serial`（**新增**） | 若主体在 `all_enterprises.json` 存在，填 `#xxxx` | ✍️ 打通两库的**关键跳板**：卡片可一键跳企业库 |
| `silver_relevance`（**新增**：核心银发 / 相关 / 背景） | 资讯版"银发关联强度" | ✍️ 资讯≠投融资，噪声过滤刚需（对应企业库 `silver_verdict`） |
| `silver_reason`（**新增**） | 为什么是这个关联强度 | ✍️ 一句话依据（对应企业库 `silver_reason`） |
| `topic_angle`（**新增**） | 具体可写角度，如"写成：国内社区养老网络怎么建" | ✍️ 把"创业者价值落点"结构化 |
| `cn_takeaway`（**新增**） | 国内可借鉴点 | ✍️ 对应企业库 `recommend` 的"可复制/国内可比" |

### D. 标签（🟡 修渲染，不推翻）
| 字段 | 含义 | 处置 |
|---|---|---|
| `tags` | 银发子领域标签 | 🟡 **确保为真实子领域词**，并修复前端渲染（现在卡片没显示） |
| `domains` | 主题域 | 🟡 不动（已渲染） |

### E. 评分维度（🟡 不动，算法产出）
`final_score` / `signal_strength` / `signal_score` / `dim_scores{signal,writing,cn_fit,urgency,industry,novelty}` / `signal_breakdown` / `centrality` / `novelty` —— 全部保留，是"选题雷达"的打分底座，不碰。

---

## 二、列表展示（index.html 资讯卡片）改造方向

> 企业库有独立 `enterprise.html` 展示页 + 丰富字段（research_value、silver_verdict、highlights 时间线等）。资讯库目前在 `index.html` 资讯流以卡片展示。本方案让资讯卡片也达到同等信息密度，但**保持干净**（小爽产品感强，不堆砌）。

**卡片信息架构（自上而下）：**
1. **顶栏**：日期 · 源 · 地区 · `event_type` 徽章 · **银发关联 chip（核心/相关/背景）** → 一眼过滤噪声
2. 标题（外链）
3. **真摘要**（替代现在的标题尾巴）
4. **选题推荐理由块**（升级版：含 `topic_angle` 角度 + `cn_takeaway` 国内借鉴）
5. **主体企业 badge**：有 `entity_serial` 时做成**跳企业库链接**（打通两库）
6. 6 维评分 chip（保留，有用）
7. `tags` 真实标签（修复渲染）
8. 收藏 / 隐藏 / 备注 / 已读（保留）

**可选增强（待定）**：是否给资讯库也建一个独立 `news.html` 列表页（对称于 `enterprise.html`）？当前 index.html 已承载资讯流，优先级低于字段质量，先不做，除非你要。

---

## 三、执行方式（吸取 7/20 的 429 限流教训）

1. 本字典定稿（你拍板后）。
2. 63 条不多，我**自己逐条联网核实 + 写**（不狂开后台工人导致限流），质量优先，每天可控批量。
3. **门禁脚本**：写一条校验——`recommendation` 非空非模板 / `summary_cn` 非空非标题切片 / `entity_name` 非空 / `silver_relevance` 合法值。避免重蹈企业库 `recommend` 覆辙（先满后水）。
4. 改完 `scored_latest.json` → 跑 `generator.py` 重生成 `index.html` → **本地预览给你看**；**部署（deploy_ghpages）等你拍板，我不擅自发**。

---

## 四、待小爽拍板（我倾向的默认，否决即可）

1. **范围**：聚焦现有 63 条做 100 分（推荐） vs 把 1031 原始更多纳入？→ 我倾向 **63 条**（它们是网站实际展示的，做出 100 分最有感；原始池可后续再说）。
2. **打通 `entity_serial` 跳转**？→ 我**强烈推荐**（情报台"资讯↔企业"交叉浏览的核心价值）。
3. **银发关联判定 `silver_relevance` 要不要**？→ 我**推荐要**（资讯≠投融资，政策/背景/泛老龄化噪声多，过滤是刚需）。
4. **企业库那 2 处 `funding.amount` 错填**（#0001 AgeClub=`银创资本投资`、#0016 小年糕=`D轮融资（一线投资机构）`），要不要我整理成审查表交你 / 另一 AI？→ 我不改企业库，可整理。

---

## 🔴 重点提醒（写资讯库时必守）
1. `summary_cn` 绝不能等于标题切片——必须是从正文/外链读来的"这条新闻讲了什么"。
2. `recommendation` 禁"X赛道，国内可参照其融资节奏"式套话；每条必须点出**具体角度 + 具体国内借鉴对象**。
3. `entity_serial` 只填"确在 all_enterprises.json 中"的主体，宁空不瞎填，避免链到错误企业。
4. `silver_relevance` 按实质判断：直接服务银发=核心；泛老龄化/通用科技有启发=相关；仅背景/宏观=背景。default 从严。
