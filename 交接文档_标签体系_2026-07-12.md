# Silver Pulse 标签体系 · 交接文档（2026-07-12）

> **写给下一任 AI（重要）：这是背景与已知问题清单，不是指令。**
> 请独立核查原始数据与规则文件，不要默认文中任何结论一定正确。前任 AI 反复犯"机械执行、伪校验、擅自改名"等错误（见第七节），请勿重蹈。小爽明确表示"希望下一任不会严格按前任说的做"——意思是：独立思考、拿证据、重大改动先对齐，而不是照单全收。

---

## 一、项目是什么

- **Silver Pulse** = 银发经济投融资资讯聚合看板 + 企业数据库。服务对象：商业写作者"小爽"，用于选题决策。本质是一个"选题情报台"。
- 三层愿景：资讯看板 → 企业数据库 → 供需对接（当前主要在第二层）。
- 技术形态：Python 静态生成器，输出到 `output/`，部署在独立 `gh-pages` 分支（GitHub Pages）。`main` 分支不跑自动化数据（5 个自动化已于 2026-07-10 起全部 PAUSED）。
- LIVE：https://shuanghello153-eng.github.io/silver-pulse/

## 二、标签体系架构（当前终态，commit `139a588` 之后）

三层分工（务必分清，别混）：

1. **分类 `category_l1` / `category_l2`** = 源数据旧字段，含大量陈旧值（含银发/老年旧词），**前台不展示**，但库里未清，是脏数据隐患（见第五节 D）。
2. **标签 `tag_l1`（13 组一级）/ `tag_l2`（58 个原子二级标签）** = 清洗后、前台真实展示的标签。新增/修正企业标签都动这里。
3. **同类词 `tag_synonyms.json`** = 仅用于搜索扩展。结构：`canon 字符串 -> 同义词集合`。canon 必须等于实际存在的某个 `tag_l2`。

当前规模：1325 家企业；一级实际挂企业 12 个（精神健康 0 家）；二级 58 个。

关键文件：
- 标签生成真相源：`_rebuild_tags.py`（含 MERGE / BRAND_OVERRIDE / GHOST_MERGE / L2_RENAME / L1_RENAME / STRUCT_MERGE / STRUCT_DROP 与 `MISLABEL_FIX` 企业级修正字典）。**重跑会覆盖 `all_enterprises.json` 的 tag 字段，务必先备份。**
- 校验：`validate_tags.py`（9 项，第 4 项已修伪绿灯）。
- 前台：`gen_enterprise.py` 卡片 `data-cat`/`data-l2` 读清洗后 `tag_l1`/`tag_l2`（474 家多一级，空格拼接多值，JS 按 split 多值匹配）。

## 三、核心规则与"为什么"（前任 AI 在这里栽过跟头，请务必理解动机）

1. **命名禁含**：国家/地区名、品牌/产品名、单字碎片、虚词。
2. **标签不与一级同名**；不与列表字段重复；企业可多标签（单企业 ≤5）；最小颗粒度（不退化成兜底伞词，也不碎成无意义单字）；≤2 家幽灵不当标签。
3. **【关键·关于"银发/老年"——被前任 AI 严重误解的一条】**
   - 小爽原意：**本库所有企业都属银发经济**，所以在"每一个标签前都加'银发/老年'前缀"是**冗余**的，才要求去掉。目的 = **去冗余**，不是禁掉这个概念。
   - 前任 AI 的错误：① 把"去掉银发/老年"理解成机械删除、又到别处加回——例如把 `老年文娱` 改名建议成 **`乐龄文娱`**。"乐龄"本质还是"老年"的同义词，纯属换汤不换药，且**擅自发明了小爽词表里根本没有的词**。② 三个含银发/老年的标签至今仍违规留在库里没处理。
   - **正确做法**：标签描述业务本身（文娱/零售/护理…），老年语境由整个库隐含。对**既含**银发/老年的旧标签，是否改名、改成什么，**必须由小爽拍板，AI 不得自创"乐龄"之类新词**。
4. **同类词规则**：只做搜索扩展；**不能退化成"自身=同义词"**（如 canon=慢病管理 但同义词只有 [慢病管理] 零价值）；不能与一级同名；噪声词（废弃/综合养老/反共识/ToB 等）必须清。

## 四、当前已验证状态（独立子代理审计 + 修复后）

- `validate_tags.py` 实跑 **9/9 全绿**，第 4 项不再伪绿（10 个退化 canon 已补全别名）。
- 退化同类词 **0 个**；跨 canon 碰撞 **0**；孤儿 canon **0**；0 标签企业 **0**；单企业 >5 标签 **0**。
- **79 家企业缺失标签已补**（只加不删，同步补 tag_l1，不破 5 上限）。
- 噪声同义词已清（银发日用下 商品品牌/面包店咖啡、银发零售下一堆渠道词、远程医疗下 虚拟 等）。
- 跨层污染已修（健康监测 别名里的"智能科技"已删）。
- 线上 gh-pages **已部署**，卡片筛选 bug 已修；但**线上数据未含 `139a588` 之后的修复**（见第八节，待推送+部署）。

## 五、已知错误 / 必须修正项（下任 AI 接手先处理这些）

### A. Carewell 错标（高优先）
- 现状：`Carewell` → `tag_l2=['辅具','银发日用']`，`tag_l1=['产业服务','消费品']`。
- 小爽指正：Carewell 本就是 **银发零售**（成人/护理用品零售），不该是 银发日用/辅具。
- 修正：Carewell 应改为 `['银发零售']`（L1 消费品/渠道零售）。**两处都要改**：① `data/enterprise/all_enterprises.json` 当前值；② `_rebuild_tags.py` 的 `MISLABEL_FIX` 里 `Carewell→银发日用/辅具` 改成 `→银发零售`，否则重跑管线会再错。

### B. 三个含"银发/老年"的标签仍违规（待小爽定名）
- `老年文娱`(150)、`银发零售`(15)、`银发日用`(18) 三个 L2 仍带银发/老年，违反命名规则。
- 前任 AI 曾建议 老年文娱→乐龄文娱 / 银发零售→适老零售 / 银发日用→适老日用 —— **"乐龄"是错误自创词，绝不可采用**；"适老"是否合规由小爽定。
- 现状：这三个标签**尚未改名**（建议只停留在纸面，未落地数据）。下任 AI 不得自行改名，必须列候选名交小爽裁决。

### C. 药品类企业疑似错标（小爽语音提及"那个药品的"）
- 19 家描述含"药/制药/处方"且语境含银发/养老。其中多家"药业"公司被标成 `保健品`（如 **健特药业、百善药业、同仁堂、一味盛长、人寿堂**），但药企 ≠ 保健品，处方药应单独归类。
- 下任 AI 应逐家复核这 19 家（可重跑：`python -c "..."` 用正则 `药|pharma|drug|处方|制药` 扫 description），把处方/制药企业从"保健品/营养食品"中剥离，具体归类由小爽定。**不要静默改，列清单交小爽。**

### D. category 旧字段脏值
- `category_l1/l2` 仍残留 银发消费/老年文娱/行业服务/综合养老/特医食品/康复设备 等旧值。前台不展示，但重跑管线或误用时会产生脏数据。建议：映射进 tag 后清空，或经小爽确认后清空。本轮未动。

### E. 79 家补标未固化进管线
- `139a588` 是直接在 `all_enterprises.json` 手动补的。若未来重跑 `_rebuild_tags.py`，这些补标会被覆盖（除非加进 `MISLABEL_FIX`）。自动化现暂停，暂无碍，但交接时应把 79 家补标固化进 `_rebuild_tags.py` 的 `MISLABEL_FIX`。

## 六、前任 AI 反复犯的错误（给下任的警示，请勿重蹈）

1. **机械执行、不传 WHY**：给子代理只下步骤、不给背景/原则/动机，导致子代理复制我的错误（例：子代理自作主张建议"乐龄文娱"）。→ 下任：给协作代理下任务必须附"为什么"和"不可越界的红线"。
2. **伪校验/盲区**：`validate_tags.py` 第 4 项原把"同义词含自身"算通过，长期掩盖 10 个退化 canon，前任却一直报"9/9 全绿"误导小爽。→ 下任：任何"全绿"都要独立抽查，不盲信脚本。
3. **等点名才改**：小爽批评"说1改1、说2改2、其他完全不管"。→ 下任：按规则主动全量自检，主动修同类问题。
4. **擅自改名/自创词**：把 老年文娱 建议成词表里没有的"乐龄文娱"。→ 下任：命名改动必须小爽拍板，不得自创词。
5. **只报"通过"不报细节**：小爽要求改完大改动必须附**完整映射表**（一级→二级→同类词全量无省略）+ 9/9 校验清单，不能只说"校验通过"。→ 下任：交付必带完整证据。
6. **改完不部署/部署不同步**：曾出现线上 HTML 与本地数据不一致。→ 下任：数据改动后必须重生成 HTML 并部署，且确认线上与本地一致。
7. **无视用户情绪与铁律**：小爽多次强调"别用 AskUserQuestion 做重大方案决策""先给方案确认再执行""命名禁用最终版/完整版，用 V 号迭代"。→ 下任：方案类先写详细文档对齐，不用四字选择题；版本用 V 号，旧版保留。

## 七、如何验证 / 关键脚本 / 部署

- 校验：`python validate_tags.py`（应 9/9 全绿，且第 4 项能真检出退化 canon）。
- 审计（可复用）：`audit_tags.py`（含 PHRASE 词典，逐家比对描述与标签）。
- 标签管线（真相源）：`_rebuild_tags.py`。**重跑前务必备份 `all_enterprises.json`**。
- 重生成标签表文档：`_gen_v3.py` → `output/标签体系_V3.md`（建议改名 V4 并刷新，因 V3 是 pre-139a588 旧快照）。
- 前台生成：`gen_enterprise.py` → `output/enterprise.html`；主站 `generator.py` → index/about。
- 部署：`deploy_ghpages.py`（自带重试，经代理 127.0.0.1:7890 常 reset，脚本已重试用）。
- Python 用托管版：`C:/Users/shuan/.workbuddy/binaries/python/versions/3.13.12/python.exe`。

## 八、待小爽拍板的开放问题

1. 三个银发/老年标签（老年文娱/银发零售/银发日用）最终改名方案？是否接受"适老"前缀？
2. "平台/系统"结尾标签（护理平台/照护系统/社交平台）是否算违规？
3. 药品类企业（尤其 药业 被标 保健品 的）如何归类？是否要"处方药/制药"独立标签？
4. category 旧字段脏值：清空还是映射进 tag？
5. 79 家补标是否固化进 `_rebuild_tags.py` 的 `MISLABEL_FIX`？
6. 是否推送 main + 部署 gh-pages（当前 `139a588` 仅本地，线上未含本轮修复）？

## 九、给下任 AI 的工作建议

- 先读 `TAGGING_RULES.md` + 本文档，**不要急着改数据**。
- 任何改动先按"已定/待补"清单对齐；重大命名/结构改动先写方案文档交小爽确认，不用 AskUserQuestion 四字题。
- 独立核查：不默认前任结论，用脚本 + 原始数据自己跑一遍。
- 交付必带：完整标签大表（一级→二级→同类词全量无省略）+ 9/9 校验清单 + 改动前后对照。
- 小爽沟通偏好：说人话、给完整过程、主动挖错、改用户手标内容须显式标注【动你标注】并给理由、拿证据反驳不盲从。
