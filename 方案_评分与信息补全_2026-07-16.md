# 方案 · 企业库全量评分推荐 + 信息补全 + 标签审查（2026-07-16 初稿）

> 状态：待小爽确认。本文是**执行前方案**，不动任何数据、不部署。
> 配套依据：文档1《评分推荐规则 2026-07-12 版》、文档2《TAGGING_RULES_V20》。
> 落地前我已先读底层代码 + 真实数据，发现 4 处文档与代码冲突（见第二章），必须先和你对齐。

---

## 一、项目背景理解（为什么做、做完是什么样）

### 1.1 这个库是什么
Silver Pulse 是小爽的「银发经济选题情报台」——上千家企业里，快速挑出"值得写、且能借鉴到中国"的标的。企业卡片上那行**推荐理由 + 研究价值分**，就是小爽早上打开网站最先看的东西。

### 1.2 这次任务的三个动作，本质是"同一遍工作"
1. **评分 + 推荐理由**（按文档1）：给每家企业算"值不值得研究"的分，并写出一段"为什么值得写、对中国有什么借鉴"的自然语言理由。
2. **信息校验 + 字段补全**：评分过程要"对企业做全面信息搜索，信息来源充分"——搜出来的东西正好用来**补全缺失字段**（中文名、成立年、融资、投资方、新闻覆盖、亮点、深度文章链接等）。
3. **标签审查（只读、不改）**：标签是另一个 AI 在维护，我**不碰** `tag_l1/tag_l2/business_tags` 的写入，只产出一张"建议新增/删除/优化"的表交你拍板。

三者关联逻辑：
- 评分的"信息量 / 差异化 / 可复制"三维**必须联网搜过**才有依据 → 顺手就把字段补全了；
- 补全后的字段又让下一轮评分更准 → 是同一遍闭环，不是三件独立事。

### 1.3 这个目标"做对"的标准
- 分数和理由能让小爽一眼判断"这家我写不写"；
- 字段真实、可追溯（每条关键数据有来源链接，不编）；
- 不和其他正在跑的 AI / 未部署代码冲突；
- 全量 1502 家，**不漏一家、不幻觉一家**。

---

## 二、我查到的真实代码/数据现状（重点：和文档的 4 处冲突）

> 用户的铁律："必须看底层代码，不能只信文档"。我照做了，果然文档和代码对不上。

### 2.1 数据真相源 = `data/enterprise/all_enterprises.json`
- **1502 家**（不是文档说的 1328——信源治理 W1-W4 又入库了一批）。
- 文件 07-16 16:44 刚被改过（疑似别的 AI 刚动过标签/信息）。
- 字段填充率（实测，非空）：

| 字段 | 填充率 | 说明 |
|---|---|---|
| serial/name/region/tag_l1/tag_l2/tags/category_l1/l2 | 100% | 标签体系已建好（V20） |
| description/desc_cn | 100% | 有，但部分是企业模板占位 |
| value_score | 100% | **旧分**，量纲 0-100，来源存疑 |
| payor_model | 100% | 别人新加的字段（我不动） |
| business_model_cn / business_model | 88% / 80% | 很多是"B2B 行业媒体/数据服务"通用占位 |
| highlights | 86% | 质量差，多"成立于 2016 年"废话 |
| recommend | 86% | 旧质量理由 |
| crunchbase_url / website_url | 78% / 69% | 可补 |
| founded | 66% | 可补 |
| name_cn | 39% | 可补（中文名） |
| funding_latest / funding_total | 59% / 52% | 结构错填（amount 被填成投资方名） |
| stage | 26% | 945 家"未披露"，大缺口 |
| investors | 26% | 大缺口 |
| news_coverage | 31% | 仅 480 家；文档要求全量 |
| related_news_ids | **0%** | 从未写入 all_enterprises.json |
| deep_article_links | **0%** | 从未写入 |
| 额外字段 | ingest_time(177) / needs_review(24) | 别的 AI 加的，我不动 |

### 2.2 冲突 1（最关键）：评分代码 ≠ 文档1
- **文档1** 规定"四阶段"：阶段3 信号强度(0-10,脚本) + 阶段4 AI 四维(信息量/差异化/可复制,0-10) → 综合=信号×30%+信息量×30%+差异化×20%+可复制×20%（**0-10**）。
- **但代码里**：阶段4 明确标着 `❌ 待建`，根本没实现。
- **实际在跑的是** `selection/enterprise_score.py`：一套**独立的** research_value(0-100)，公式 V1规模+V2信息+V3模式+V4可借鉴 + 事件boost + 海外镜像 + 证据厚度。**但它不联网、且 V2/V3/V4 读的是已废弃的 `tags`/`business_model` 字段**，等于空壳。
- **前端 `gen_enterprise.py`（正在重写的 UI）吃的是 `research_value`(0-100)**，回退才用 `value_score`。
- 结论：文档1 的评分公式是"设计稿"，代码里没有；代码里那套又依赖废弃字段、不联网。两边对不上，必须先定基准（见第五章 Q1）。

### 2.3 冲突 2："一级/二级分类弃用"指哪个？
- 数据里有**两套**分类：
  - `category_l1/l2` = 选题库旧分类，文档说"保持不动"；
  - `tag_l1/tag_l2` = V20 刚重做的核心标签（8 一级/95 二级，另一个 AI 正在维护、9/9 校验通过）。
- 你说"一级、二级分类目前处于弃用状态"——到底是哪一个？这决定我标签审查的范围是只审 `business_tags`，还是连 `tag_l1/tag_l2` 也提建议（见 Q2）。

### 2.4 冲突 3：`business_model` 文档说废弃，但数据里 80% 填充且可读
- 文档1 §二 把 `business_model/business_model_cn` 列为"⚡️已废弃，不补全"；
- 但 V20 §2.3 又说"技术维度默认不标，SaaS/AI 按机制降级不标或并入具体产品词"，且它目前是卡片上重要的可读信息。
- 你让我"关注 business model、role 等标签的必要性"——我的理解是**不删，但重写质量差的占位**。需你确认（见 Q3）。

### 2.5 冲突 4：别的 AI 正在动代码，且线上未部署
- `git status` 显示 `gen_enterprise.py` / `ui_common.py` **被改了没提交**（mtime 17:18，刚刚），说明有进程正在重写 UI。
- `PROJECT_STATUS.md` 明写：UI 大改(T22-T33) 代码改完但**未重新生成 HTML、未部署**，线上还是旧版。
- `enterprise_scores.json` 最后更新 07-10（在 W1-W4 入库之前，已过期）。
- 历史教训：`PROJECT_STATUS` 记载 T40"全量 news_coverage 扩到 1118 家"**上一个 Agent 崩溃未完成**——所以现在 news_coverage 只有 480 家。这提醒我：必须分片、可断点续跑、每批落盘，不能一把梭。

---

## 三、详细执行方案

> 以下流程以"文档1 为基准（Q1 选 A）"展开；若你选 B 则只改评分公式来源，其余不变。

### 3.1 评分流程（四阶段，落地版）
**阶段1-2（准入）**：企业库已先验收入库，跳过资讯那套初筛；企业侧只做"银发信号铁律"复核（非银发企业按铁律删，不进兜底桶——这是另一个 AI 的活，我只读不参与）。

**阶段3 信号强度（脚本，0-10）**：对企业这样算（文档1 阶段3 是给资讯的，企业要对齐）：
- IPO/上市 → +6
- 融资：读 `funding_latest.amount`/`funding_total`，按文档1 的海外美元/国内元双阈值表给分
- 收购：读 `funding_latest.acquisition` 金额分级
- 时效：以"最新关联资讯日期 或 成立年"算近1周/月/半年…的 recency
- 信源起点：企业无信源等级概念，该项归零或取 `source` 健康度（待定）
- 合计 clamp 0-10。

**阶段4 AI 四维（联网，0-10，本任务核心）**：每个 worker 子智能体对一家企业：
1. **联网搜前几页**：官方/年报/招股书/Crunchbase/新闻，评估
   - 信息量：资料多寡、是否上市公司/热点（决定好不好写）
   - 差异化：商业模式反不反共识、起落是否剧烈
   - 可复制：国内有无同类赛道、支付方差异（Medicare/自费 vs 医保/自费）、是空白还是红海
2. 综合 = 信号×0.3 + 信息量×0.3 + 差异化×0.2 + 可复制×0.2（0-10）
3. **推荐理由**（文档1 §四.5）：一段自然流畅的话，把四维判断融进去讲"为什么值得写、从中国什么角度切入"；**不重复卡片已展示的结构化信息**（融资金额/轮次/标题/日期等），点到为止。

**落库**：综合分 ×10 存 `research_value`(0-100) 进 `enterprise_scores.json`，并存 `value_score`(0-100) 进 `all_enterprises.json` 作前端兜底；理由存 `recommend`。

### 3.2 信息校验策略（边搜边校验）
- 每家企业联网搜到的关键事实（成立年、融资、投资方、官网、中文名）**与库内现有字段逐条比对**：
  - 库内空 → 填；
  - 库内错（如 `funding_latest.amount` 是投资方名）→ 纠；
  - 库内与权威源冲突 → 以权威源为准，标注来源链接。
- **不编**：搜不到就标"待补充"，不填假数（铁律）。
- 证据留存：每条补全附来源 URL，写入该企业的 `news_coverage.latest_news` 或 `deep_article_links`。

### 3.3 字段补全计划（一轮补全/校正，清单）
| 优先级 | 字段 | 动作 |
|---|---|---|
| P0 | news_coverage | 全量联网补（目前 31%→100%），含 news_count/quality/latest_news |
| P0 | recommend | 按文档1 §四.5 重写（86%→质量达标） |
| P0 | research_value / value_score | 新分写入 |
| P0 | update_time（新建） | ISO 时间戳，每次本任务改动即刷新 |
| P1 | stage | 945 家"未披露"→ 通读后判（种子/天使/A轮/成长期/已上市…） |
| P1 | investors | 26%→补，统一为数组（只记最新一轮/收购方） |
| P1 | name_cn | 39%→补中文名 |
| P1 | funding_latest / funding_total | 纠结构错填 + 补缺失 |
| P1 | highlights | 重写废话（"成立于2016年"→真实亮点） |
| P2 | website_url / crunchbase_url / founded / desc_cn / business_model_cn | 补缺失、重写占位 |
| 不碰 | tag_l1/tag_l2/business_tags/category_l1/l2/payor_model/ingest_time/needs_review | 别人维护，我只读 |

### 3.4 标签审查方案（只读、不改、出表）
- 对每个企业检查 `business_tags`(customer/role/channel) + `business_model` + `tag_l1/tag_l2`（范围取决于 Q2）：
  - **建议新增**：企业明显属于某未建标签 → 列"企业名+建议词+理由"（不自造，交你拍板）
  - **建议删除**：标错（如 V20 说的保健品桶里混进 Oak Street Health 类）→ 标【动你标注】+理由
  - **建议优化**：role 缺失/错配、business_model 占位废话
- 输出一张总表（企业数 + 每条建议 + 证据），**绝不写回数据**。

### 3.5 并行子智能体架构（你明确要求）
- **分片**：1502 家切成 **~30 批，每批 ~50 家**。
- **每批 = 1 worker + 1 validator（独立）**：
  - worker：联网打分 + 补全 + 标签建议，**只写到一个独立的 `patch_batchXX.json`**（不动 `all_enterprises.json` 本体，防并发写冲突）。
  - validator：独立复核该批——有没有幻觉（理由/字段与来源对不上）、字段是否一致（金额格式、数组）、推荐理由是否合规（不重复卡片信息）、标签建议是否非破坏性。输出"通过/打回"清单。
- **主智能体（我）汇总**：
  1. 收齐 30 批 patch → 校验 `count(patch)==1502`、每家 serial 都在、无字段丢失；
  2. **merge 回写**：只写我的字段（见 3.3），**完整保留别人的 tags/business_tags/payor_model/ingest_time/needs_review**；
  3. 跑 `rebuild_association.py` 补 `related_news_ids`（现成工具，写 enterprise_scores.json）；
  4. **全局校验**：schema/量纲/范围/一致性 100% 复扫（不是抽样）；
  5. **抽审样本**：每批抽 3-5 家人工级复核；
  6. 出"改动 diff 报告 + 标签建议总表 + 未达标清单"交你。

### 3.6 怎么保证"正确、没遗漏"（你的核心关切）
1. **manifest 全覆盖**：开工先建 `1502 家 serial 清单`，每批完成打勾，结束核对"已处理==1502、无遗漏"。
2. **双重校验**：worker 自校验 → validator 独立复核 → 主智能体全局复扫 → 你终审。四道防线。
3. **不并发写本体**：所有子智能体只写各自 patch 文件，最后由我单线程 merge，根除"两个 AI 同时写 all_enterprises.json 互相覆盖"。
4. **可断点续跑**：每批落盘即 checkpoint，某批崩了只重跑该批（吸取 T40 崩溃教训）。
5. **diff 报告**：交付时附"改了哪些字段、哪些企业、哪些标了【动你标注】"，你一眼能审。

### 3.7 冲突规避（和其他 AI / 未部署代码）
- **标签**：绝对不碰写入，只产出建议表。
- **gen_enterprise.py / ui_common.py**：当前有未提交改动（别人在重写 UI）。我**本轮绝不自行重新生成 HTML 或部署**。我的产物先落数据文件 + 一份本地可预览 HTML（不 push）。
- **enterprise_scores.json**：我会重新生成（含 research_value + related_news_ids），但先备份、且和 `rebuild_association.py` 输出对齐。
- **部署**：等你和那个 UI 编辑都确认后，再统一走 `deploy_ghpages.py`。

---

## 四、分阶段排期（HY3 免费期到 2026-07-22，约剩 6 天）
- **阶段A（先保质量）**：挑"值得深度写"的高分候选（用现有 research_value/信号强度 筛，约 300-500 家）做全量联网打分 + 全字段补全 + 标签审查。
- **阶段B（扩面）**：剩余企业做轻量补全（补 stage/investors/name_cn/news_coverage）+ 脚本信号强度分 + 模板化理由。
- 免费期火力全开；若 07-22 前做不完，07-22 后切 DeepSeek V3.2（低成本）续跑。

---

## 五、需要你拍板的疑问（附我的倾向）

**Q1 · 评分基准（最关键）**
文档1 四阶段(0-10) 代码里没实现；实际代码是 enterprise_score.py 的 research_value(0-100, 依赖废弃字段、不联网)。你希望：
- (A) 严格按文档1 重建四阶段（信号强度脚本 + AI 四维联网），分接到前端（写 research_value 0-100 + value_score 兜底），退役旧 enterprise_score.py；
- (B) 沿用现有 research_value(0-100) 为基准，只把它的 V2/V3/V4 从"读旧字段"升级成"联网真实信息"。
- **我的倾向：A**。你明确说"依据文档1"，且 A 让评分真正用上联网信息（正好和信息补全同一遍）。代价是要动前端取数逻辑，会和正在进行的 UI 重写有交集——需你授权我同步改 gen_enterprise.py 的取数字段。

**Q2 · "一级/二级分类弃用"指哪个？**
是 `category_l1/l2`（选题库旧分类，文档说保持不动），还是 `tag_l1/tag_l2`（V20 核心标签，另一个 AI 在维护）？这决定我标签审查范围。
- **我的倾向**：你说的大概率是 `category_l1/l2`（旧分类确实边缘化）；`tag_l1/tag_l2` 我建议仍纳入"只读审查"（因为我在打分时会发现错标，可一并提建议，但绝不写回）。

**Q3 · business_model 字段去留**
文档1 说废弃，但数据里 80% 填充且可读。
- **我的倾向**：**不删**，但重写质量差的占位（"B2B 行业媒体/数据服务"这种），并核对 `business_tags.role` 是否和 business_model 一致。你说的"关注 role 等标签必要性"我理解为：role 来自 business_tags，需要补/核，而非废掉 business_model。

**Q4 · "更新时间"字段命名**
数据里已有别人加的 `ingest_time`(177家) 和 `needs_review`(24家)。
- **我的倾向**：新建统一字段 `update_time`（ISO 时间戳，本任务每次改动即刷新），与 `ingest_time` 并存、不覆盖别人的字段。

**Q5 · 部署授权**
gen_enterprise.py 正被另一个进程改到一半没提交，线上还是旧版。
- **我的做法（非问句，先告知）**：本轮**绝不自行部署**。产物落数据 + 本地预览 HTML，等你和 UI 编辑都确认后再统一部署。若你希望我直接用我这边逻辑重新生成 HTML 也可以，但需你明确说。

**Q6 · 全量 vs 分阶段**
1502 家全量联网打分 + 补全，6 天免费期可能不够。
- **我的倾向**：分阶段（见第四章），先保 300-500 家高分企业的质量，再扩面。

**Q7 · 并行架构 + 额度**
我计划 30 批 ×(worker+validator) 子智能体并行，最后我汇总校验。
- 请你确认这个架构 OK；另：worker 联网搜索会消耗积分/额度，免费期内我默认火力全开——**是否有额度上限我需要你知道的提醒**？没有的话我就按"免费期拉满"推进。

---

*待你回复 Q1-Q7（尤其 Q1/Q2/Q3）后，我出拆解版执行清单（含每批 serial 范围、worker/validator prompt 模板），再开工。*
